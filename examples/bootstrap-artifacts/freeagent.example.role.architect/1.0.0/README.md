# Architect role example

This package is a declarative example, not a privileged Role runtime. It
provides one ordered `context.provide/v1` contribution through the same module
contract used by every other context provider. It requests no permissions and
contains no executable code.
