# Implementation skill example

This package is a static Skill example expressed through the universal module
contract. It contributes one ordered `context.provide/v1` value, requests no
permissions, and introduces no Skill-specific runtime or compiler behavior.
