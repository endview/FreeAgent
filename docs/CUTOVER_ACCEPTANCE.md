# CUTOVER_ACCEPTANCE

状态：`S1_ACCEPTED_DEVELOPMENT_BASELINE / S2.1_CONTEXT_COMPILER_ACCEPTED_DEVELOPMENT_SLICE / S2_MODEL_PROFILE_ACCEPTED_DEVELOPMENT_SLICE / S2_RAG_ACCEPTED_DEVELOPMENT_SLICE / S2_MEMORY_ACCEPTED_DEVELOPMENT_SLICE / S2_ACTION_GATEWAY_ACCEPTED_DEVELOPMENT_SLICE / S2_MCP_LOCAL_PROCESS_STDIO_TOOL_ACCEPTED_DEVELOPMENT_SLICE / S2_CHANNEL_ACCEPTED_DEVELOPMENT_SLICE / S2_PARENT_CHILD_COMPOSITE_ACCEPTED_DEVELOPMENT_SLICE / S3_FAIR_SCHEDULER_ACCEPTED_DEVELOPMENT_SLICE / S3_COMPOSITE_REVIEW_GATE_ACCEPTED_DEVELOPMENT_SLICE / EXACT_ADAPTER_LAZY_MATERIALIZATION_ACCEPTED_DEVELOPMENT_OPTIMIZATION_SLICE / W2_D_LOCAL_ASSEMBLY_ACCEPTED_DEVELOPMENT_SLICE / W2_E2_TRUSTED_TEXT_STATS_APPLY_ACCEPTED_DEVELOPMENT_SLICE / W2_E3_WORKSPACE_CHANNEL_APPLY_ACCEPTED_DEVELOPMENT_SLICE / W2_E4_DEEPSEEK_MODEL_REPLACEMENT_ACCEPTED_DEVELOPMENT_SLICE / W2_E5_A_REQUIRES_PERMISSION_GRANT_ACCEPTED_DEVELOPMENT_SLICE / W2_E5_B_DOCUMENT_INSIGHT_DUAL_PORT_ACCEPTED_DEVELOPMENT_SLICE / W2_R1_REMOTE_ACTION_HOST_ACCEPTED_DEVELOPMENT_SLICE / W2_R2_WASM_HOST_ACCEPTED_DEVELOPMENT_SLICE / W2_R3_UNTRUSTED_MODULE_ISOLATION_ACCEPTED_DEVELOPMENT_SLICE / W2_U0_SIGNING_SOURCE_CONTRACT_ACCEPTED_DEVELOPMENT_SLICE / W2_U1_SIGNING_SOURCE_POLICY_ACCEPTED_DEVELOPMENT_SLICE / W2_U2_DISCOVERY_SNAPSHOT_ACCEPTED_DEVELOPMENT_SLICE / W2_U3_UPGRADE_REVIEW_ACCEPTED_DEVELOPMENT_SLICE / W2_U4_OPERATOR_APPLY_ACCEPTED_DEVELOPMENT_SLICE / W1_COMPLETE_REAL_DEEPSEEK_50 / W3_COMPLETE_ACCEPTED_DEVELOPMENT_SLICE / W4_L1A_KNOWLEDGE_PROPOSAL_STORE_ACCEPTED_DEVELOPMENT_SLICE / W4_L1B_STATIC_SKILL_PROPOSAL_STORE_ACCEPTED_DEVELOPMENT_SLICE / W4_L2_LEARNING_REVIEW_ACCEPTED_DEVELOPMENT_SLICE / W4_L3_LEARNING_VERSION_ACCEPTED_DEVELOPMENT_SLICE / W4_L4_LEARNING_CYCLE_ACCEPTED_DEVELOPMENT_SLICE / W4_COMPLETE_ACCEPTED_DEVELOPMENT_SLICE / W5_D1_COLLABORATION_REPAIR_ACCEPTED_DEVELOPMENT_SLICE / W5_X1_WORKSPACE_TRANSFER_ACCEPTED_DEVELOPMENT_SLICE / W5_F1_COLLABORATION_STABILITY_ACCEPTED_DEVELOPMENT_SLICE / W5_COMPLETE_ACCEPTED_DEVELOPMENT_SLICE / W6_1_APPLICATION_SERVICES_READ_API_ACCEPTED_DEVELOPMENT_SLICE / W6_2_CONFIRMATION_CONTRACT_ACCEPTED_DEVELOPMENT_SLICE / W6_2_DURABLE_RECEIPT_SCHEMA_ACCEPTED_DEVELOPMENT_SLICE / W6_2_MODULE_DISABLE_MUTATION_WIRING_ACCEPTED_DEVELOPMENT_SLICE / W6_3_WEB_SHELL_READ_ONLY_OVERVIEW_ACCEPTED_DEVELOPMENT_SLICE / W6_4_MODULES_CONFIGURATION_UI_ACCEPTED_DEVELOPMENT_SLICE / W6_5_SERVER_OWNED_MODULE_ARTIFACT_INGRESS_ACCEPTED_DEVELOPMENT_SLICE / CUTOVER_NOT_APPLICABLE_NEVER_DEPLOYED`  
Machine status: `CUTOVER_NOT_APPLICABLE_NEVER_DEPLOYED`  
Deployment status: `NEVER_FORMALLY_DEPLOYED`  
S2 entry: `APPROVED`  
操作员确认：`OPTION_A_CONFIRMED / 2026-08-02T18:09:03Z`  
证据快照：`2026-08-17`（Asia/Shanghai）  
W1 Conversation status: `W1_COMPLETE_REAL_DEEPSEEK_50`  
W3 RAG/Memory status: `W3_COMPLETE_ACCEPTED_DEVELOPMENT_SLICE`  
W2-D Local Assembly status: `W2_D_LOCAL_ASSEMBLY_ACCEPTED_DEVELOPMENT_SLICE`  
W2-E2 trusted text.stats Apply status: `W2_E2_TRUSTED_TEXT_STATS_APPLY_ACCEPTED_DEVELOPMENT_SLICE`  
W2-E3 Workspace Channel Apply status: `W2_E3_WORKSPACE_CHANNEL_APPLY_ACCEPTED_DEVELOPMENT_SLICE`  
W2-E4 DeepSeek Model replacement status: `W2_E4_DEEPSEEK_MODEL_REPLACEMENT_ACCEPTED_DEVELOPMENT_SLICE`  
W2-E5-A Requires/permission grant status: `W2_E5_A_REQUIRES_PERMISSION_GRANT_ACCEPTED_DEVELOPMENT_SLICE`  
W2-E5-B Document Insight dual-Port status: `W2_E5_B_DOCUMENT_INSIGHT_DUAL_PORT_ACCEPTED_DEVELOPMENT_SLICE`  
W2-R1 REMOTE Action Host status: `W2_R1_REMOTE_ACTION_HOST_ACCEPTED_DEVELOPMENT_SLICE`  
W2-R2 WASM Action Host status: `W2_R2_WASM_HOST_ACCEPTED_DEVELOPMENT_SLICE`  
W2-R3 untrusted WASM pure-compute status: `W2_R3_UNTRUSTED_MODULE_ISOLATION_ACCEPTED_DEVELOPMENT_SLICE`  
W2-U0 supply contract status: `W2_U0_SIGNING_SOURCE_CONTRACT_ACCEPTED_DEVELOPMENT_SLICE`  
W2-U2 discovery snapshot status: `W2_U2_DISCOVERY_SNAPSHOT_ACCEPTED_DEVELOPMENT_SLICE`  
W2-U3 upgrade review status: `W2_U3_UPGRADE_REVIEW_ACCEPTED_DEVELOPMENT_SLICE`  
W2-U4 approved upgrade Apply status: `W2_U4_OPERATOR_APPLY_ACCEPTED_DEVELOPMENT_SLICE`  
W6-0 historical Control API contract status: `W6_0_CONTROL_API_CONTRACT_ACCEPTED_DEVELOPMENT_SLICE / W6_1_APPLICATION_SERVICES_READ_API_NEXT`  
W6-1 historical Control Application Services status: `W6_1_APPLICATION_SERVICES_READ_API_ACCEPTED_DEVELOPMENT_SLICE / W6_2_CONTROLLED_MUTATIONS_AUDIT_NEXT`  
W6-2 historical Control confirmation status: `W6_2_CONFIRMATION_CONTRACT_ACCEPTED_DEVELOPMENT_SLICE / W6_2_DURABLE_RECEIPT_SCHEMA_NEXT`  
W6-2 durable receipt Schema status: `W6_2_DURABLE_RECEIPT_SCHEMA_ACCEPTED_DEVELOPMENT_SLICE`  
W6-2 MODULE_DISABLE mutation wiring status: `W6_2_MODULE_DISABLE_MUTATION_WIRING_ACCEPTED_DEVELOPMENT_SLICE`  
W6-3 Web Shell/read-only Overview historical status: `W6_3_WEB_SHELL_READ_ONLY_OVERVIEW_ACCEPTED_DEVELOPMENT_SLICE / W6_4_MODULES_CONFIGURATION_UI_NEXT`  
W6-4 Modules configuration UI historical status: `W6_4_MODULES_CONFIGURATION_UI_ACCEPTED_DEVELOPMENT_SLICE / W6_5_SERVER_OWNED_MODULE_ARTIFACT_INGRESS_NEXT`  
W6-5 server-owned module artifact ingress status: `W6_5_SERVER_OWNED_MODULE_ARTIFACT_INGRESS_ACCEPTED_DEVELOPMENT_SLICE`  
Current next entry: `W6_6_SERVER_OWNED_MODULE_UPGRADE_REVIEW_NEXT`  
W4 Learning status: `W4_L4_LEARNING_CYCLE_ACCEPTED_DEVELOPMENT_SLICE / W4_COMPLETE_ACCEPTED_DEVELOPMENT_SLICE / W5_NEXT`  
W5 Collaboration status: `W5_F1_COLLABORATION_STABILITY_ACCEPTED_DEVELOPMENT_SLICE / W5_COMPLETE_ACCEPTED_DEVELOPMENT_SLICE / W6_NEXT`  
性质：旧 Store/Runtime 到 Current Store/Universal Loop 的一次性切换清单

S3-A/S3-B 历史开发切片当时的 Current Store 是 20 表，fingerprint
`098bdf44ee765727dc41f84ee3a8af65069691226d815cde882bfa45a9ff6409`，migration 为
29,575 bytes / SHA-256 `98c2f1e520bcecb1b3777bcab6014c24d34d110cb6dc751d5bd93eee84e293ce`；
这些值继续作为对应历史树证据保留。W1 Conversation 开发切片在同一 Schema Draft 中增加
唯一的 `conversations` 表和三项 `runs` 投影后，当时的 ordinary table count 为 `21`，schema
fingerprint 为 `812c8c2c35f8c7eb4426aee6362d568718140d70f7b851b9847e9fcab913c748`，
migration 为 `31,710` bytes / SHA-256
`b9bd51c0afcd3f2563013c5ead5389194f62d3028c2d94329ebffd921c5ee496`。W4-L1A 在该 Draft
中新增唯一的 `learning_proposals` 表后，当时 ordinary table count 为 `22`，schema fingerprint 为
`5afa72fce368a1997f7f37422ed8dcdf4fa2ad854a2534909463d1981de13624`，migration 为
`34,580` bytes / SHA-256 `dc48db067d107748895bef613886a366d07e74e2acdb32686057a4d32fde4157`。
W4-L1B 复用同一表加入静态 Skill Draft 后仍为 `22` 表；其历史 schema fingerprint 为
`18722b0c8a18e9f6563e65cf30d0ebc50b723aa6f6465aa4b95d83ba001bc422`，migration 为
`34,592` bytes / SHA-256 `75b7fe488932e0f562c84073a659c9803ffcd4bfdb4efa500b19720c7648c47f`。
W4-L2 继续保持 `22` 表；其历史 schema fingerprint 为
`feed0dd35f98e4f177934092d93a1b3d7f834f0fae3b076e722aa6bcd4722484`，migration 为
`35,743` bytes / SHA-256 `715c2904f417ecf8732f554eda5c47af1a6c45eecbf8e21f408f51badb70519c`。
W4-L3 已验收历史切片仍为 `22` 表，schema fingerprint 为
`a744ea62d0ead80b5eb0669409f0ab4cd0d297b662e67181b0a88f760b2c5024`，migration 为
`37,810` bytes / SHA-256 `025acc044f53a71c674d5963c171806e87095911025a129f65b4be33a0601070`。
W5-F1 当时沿用 W5-X1 的 `24` 张 ordinary table，schema fingerprint 为
`98d658e68907f77516ce0366a7b08bfa3851600326850590ed79584f581fa588`，migration 为
`44,215` bytes / SHA-256 `0a6701405e471e1ee7f41ea89ed62b50e5a4723e71b320e50f073cdd3e6b727a`。
W2-R1 的历史树仍为 `24` 张 ordinary table，schema fingerprint 为
`dcad8f8837ecc5832f444acbc2680a2c2debff5161319710f60e7f649e4aede5`，migration 为
`44,237` bytes / SHA-256 `fd7270130b5b13e5dfe9e3bb0a3bc934d46eb0c3b4a0647b1a3fddd81773e665`。
W2-R2 的历史树仍为 `24` 张 ordinary table，schema fingerprint 为
`7d2e0850a0253a630d5e2264c720b10fbac3adfdb6f9e53fcd722c2dd2615a8`，migration 为
`44,257` bytes / SHA-256 `2520299390885b4c23df85d2ff217629f6a458c368735e5bdc0df2eca37b8f2f`。
W2-U3 至 W6-2 confirmation 的历史树为 `32` 张 ordinary table，schema fingerprint 为
`37258c1939308be83f21a109e59a19e32946734d1c53b1b262f876dc36e47dbd`，migration 为
`57,652` bytes / SHA-256 `e4f1047eb527b65ab050d444062f3d286cc2ad87d56e60b1cd7f4e96df87652d`。
W6-2 receipt Schema 的历史树为 `33` 张 ordinary table，schema fingerprint 为
`51be9081a815e6379380b93e745db03d33ca2f358962f85ace63bce2534dcc10`，migration 为
`67,998` bytes / SHA-256 `8feea38beb505ab4f371afe32eecd877744062bd149d0d24982839c5450fc952`。
W6-3/W6-4 的历史树为 `41` 张 ordinary table / `23` indexes / `56` triggers，schema fingerprint 为
`87d63a2e468ee27ee2c0e1918a92e580532825bde85dbb872b9c795bf577f2c1`，migration 为
`143,588` bytes / SHA-256 `5bd9743e988b82750f785c015f07c11fbcf6025da70148405aec11106b4efb22`。
当前 W6-5 树为 `43` 张 ordinary table / `25` explicit indexes / `64` triggers，schema fingerprint 为
`47981b9bf147ec81c6e98382f281db0d5395187a1f090f7ce183c116fba82a0d`，migration 为
`150,301` bytes / SHA-256 `6def433a59fa8f4876894572f1610cae499abc5b389ab5930ea697055419ca86`。
W2-U2 的历史 `29` 表 identity 仍为
`6d2bded477e7b1d2c755bf47f5b41f63f1b2f7b568fd72496fc43fbceac3a5f1`、`49,972` bytes /
`d2bcc27bff2e17a058165f7c544b0c97cd1c99efca3264ab401631477611263e`。
W4-L4 当时的 `24` 表、fingerprint
`9941c957b0e6a1a1e1770b38cd06605025d12df30067d1271fe8f7b30f2f0518` 与 `44,138` bytes / SHA-256
`4cf260d368fb7b69e0a86dad99ff593bea216e9d0da341f607d000999e076be4` 继续只作历史切片证据。
本文后续保留的 19/20/21/22-table、W4 各阶段与 W5 的旧数值均是对应历史切片的验收证据，不得冒充
当前 W2-U3 开发树，也不代表生产部署或 S3-D 已发布；最终 Release
seal 仍只在 S3-D 静止树上重新冻结。

前述 20 表和 21 表分别是 S3-A/S3-B 与 W1 开发切片形成时的历史边界。当前 W1 与 W3 状态由
顶部状态和 §8.6 的 2026-08-09 增量验收记录表达；W4-L1A/L1B 历史验收状态分别由 §8.7/§8.8
表达，W4-L2 验收边界由 §8.9 表达，W4-L3 历史边界由 §8.10 表达，W4-L4 与 W4 complete
历史边界由 §8.11 表达；W5-X1 当时边界由 §8.12 表达，W5-F1/W5 complete 后续边界由
§8.13 表达；W2-R1/R2 历史边界由 §8.5.7/§8.5.8 表达，W2-U2 历史边界由 §8.5.12 表达，
当前 W2-U3 边界由顶部状态和 §8.5.13 表达。
历史段不回写，也不能用后续证据改造旧 seal。

这里的状态必须同时理解：

- `S1_ACCEPTED_DEVELOPMENT_BASELINE`：当前代码已经形成 S1 纵链，物理减重后的源码
  树已经重建全仓测试、Race/WSL、发布扫描和外部封存证据；项目所有者已确认方案 A，
  因而允许从该基线进入 S2。
- `S2.1_CONTEXT_COMPILER_ACCEPTED_DEVELOPMENT_SLICE`：Context Compiler、唯一
  Universal Loop 消费者、Current Store 原子记录、恢复及完整备份闭包已经按当前树
  重验；它不提前声称普通多轮生产入口已经闭环。
- `S2_MODEL_PROFILE_ACCEPTED_DEVELOPMENT_SLICE`：可选画像已经按精确模型
  build/config/artifact/adapter 冻结，只能收紧 ContextPolicy；发布、Admission、
  Store/Loop、重启和完整备份恢复闭包已经按当前树重验。它不声明自动评测或选模。
- `S2_RAG_ACCEPTED_DEVELOPMENT_SLICE`：首个本地、确定性、只读、无计费共享知识
  Provider 已通过同一 Port/Host/Compiler/Store/Loop 生产链；四级 scope、污染隔离、
  撤权、重入和 artifact/backup 恢复闭包已按当前树重验。它不声明远程检索、缓存、
  自动路由或知识更新。
- `S2_MEMORY_ACCEPTED_DEVELOPMENT_SLICE`：首个本地、确定性、有界 Agent 轻量 Memory
  Provider 已通过同一 Port/Host/Compiler/Store/Loop 生产链；作用域隔离、Attempt 级
  冻结、成功终态原子 revision、并发合并、重入和完整备份恢复已按当前树重验。它不
  声明后台学习、补偿任务、缓存或 24h 更新。
- `S2_ACTION_GATEWAY_ACCEPTED_DEVELOPMENT_SLICE`：首个本地、确定性、无副作用的
  `text.stats` 已通过同一 Module/Assembly/Store/Loop/Gateway 生产链；模型一、Proposal、
  Action PENDING、私有 executor、模型二、UNKNOWN、进程中断、终态写失败和完整备份
  恢复均按当前树重验。该 Action 批次本身不声明第三方 Action、MCP 或可产生外部
  副作用的工具已获准。
- `S2_MCP_LOCAL_PROCESS_STDIO_TOOL_ACCEPTED_DEVELOPMENT_SLICE`：MCP `2025-11-25`、
  官方 Go SDK `v1.6.0` 的 `LOCAL_PROCESS + stdio + Tool-only` 已作为现有 Action Adapter
  接入同一 Describe/Prepare/Gateway/DispatchAttempt 纵链。它只允许 Operator exact
  ArtifactDigest 显式批准且完全信任的本地开发模块；不新增 MCP Runtime、Store、Ledger
  或 Port，也不声明完整 MCP、生产就绪、OS 文件系统/网络 sandbox 或不可信第三方隔离。
- `S2_CHANNEL_ACCEPTED_DEVELOPMENT_SLICE`：首个 Workspace-scoped loopback Channel 已作为
  默认关闭的可选装配接入同一 Current Store、Universal Loop、`dispatch_attempts` 与
  Gateway 纵链。它闭合显式启用、Cursor、ACCEPTED/REJECTED、去重、原子 Admission、
  UNKNOWN、严格回复目标、优雅关停和完整备份，但不声明公网 Provider 或生产部署就绪。
- `W2_E3_WORKSPACE_CHANNEL_APPLY_ACCEPTED_DEVELOPMENT_SLICE`：在上述 Channel 纵链之上，统一
  Operator Apply 已接受 `WORKSPACE_CHANNEL_ENDPOINT` target，并把 Control/Catalog 与 revision-0
  Cursor seed 原子发布。该窄状态只闭合 first-party loopback 双 Workspace/Endpoint 的隔离、
  duplicate、Channel `UNKNOWN`、Disable 引用与 Backup/Restore；不声明公网或任意第三方 Channel、
  任意第三方进程内代码、Beta 或生产就绪。
- `W2_E4_DEEPSEEK_MODEL_REPLACEMENT_ACCEPTED_DEVELOPMENT_SLICE`：统一
  Operator Apply 已接受第七个 exact Model handler，只允许同一内建 DeepSeek
  artifact/adapter/provider Instance 在 Flash/Pro 之间显式替换。该切片闭合 Config/
  Authority/optional ModelProfile 完整期望状态、Profile 清除、SecretRef 本地瞬时授权、
  exact PriceSnapshot、新 Run-only 冻结、UNKNOWN 零替代/重放与无 Run 当前闭包的
  Backup 重验。它不声明通用 Provider、自动选模/路由、任意第三方进程内代码、
  Beta 或生产就绪。
- `W2_E5_A_REQUIRES_PERMISSION_GRANT_ACCEPTED_DEVELOPMENT_SLICE`：在既有
  Knowledge handler 上接受唯一 governed Manifest 形状：只提供 `context.provide/v1`，精确
  Require 同一 Profile 中唯一 `model.generate/v1`，并请求 `knowledge.read`。共享 classifier、
  完整 `Binding → Catalog → exact Activation → Installation → canonical Manifest` 身份链、
  Core-owned source/scope/limit grant、Apply/Dry-run fast-path 前完整 publication closure 与
  Backup/Restore 已闭合。legacy 无 Require/permission Knowledge 仍逐字节兼容；任一半声明均
  fail-closed。该状态只是 E5-A 历史增量；它本身不证明双 Port 或完整 E5。
- `W2_E5_B_DOCUMENT_INSIGHT_DUAL_PORT_ACCEPTED_DEVELOPMENT_SLICE`：唯一固定
  `freeagent.builtin.document-insight@1.0.0` 以同一受信、编译进 Core 的 Adapter 同时提供
  `action.provider/v1` 与 `context.provide/v1`。Context→Action 两步 Apply、Action→Context 两步
  Disable、同一 Installation/Activation/Catalog identity、真实 RAG + Gateway `text.stats`、冻结
  Action result reservation、exact retry、完整 Backup/Restore 与 Pure Chat 回归已经闭合。该状态只
  证明一个 exact selector 产品纵链；任意第三方代码、通用 multi-Port、多个 ProviderBinding 合并、
  REMOTE/WASM、不可信包内代码与在线控制面仍未开放。
- `W2_R1_REMOTE_ACTION_HOST_ACCEPTED_DEVELOPMENT_SLICE`：默认关闭的第一个原生 REMOTE
  Action Host 已按 exact `action.provider/v1 + REMOTE/freeagent-action-http/v1` 接入既有 Module
  Apply、Catalog、lazy loader、Action Proposal、DispatchAttempt、Gateway 与 Backup 纵链。只有
  Operator 显式批准 exact artifact、HTTPS endpoint、SecretRef grant 并启用 runtime 后才可装配；
  歧义外部结果只进入原 Attempt `UNKNOWN`，禁止语义重放。该状态不声明其他 REMOTE 协议、WASM、
  不可信第三方隔离、生产就绪或真实公网互操作。
- `W2_R2_WASM_HOST_ACCEPTED_DEVELOPMENT_SLICE`：默认关闭的 exact
  `action.provider/v1 + WASM/freeagent-action-wasm/v1` 已接入同一 Module Apply、Catalog、lazy loader、
  Action Proposal、DispatchAttempt、Gateway 与 Backup 纵链。它只允许无 imports/WASI/Host Module/
  网络/文件/Secret 的有界 Core v1 pure-compute guest；确定性 guest 失败收口原 Attempt 为 FAILED，
  只有终态 Store 事务/崩溃留下的原 PENDING 恢复为 UNKNOWN。该状态不声明 OS/container、生产级
  恶意多租户隔离、任意第三方包、签名/发现/升级、生产就绪或公开 Beta。
- `S2_PARENT_CHILD_COMPOSITE_ACCEPTED_DEVELOPMENT_SLICE`：首个 Parent/Child + Specialist
  family 已接入同一 Assembly Compiler、Universal Loop 和 Current Store，并由真实
  `chat --composite` 消费。它闭合 whole-family 原子 Admission、有界并行 Child 推进、
  固定 `ALL_REQUIRED` merge、取消、用量派生与完整备份；只覆盖同 Workspace、depth 1、
  2..8 Specialist；该 S2 裁决不声明 Reviewer、公平 Scheduler、Learning、动态图或跨
  Workspace。公平 Scheduler 后续由独立 S3-A 开发切片验收。
- `S3_FAIR_SCHEDULER_ACCEPTED_DEVELOPMENT_SLICE`：默认关闭的 tenant-bound 本地公平
  Scheduler 已复用现有 RunLease 与 Universal Loop，闭合三层上限、least-served claim、
  UNKNOWN 零重放、优雅关停和完整备份；不声明分布式公平或跨 Tenant 配额。
- `S3_COMPOSITE_REVIEW_GATE_ACCEPTED_DEVELOPMENT_SLICE`：一个可选 Reviewer 已作为普通
  `REVIEWER` Run 接入同一 Composite family。合法 Provider 输出在成功 outcome 前由 Core
  严格校验并规范化，canonical `ReviewVerdictV1` 保存于既有 MODEL_RESULT；APPROVE 才
  允许 Root merge，REJECT/FAILED/INVALID 不 merge，UNKNOWN 不语义重放。该切片不声明
  自动选 Reviewer、返工循环、真实外部模型效果或 S3-D 发布完成。
- `W1_CONVERSATION_ACCEPTED_DEVELOPMENT_SLICE`：最小 Conversation 已在同一 Current Store、
  Universal Loop、Context Compiler、History、Usage 与模型 Attempt 上闭合创建、精确重入、
  head/revision CAS、一轮一 Run、完整 USER+ASSISTANT predecessor pair、85% 摘要、100% 最小
  完整前缀 Drop、CLI/HTTP 创建与续聊、服务重启、正常 OS 进程 25+25 轮和
  backup/verify/restore 后继续。HTTP create 严格固定 scope，首次为 201、精确重试为同一
  记录和 200。它不表示 W1 整体完成；真实 DeepSeek 2/50 轮尚未执行，Conversation 与
  Composite 显式互斥，本切片也不声明 Conversation+Action 或 Conversation+Channel 已成熟。
- `W1_COMPLETE_REAL_DEEPSEEK_50`：在上述历史开发切片之后，真实官方 DeepSeek 50/50 个首次
  Model Attempt 成功，turn 25 backup→verify→restore、turn 25/50 exact retry、Usage/费用与
  测试资产清理均闭合。统一零模块验收又证明已安装但未选择的 Knowledge、Memory、Skill、
  MCP/Action、Channel、Learning 与 Team 不进入请求或产生可选持久化增量。该状态只完成 W1
  计划范围，不声明 Conversation+Composite、Action、Channel 或公开 Beta 已成熟。
- `W3_COMPLETE_ACCEPTED_DEVELOPMENT_SLICE`：W3-M0/M1/M2 与 W3-K0/K1/K2A/K2B/K3 已在唯一
  Runtime/Store/Loop/Compiler/Backup 上完成本地开发纵链与集中验收。它不表示 W4 Learning、
  远程向量库、生产部署或公开 Beta 已完成。
- `W4_L1A_KNOWLEDGE_PROPOSAL_STORE_ACCEPTED_DEVELOPMENT_SLICE` 与
  `W4_L1B_STATIC_SKILL_PROPOSAL_STORE_ACCEPTED_DEVELOPMENT_SLICE`：只验收唯一
  `learning_proposals` 表中的 Knowledge/静态 Skill Proposal、三轴去重、proposer lineage、
  exact retry 与 backup semantic gate。
- `W4_L2_LEARNING_REVIEW_ACCEPTED_DEVELOPMENT_SLICE`：完整 ReviewRequest、显式独立 Reviewer
  的普通 Run/Model Attempt/Usage/UNKNOWN、同表 review refs/state/revision、Store 派生终态、
  Backup 语义闭包与真实 DeepSeek runner 已完成本开发切片验收。它不授予发布、安装、激活、
  不可变 Version 或周期执行能力。
- `W4_L3_LEARNING_VERSION_ACCEPTED_DEVELOPMENT_SLICE`：`APPROVED/2|3` Proposal 的无权限
  canonical Version、跨 Tenant/kind 全局 ModuleRef 与 Installation 双向门禁、安全 Operator
  artifact 交接、完整 Backup/Restore 和恢复后产品重新导出已完成开发切片验收。它不自动生成
  Apply plan、安装、激活、绑定或扩权。
- `W4_L4_LEARNING_CYCLE_ACCEPTED_DEVELOPMENT_SLICE / W4_COMPLETE_ACCEPTED_DEVELOPMENT_SLICE`：
  默认关闭 Schedule、24 小时默认周期、不可变逻辑窗口 Task、显式 Tick、普通 proposer
  Run/Attempt/Usage、Store-only reconcile、只读 report 与完整 Backup/UNKNOWN 边界已闭合。
  W4 计划范围完成，下一工作包为 `W5_NEXT`；该状态不授权后台 Worker、自动 Review/Apply、
  主动外部检索、生产部署或公开 Beta。
- `W5_X1_WORKSPACE_TRANSFER_ACCEPTED_DEVELOPMENT_SLICE / W5_F1_NEXT`（§8.12 当时状态）：显式、默认关闭的
  Composite Transfer 已在同一 Runtime/Store/Loop/Attempt 账本上闭合双边独立授权、trusted
  `TASK_SUMMARY` REQUEST、严格 `SPECIALIST_RESULT` RESULT、一个跨 Workspace Child、Reviewer
  `APPROVE` 后 Root merge 与 exact retry 零新效果。它只验收 X1 开发纵链；持续负载公平、取消、
  重启、UNKNOWN 对账、Backup/Restore 与完整权限矩阵仍属于唯一下一入口 `W5-F1`，不授权 W6、
  生产部署或公开 Beta。
- `W5_F1_COLLABORATION_STABILITY_ACCEPTED_DEVELOPMENT_SLICE / W5_COMPLETE_ACCEPTED_DEVELOPMENT_SLICE / W6_NEXT`：
  在同一 Scheduler、Store-derived Decision frontier、Universal Loop、Attempt/Usage 与 Backup 边界
  闭合持续负载公平、稳定 plan order、完整 `2N+3` 取消、重启、UNKNOWN 原 Attempt 可靠 evidence
  对账、权限负例以及 Pure Chat/ordinary Composite 零 Transfer canary。它不表示 W2 通用装配总
  工作包、W6/W7、首次生产部署、公开 Beta 或 `RELEASE_READY` 已完成。
- `EXACT_ADAPTER_LAZY_MATERIALIZATION_ACCEPTED_DEVELOPMENT_OPTIMIZATION_SLICE`：必需
  Model 保持 eager；可选实现只在已授权的 exact Binding 首次使用时物化。同键合并、异键
  并行，失败不缓存；cache 不持久化、不授予权限，也不改变 Store、Schema、Loop、Gateway、
  模型请求或 S3-C/S3-D 状态。
- `PRODUCTION_CUTOVER_NOT_APPLICABLE`：项目所有者确认 FreeAgent 从未正式部署，
  Legacy、Temp、snapshot 和 evidence 均为开发测试资产，不存在远端/未申报 writer
  或待对账外部效果，所以本轮没有可以执行或记录的生产停机窗与首个正式 Admission。
  上述资产继续保留并只读隔离，当前开发切片没有移动、改写或删除它们。

这不是 `CUTOVER_GO` 或 `RELEASE_READY`，也不声称发生过生产切换。第 4 节把已封存
S1、S2.1 历史批次与 ModelProfile、RAG、Memory、Action、MCP、Channel、Composite、
S3-A Scheduler、S3-B Reviewer 当前树证据明确分开；物理删除或源码变化前的封印不能
证明后来的树。未来首次生产部署仍必须重新执行路径、writer、备份、外部效果和发布门禁，
不得复用本次“从未部署”确认跳过。

## 1. 权威范围与本次证据锁

本清单只负责一次性切换步骤、证据和 GO/NO-GO 判定，不创建新的 Runtime、Store、
Module Protocol 或装配事实源。

长期编码契约只有：

- `docs/specs/CORE_RUNTIME_V1.md`；
- `docs/specs/CURRENT_STORE_V1.md`。

本清单是一次性门禁。仓库外主方案只记录本轮 provenance，不作为公开使用者无法
读取的隐藏运行协议。

下表是已验收 S1 封存批次的历史输入锁。它证明该批次，不再冒充后续 S2 当前树哈希：

| S1 历史输入 | SHA-256 | 封存用途 |
|---|---|---|
| 当时的精简主方案 | `9fa0234c17469b9ed4076972ecb4f422f6fbc8a0b2237e8017c5b311da37f106` | S1 阶段、减重和原始设计影响裁决 |
| `docs/specs/CORE_RUNTIME_V1.md` | `8ae1a691a739f4775a59ad20f1267787c0d29b0ef656784ed22221303730940a` | 当时的 S1 Runtime 编码契约 |
| `docs/specs/CURRENT_STORE_V1.md` | `a2d00830d3f8121938ac3a9479c349305c088db259d09a55beddfffb84e2bc28` | 当时的 S1 Store 编码契约 |
| `docs/S0_SEMANTIC_RETENTION_MATRIX.md` | `8033b9dd3138ad809790e3d981e6ef84e372e84417051b3c594bd57b00cd9caa` | 旧语义保留、重写、延后和删除依据 |
| `examples/current-v1.bootstrap.seed.json` | `19df7fbccbecd4b1f72151fb2847f840fc61d8901cce87c8c524c69b146f1f12` | 当时的 S1 bootstrap seed |
| `internal/currentstore/migrations/0001_current.sql` | `5a894f61c5da3a8825ebb590581265cfa18fd1756e5d869dac3ebee890022f13` | 当时的 S1 Current Store 初始化 Schema |

Seed 文件本身必须是 RFC 8785 精确 canonical JSON，导入器按字节比较，不接受只在
解析后语义等价的重排、空白或尾随内容。因此上表 Seed 文件哈希同时锁定本次实际
导入字节。

上述哈希只对应 S1 证据快照。文件变化后，S1 接受结论仍是历史事实，但不得沿用其
封印证明当前树；受影响测试、当前输入锁和外部 Stage 必须重新生成。

### 1.1 无 Git 源码封存证据

源码静止并完成全部仓内修改后，使用 `scripts/New-PublicStaging.ps1` 从当前 Source
生成仓库外 Stage 与 Artifact，再使用 `scripts/Test-PublicStaging.ps1` 对同一 Stage
和 manifest 复核。外部证据至少保存：

- `manifest_sha256`；
- `directory_set_seal_sha256`；
- `file_tree_seal_sha256`；
- Stage 上 PublicTree、License、全仓测试和 S1 定向测试的命令、退出码及输出哈希。

最终 manifest 与测试日志只能保存在 Source/Stage 之外；不能把最终 tree hash 写回
本文件后继续声称它仍对应同一棵树，否则会形成自引用并使封存失效。封存完成后任何
源码修改都必须废弃该批外部证据并重新生成。

### 1.2 S2.1 历史输入锁

S2.1 Context Compiler 切片当时使用以下输入重新验证；本表随对应外部批次冻结，不因
后续 ModelProfile 修改而重写：

| S2.1 当时输入 | SHA-256 | 用途 |
|---|---|---|
| 最新精简主方案 | `41a85f6c83b87693bef63c4ff2bfb2a20d5e0d8ddcc6ec091573c19624c70a10` | S2.1 收口与下一 ModelProfile 原子切片裁决 |
| `docs/specs/CORE_RUNTIME_V1.md` | `8adf13bc9448bbe193f5f8eaed88092827e3e44ee7436fda34bc53c150a15372` | 当前 Runtime/Context 编码契约 |
| `docs/specs/CURRENT_STORE_V1.md` | `30e7455aff4215df318fabad285f9e8286c0a3345b9bde6e077a6609a4182011` | 当前 Store/恢复/备份编码契约 |
| `docs/S0_SEMANTIC_RETENTION_MATRIX.md` | `8033b9dd3138ad809790e3d981e6ef84e372e84417051b3c594bd57b00cd9caa` | 原始语义保留边界 |
| `examples/current-v1.bootstrap.seed.json` | `b7cf56100b0833ea86b23499f83bf5a4179a869a31da6b7d63fd8becc3c6196d` | 当前唯一 bootstrap seed |
| `internal/currentstore/migrations/0001_current.sql` | `721ebcb68de163c4d5cb10ca34499152dfab8400acef5db4d18602d8306069a1` | 当前唯一 Current Store 初始化 Schema |

最终 Source/Stage seal 仍只写入仓库外证据批次
`s2.1-context-20260803-034909`，不回写本文件。

### 1.3 ModelProfile 当前输入锁

ModelProfile 切片在源码、两份规格和 S0 当前注记定稿后使用以下精确输入验证：

| 当前输入 | SHA-256 | 用途 |
|---|---|---|
| 最新精简主方案 | `8b264873d1e4287a17e7551b778b2d8d0c74e388704c2e9e2b33abd099ba1388` | ModelProfile 收口与下一 RAG 原子切片裁决 |
| `docs/specs/CORE_RUNTIME_V1.md` | `525812dd4d53be3dc1ea421f3e4cc777caa58dfd57c9761215766ae8a2ca540b` | Runtime/ModelProfile/Context 编码契约 |
| `docs/specs/CURRENT_STORE_V1.md` | `900894fc701edeaa1c5fd1df0ba63e39ede4f2a9d0c97dff47de6363ef637ed8` | Store/Admission/恢复/备份编码契约 |
| `docs/S0_SEMANTIC_RETENTION_MATRIX.md` | `0d342743d4636c47849b06e1fef8c1490d4917002701379e57433e69c1331d9d` | 历史语义与当前切片边界 |
| `examples/current-v1.bootstrap.seed.json` | `34f712989fd31120c2c4ccc026d36ed9e365813747e6de8bf2ca85af9ddb5a3d` | 当前默认无画像 Pure Chat seed；含精确 model build |
| `internal/currentstore/migrations/0001_current.sql` | `721ebcb68de163c4d5cb10ca34499152dfab8400acef5db4d18602d8306069a1` | 未改变的 16 表 Current Store Draft |

最终 Source/Stage seal 只写入仓库外证据批次
`s2-model-profile-20260803-052323`；不把 manifest/tree seal 回写本文件，避免自引用。

## 2. 历史 S0 断点与当前 S1 候选必须分开

### 2.1 历史 S0 断点

S0 归档时，旧入口虽能创建 Unified Admission，但后续仍进入旧 Runtime 和 Legacy
执行门禁，无法完成新的统一纵链。旧 Store 还把 migration、seed 和部分恢复动作
混入普通打开路径。

这只是历史基线，不是当前 `cmd/freeagent` 的接线说明，也不得继续作为生产入口的
验收目标。

历史旧 Store 身份仅用于拒绝测试和离线辨识：

| 项目 | 历史值 |
|---|---|
| schema identity | `github.com/endview/freeagent/public-core` |
| SQLite application ID | `1179796805` / `0x46524545` / `FREE` |
| user/meta version | `11` |
| schema fingerprint | `58a331f917a9771fec95073a1c9141fabde8ecd69e6619112a67a8e16807102a` |
| migration | 11 个版本 |

最终 S0 源码基线仍只作为离线诊断材料：

| 项目 | 值 |
|---|---|
| 文件 | `freeagent-s0-final-baseline-20260730T022135.zip` |
| SHA-256 | `c51eda49d492d49e26523411a9c9d76f9ec9d61059f8978c29920cc651414237` |
| 本机离线位置 | 不写入公开仓库；由操作者在离线切换记录中填写 |

该归档不授权切换后恢复旧 Runtime 或旧 Store 写入。

### 2.2 当前 S1 基线、S2.1 Context、ModelProfile、RAG、Memory、Action、窄 MCP、Channel 与 Composite 扩展

当前生产候选入口已经改为一条显式链：

```text
freeagent init
  → bootstrapseed.PrepareFile
  → 校验并暂存 content-addressed artifacts
  → InitFreshCurrentStore
  → 只通过 Current Store API 导入 seed
  → PrepareClosedCurrentStoreForPublication
  → artifact / FAC1 Store no-replace 发布

freeagent chat / freeagent serve
  → VerifyCurrentStoreReadOnly
  → OpenExistingCurrentStore
  → Store-only 启动 safety transaction（Catalog/Registry/Loop 构造前）
  → 只读最小 ledger；原 PENDING CAS 为原 Attempt UNKNOWN
  → LoadPublishedBasis / 构造 Catalog、Registry 与 Universal Loop
  → ChatService
  → Universal Loop
  → 从冻结成员闭包恢复可选 ModelProfile
  → 按冻结 Binding 精确调用可选本地 RAG / Agent 轻量 Memory Provider
  → 唯一 Context Compiler（每个模型步骤恰好准备一次）
  → Current Store 原子写入 Context Compilation + MODEL_REQUEST + Attempt
  → exact in-process model adapter
  → 无 Action 时：Current Store 提交 Usage / History / Frame / Event
  → 有 Action 时：Core 生成 Proposal，并原子提交模型一 + Action PENDING
  → Gateway 消费一次性 permit，调用精确私有 executor
  → 显式 MCP Binding 时：在 Describe/Execute 使用边界验证完整 ArtifactDigest，
    单次 stdio 会话只发一次 tools/call
  → AVAILABLE 结果作为不可信数据追加到冻结请求，执行模型二并提交终态
  → 首次最终成功终态按最新 Agent head 原子追加可选 Memory revision
  → 显式 Channel 装配时：模型最终回复原子追加 CHANNEL_SEND PENDING 与一次性 permit
  → 同一 Gateway 对冻结的 Workspace Endpoint 执行一次 loopback 发送；歧义结果为 UNKNOWN

freeagent backup / backup-verify / restore
  → currentbackup 完整 bundle
  → database + 全部 retained module artifacts + canonical manifest
  → no-replace 恢复并复核 Store identity
```

当前候选边界：

- `init` 是唯一 bootstrap 入口，必须显式提供 `--db` 和 `--seed`。
- `chat`、`serve` 只验证并打开已存在的 FAC1 Store；不会自动 init、seed、migrate、
  repair 或删除数据库。
- `serve` 的 Core HTTP 只允许 loopback，公开 `GET /healthz`、同步
  `POST /v1/conversations` 和 `POST /v1/chat`；可选
  Channel 入口必须通过显式参数选定唯一 Workspace、Endpoint 与 SecretRef，默认不构造。
- 默认 Pure Chat seed 只激活 S1 已接线的声明式 Context 与 exact 内置 Model；独立 RAG、
  Memory、Action 或 Operator-granted MCP 装配才显式增加相应本地 artifact 与 Binding。
- S2.1 Context Compiler 是 Core 的纯转换，不是可自授权模块；低于 85% 不产生编译
  记录，达到 85% 产生恰好一份可恢复编译记录，达到 100% 直接按序移除最小完整
  旧历史前缀，不先执行不可达的“摘要后再 Drop”分支。
- 历史快照（截至 2026-08-06）：W1 Conversation 开发切片已经实现“一轮一 Run、Conversation head/revision CAS、
  predecessor 完整 USER+ASSISTANT pair”，并通过真实本地两轮、阈值、重启与
  backup/verify/restore 后续聊测试。CLI 可以创建、读取和继续 Conversation；loopback HTTP
  可以创建固定 scope Conversation（首次 201、精确重试 200）并按 exact revision/head 继续，
  产品 E2E 覆盖服务重启。当时连续 50 轮真实 DeepSeek 调用尚未执行，因此该历史
  切片不得冒充 W1 整体完成或开放式生产多轮聊天闭环；当前 W1 状态见顶部与 §8.6。
- ModelProfile 复用 `CONFIG` 与成员快照可选 ref，不增加表、Port 或路由器；画像必须
  精确匹配已选择的模型 Binding，只能以 `min(base, profile)` 收紧上下文窗口。无画像
  路径不发现画像内容，较小画像同时约束 compilation 和 nil-compilation Store 门禁。
- 本地 `text.stats` 是默认示例中唯一启用的 Action，EffectClass 为 `none`。窄 MCP 只有
  在 Operator exact ArtifactDigest grant 与显式 Binding 同时存在时才惰性加载。W2-R1 另验收
  默认关闭、需显式 grant/runtime 开关的 exact `REMOTE/freeagent-action-http/v1` Action Host；
  W2-R2 再验收默认关闭、需独立 artifact grant/runtime 开关的 exact
  `WASM/freeagent-action-wasm/v1` 纯计算 Host。其他 REMOTE/WASM 协议、Resource、Prompt、Sampling、
  Elicitation、Roots、Tasks、常驻池、自动发现、任意第三方/生产级隔离、真实公网第三方互操作，以及远程/计费 RAG、后台学习式 Memory、
  Reviewer 自动选择/返工循环、动态图、跨 Workspace、完整团队协作、Learning 和公网
  Channel Provider 均未验收；公平 Scheduler 只按 S3-A 的默认关闭边界可用。请求未装配
  能力由 Assembly Compiler fail-closed。已验收的 Composite 仅限同 Workspace、depth 1、
  2..8 Specialist、固定 `ALL_REQUIRED` 和一个可选 S3-B `RESULTS_GATE` Reviewer。
- loopback Channel 默认关闭；未显式启用时不读取 Channel 配置、Cursor、SecretRef 或
  adapter，不开放 Channel ingress，也不产生隐式外部发送。显式启用只增加当前
  Workspace 的冻结 Channel PortPlan，不创建 Channel Runtime、Store 或 Outbox。

## 3. 当前 Seed、Artifact 与 Store 身份

### 3.1 Seed

当前 seed：

| 项目 | 值 |
|---|---|
| schema | `freeagent.bootstrap-seed/v1` |
| seed ID / revision | `freeagent.local.pure-chat` / `1` |
| Tenant | `default` |
| 默认 Workspace / Agent / Profile | `local-chat` / `assistant` / `pure-chat` |
| Catalog generation | `catalog-local-s1` / `1` |
| Control snapshot | `control-local-s1` / revision `1` |
| Secret 值 | 无 |

`expected_execution_class` 和 `expected_adapter_identity` 只是导入后的断言。真正授权
来自 composition root 的本地 allowlist、Activation Resolver 和 exact adapter
registry；seed 不能自授 Trust、ExecutionClass、AuthorityCeiling 或 FailurePolicy。
独立 Action seed 为 `freeagent.local.action-chat/1`，默认 Profile 为 `action-chat`；它
不改变上述 Pure Chat 默认 seed，也不能自行扩大本地 Action allowlist。

### 3.2 Artifact locks

| Module | ExecutionClass | Port | ArtifactDigest | package bytes |
|---|---|---|---|---:|
| `freeagent.builtin.model.echo@1.0.0` | `TRUSTED_IN_PROCESS` | `model.generate/v1` | `884c16b339b6f9154a6f64b23e800010f0b29074c1ad9a01c540f9c9ed9182f7` | 36,320 |
| `freeagent.builtin.context.basic@1.0.0` | `DECLARATIVE` | `context.provide/v1` | `5fd284dffdd51e8398d147b38ca220fbb939cec53a750b8189904483ee03d526` | 35,430 |
| `freeagent.builtin.action.text_stats@1.0.0` | `TRUSTED_IN_PROCESS` | `action.provider/v1` | `2331b8b5209f4acffd7fef96f55ce1e0841bc3c4e39d90581efb941ecae9b955` | 1,936 |

各项摘要和大小由 Seed 导入器重新扫描真实目录计算，不只信任 Seed 字段。普通文件
或 canonical Manifest 变化会使导入和启动 fail-closed。

### 3.3 Current Store

| 项目 | 当前值 |
|---|---|
| schema identity | `github.com/endview/freeagent/current-store-v1` |
| SQLite application ID | `1178682161` / `0x46414331` / `FAC1` |
| SQLite user version | `1` |
| generator | `freeagent-current-store-draft-v1` |
| expected schema fingerprint | `47981b9bf147ec81c6e98382f281db0d5395187a1f090f7ce183c116fba82a0d` |
| `0001_current.sql` ordinary tables / explicit indexes / triggers | `43 / 25 / 64` |
| migration bytes / SHA-256 | `150,301` / `6def433a59fa8f4876894572f1610cae499abc5b389ab5930ea697055419ca86` |

新进程必须拒绝旧 `FREE` identity、空文件、缺失文件、Schema drift、hardlink alias
和第二个可写 owner。Schema 仍处于首次公开发布前的 Draft 期；变更必须停 writer、
显式删除开发库并从当前 `0001_current.sql` 重建，不能在启动时自动修改。

## 4. S1/S2.1 与后续 S2 开发切片的历史封存证据

### 4.1 S1 已验收历史封存

S1 结果来自仓库外证据批次 `s1-final-20260802-052440`。最终 Public Stage manifest、
目录 seal、文件树 seal 和逐命令日志继续只保存在 Source/Stage 之外，避免把最终树
摘要写回源码后形成自引用。该批次证明已接受的 S1 历史基线；S2.1 修改后不再用它
证明当前树。

| 命令或探针 | S1 封存状态 |
|---|---|
| Windows `go mod verify` | PASS，exit `0` |
| Windows `go test -json -count=1 -timeout=30m ./...` | PASS；14 个包、633 个测试/子测试通过、1 个平台相关 skip、0 fail；日志 SHA-256 `63b8c9614bdd47b352468e3df1304a054be3fe66bcb0f359da25a1f8e8325b45` |
| Windows `go vet ./...` | PASS，exit `0` |
| Windows/Linux/Darwin `CGO_ENABLED=0` production cross-build | PASS；binary SHA-256 分别为 `f8a4f152319f540354be6eed62847d5dc763125d0285836a48c79ccd8e40ff7c`、`17dd3171ee2122b349d36e3de8d3b960d6ef82d166e489ea399d758e2eddf594`、`587baf8941c6d325a450805a70e5b0bd84634fe67e7e30788188a265c9f246c8` |
| WSL2/Linux `go mod verify` | PASS，exit `0` |
| WSL2/Linux `go test -json -race -count=1 -timeout=30m ./...` | PASS；14 个包、634 个测试/子测试通过、0 fail、0 data race；日志 SHA-256 `93166e6bd3ffd8bb607d719be256ef5bf7a7ee83511b5a5f3c436ab78a068ac6` |
| S1 定向恢复/长链路/E2E | PASS；4 个包、12 个测试/子测试通过、0 fail；日志 SHA-256 `59306aa0e66dfc6af9b3aec345e2a81a8855aa7ce172e2e6aa9d1298f9e9be5f` |
| Secret、许可证和公开目录扫描 | PASS；License 为 26 个 Go 依赖、29 个分发资产 |
| 仓库外 Public Stage、manifest 与 source seal | PASS；摘要和逐命令日志仅存于本节标识的外部证据批次，不写回源码 |

本轮定向测试包：

```text
./cmd/freeagent
./internal/currentbackup
./internal/currentstore
./internal/bootstrapseed
./internal/localchat
./internal/coreloop
./internal/corecontract
./internal/modulehost
./internal/exactadapter
./internal/assemblycompiler
./internal/activationresolver
```

上述包清单用于定位 S1 封存证据；它不能证明 S2.1 修改后的源码树，也不能替代正式
部署路径、停机窗和外部效果对账。

### 4.2 S1 真实入口与 E2E（历史已验收）

| 范围 | 证据入口 | 当前判定 |
|---|---|---|
| CLI init → chat → retry | `TestRunInitAndChatCommands`、`TestProductionCompositionInitChatAndRestartIdempotency` | PASS |
| init no-overwrite/恢复 | `TestInitializeProductionDataNeverOverwritesTargets`、`TestInitializeProductionDataResumesExactOrphanArtifactRoot`、`TestPublishInitNoReplacePreservesExistingTarget` | PASS |
| 正常启动不隐式初始化 | `TestOpenProductionCompositionDoesNotCreateMissingStore` | PASS |
| Artifact 篡改 | `TestOpenProductionCompositionRejectsArtifactTampering` | PASS，fail-closed |
| HTTP Conversation create / chat / retry / restart | `TestPostConversationCreatesAndExactlyReusesConversation`、`TestPostChatCarriesExactConversationCASAndReturnsNewRevision`、`TestRunServeLoopbackAndGracefulShutdown` | PASS；首次 create 201、精确重试 200，服务重启后保留 head 并续转下一轮 |
| HTTP 优雅/强制关停 | `TestServeHTTPDrainsActiveRequestBeforeCancellation`、`TestForcedHTTPShutdownReportsUndrainedHandlerAndCancelsOnlyAfterGrace` | PASS |
| 声明式基础 Context | `TestBootstrapDeclarativeContextReachesModelRequestWithoutReplay` | PASS |
| 声明式 Role + 静态 Skill | `TestDeclarativeRoleAndSkillTraverseProductionPureChatChain` 通过真实 artifact 扫描、Install、Activate、Bind、冻结 PortPlan 与 Pure Chat model request；同一测试同时证明零声明式模块时不创建 Context PortPlan | PASS；已登记机器可读测试证据，但不作成熟度晋级 |
| Admission 幂等/冲突 | `TestChatServiceRunsStablePureChatAdmissionAndReusesTerminalRun`、`TestChatServiceSameRequestIDRejectsChangedStableIntent` | PASS |
| Model pre-dispatch/UNKNOWN | Current Store `BeginModelDispatch*`、`TestCommitModelDispatchOutcomeUnknownBlocksReplayUntilReconciled` 与 Universal Loop 恢复测试 | PASS；禁止重放 |
| DeepSeek frozen estimate | `TestCommitModelDispatchOutcomePersistsFrozenDeepSeekEstimatedCost`、`TestCommitModelDispatchOutcomePreservesUnknownFrozenCostAsNull`、`TestProductionDeepSeekSingleAgentPureChatPersistsExactUsage` | PASS；估算与 Usage 同事务，未知价格/token 保持 NULL，reasoning 不重复收费，未报告/未对账费用保持 UNKNOWN |
| Store-only 启动 safety transaction | `TestScanStartupRecoveryIsGlobalDeterministicAndReadOnly`、`TestScanStartupRecoveryValidatesContextAndStoreLifecycle`、`TestProductionStartupRecoveryClosesPendingWithoutArtifactInspection`、`TestHistoricalMCPUnsettledIsZeroAccessDuringPureChatStartup`、`TestOpenProductionCompositionFailsClosedDuringStartupRecovery` | PASS；旧 Universal Loop/frozen artifact 启动证明已退役 |
| 真实进程 Kill | `TestProductionCompositionRecoversRealProcessKillWithoutModelReplay` | PASS；未重放模型调用 |
| 终态事务失败 | `TestProductionCompositionRecoversTerminalTransactionFailureWithoutReplay` | PASS；恢复后不重放 |
| backup → verify → restore → terminal retry | `TestRunBackupVerifyRestoreCommands` | PASS |
| 完整备份状态矩阵 | `TestFullBundleRoundTripPreservesIdentityPendingAndNullUsage`、`TestFullBundleRoundTripPreservesOriginalModelUnknownAndKnownZeroUsage` | PASS；UNKNOWN 与已知零值保持区分 |
| 长链路 | `TestProductionLongChainSurvivesRetryRestartBackupAndRestore` | PASS；S1 封存批次日志已生成 |

这些测试入口和本轮日志共同构成技术证明，但不替代正式部署停机窗验证。

### 4.3 Production dependency closure

Channel 接线曾使 `cmd/freeagent` 的 FreeAgent 自有依赖闭包由窄 MCP 批次的 19 包增加为
21 包；S3-A 随后加入唯一 `internal/runscheduler`，形成当时的 22 包闭包。S3-B Reviewer
复用既有合同、Assembly、Store、Loop、LocalChat 与 backup 包，没有新增第二 Runtime 或
专用 Reviewer 包。W2-R1 加入 `internal/remoteactionhttp`，W2-R2 再加入唯一
`internal/wasmaction`；W2-U2/U3 分别加入 `internal/modulesource` 与 `internal/moduleupgrade`，W2-U4
共享 Handler 入口门再加入唯一 `internal/modulehandler`。当前树以 `cmd/freeagent/dependency_test.go` 的
exact allowlist 为权威，生产依赖闭包精确为下列 35 包；最终静止树
的全仓/发布门禁仍须按第 5 节由根任务确认后记录：

```text
cmd/freeagent
internal/actiongateway
internal/actionmaterializer
internal/activationresolver
internal/assemblycompiler
internal/bootstrapseed
internal/channelservice
internal/controlcontract
internal/contextcompiler
internal/corecontract
internal/coreloop
internal/currentbackup
internal/currentstore
internal/deepseekcost
internal/deepseekmodel
internal/exactadapter
internal/knowledgecore
internal/learningcontract
internal/localchat
internal/loopbackchannel
internal/memorycore
internal/mcpstdio
internal/moduleconformance
internal/modulehandler
internal/modulehost
internal/modulesource
internal/moduleupgrade
internal/remoteactionhttp
internal/runscheduler
internal/s3eval
internal/s3audit
internal/s3cellaudit
internal/wasmaction
sdk/loopapi
sdk/moduleapi
```

门禁应使用上述 35 包的精确 allowlist；任何未批准的 FreeAgent 自有包进入生产
闭包都会失败。它同时覆盖并明确拒绝下列旧生产包族：

```text
internal/channel
internal/assemblyevidence
internal/store
internal/httpapi
internal/runtime
internal/executor
internal/team
internal/model
```

当前依赖列表未包含上述旧包族。旧专用 Skill、Channel、RuntimeCatalog 证明包的物理
退役不删除产品能力：首个 Channel 已通过同一个 Module/Port、Universal Loop 与 Current
Store 重建；`RuntimeCatalog` 继续作为逻辑控制平面职责存在，而不是同名运行包。

### 4.4 S2.1 Context Compiler 历史批次证据

S2.1 证据只接受当前生产消费者、纯 Compiler、Current Store、完整备份和恢复链；不
用未接线的旧实现或测试专用第二条请求构建路径代替：

| 范围 | 当前证据 | 判定 |
|---|---|---|
| 精确水位与确定性 | `<85%` 无编译记录；`==85%` 最多一次摘要；`==100%` 直接 Drop；重复相同正文按稳定历史序号区分；20 次边界重跑一致 | PASS |
| 最小有序移除 | 只移除恢复预算所需的最小完整旧历史前缀；最近窗口、静态 Context 和任务受保护 | PASS |
| 污染与配置边界 | 不可信 Context 使用固定前缀 + canonical JSON 用户数据；S2.1 静态 Context 的 `allow_summary/allow_drop=true` 在 bootstrap 前失败关闭 | PASS |
| 唯一真实消费者 | `UniversalLoop` 只调用一次 `preparePureChatRequestV1`，模型请求与编译字节成对进入 `BeginModelDispatch`；兼容入口禁止丢弃高水位审计字节 | PASS |
| 原子 Store 与恢复 | Context Compilation、MODEL_REQUEST、Attempt、Usage、Frame/Event 原子持久化；伪造摘要在 PENDING/permit 前拒绝；重启不重编译、不重放 | PASS |
| 完整备份 | backup → verify → restore → reopen 后 compilation ref、请求/编译 canonical bytes、digest 和内容记录数逐项不变 | PASS |
| Windows 全仓 | `go test -count=1 -timeout=30m ./...`，15 个包通过 | PASS |
| WSL2/Linux 受影响闭包 | Context/Core/Loop/Store Race 通过；cmd/localchat/currentbackup 通过；`linux/amd64` production build 通过 | PASS，0 data race |
| 安全复审 | 最新实现、恢复与备份闭包未发现 P0/P1；生产单步限制和未来新 Begin 调用者封装列为 P2 防御增强 | PASS WITH RECORDED P2 |

当前生产入口真实覆盖低水位、85% 后无可压缩历史的稳定 stop reason，以及受保护内容
溢出在模型调用/Attempt 前失败关闭。带旧历史的成功摘要/Drop 仍属于纯 Compiler 与
合成 Store/恢复闭包证据；在多轮生产入口出现前，不把它表述成真实多轮 E2E。

### 4.5 ModelProfile 当前树证据

ModelProfile 证据只接受当前 Assembly、Context、Current Store、Universal Loop 与
完整备份闭包，不用品牌分支、最新元数据查询或第二模型路由器代替：

| 范围 | 当前证据 | 判定 |
|---|---|---|
| 精确画像合同 | `model-profile/v1` canonical、typed ref、分类内 tendency 排序/去重、JCS 安全整数和 64 KiB 上限 | PASS |
| 精确模型闭包 | provider/model/build/config/artifact/adapter 任一错配在 Publish/Admission/Run/Attempt 前失败关闭 | PASS |
| 唯一策略效果 | 有效窗口只取 `min(base, profile)`；较大画像不扩张，较小画像参与 85% compilation 门禁 | PASS |
| 轻量无画像路径 | 成员 wire 不出现 `model_profile`，不发现未引用画像 CONFIG，最终 model request 不因画像功能改变 | PASS |
| Store 与恢复 | 发布、Admission、LoadRunForLoop 和 BeginModelDispatch 使用同一冻结闭包；nil compilation 不能绕过收紧水位 | PASS |
| 完整备份 | backup → verify → restore → reopen 后画像 ref 与 canonical CONFIG 字节逐项不变 | PASS |
| 发布前合同修订 | `model-binding-config/v1` 必填 `model_build_id`，seed、Echo artifact 与本地 allowlist 原子重锁；无 Legacy decoder | PASS，方案 A |
| 安全复审 | 无 P0/P1；跨 tendency 分类同名以 `(category, metric_id)` 区分；凭据门禁明确为 known-key + Provider schema 边界 | PASS WITH RECORDED P2 BOUNDARY |

### 4.6 S2 RAG 当前树证据

本切片只接受当前生产 composition、统一 Module Host、同一 Context Compiler、Current
Store、Universal Loop 与完整备份闭包，不用旁路 RAG Runtime、可变索引或合成 evidence
替代真实消费者：

| 范围 | 当前证据 | 判定 |
|---|---|---|
| 最小公开合同 | `context.provide/v1` 明确区分 DECLARATIVE 静态引用与 REQUIRED `TRUSTED_IN_PROCESS` 动态读取；Config、Authority、source 和输出均 canonical 且有上限 | PASS |
| 共享与隔离 | 同一 source artifact 可被不同 Agent/Workspace 绑定；Tenant、Workspace、Agent、Task 四级 scope 与 Config∩Ceiling 逐项验证 | PASS |
| 确定性与污染防护 | 命中、零命中、整数排序和同输入同字节成立；知识正文只作为固定安全提示后的 canonical USER 数据 | PASS |
| Compiler 与 Store | 执行过 RAG 即使低于 85% 也产生 retrieval evidence；新 Attempt 只接受同一 Compiler 的完整 request/compilation bytes | PASS |
| 撤权与调用边界 | Begin 前与实际 Host 调用前执行 current Catalog deny-only 检查；只能拒绝原 Provider，不能换 Binding、fallback 或调用新版本 | PASS |
| Pure Chat 轻量路径 | 同一 Catalog 含 RAG、所选 Run 无动态 Binding 时，不读取知识 artifact、不解析知识 Adapter、不调用知识 Host | PASS |
| 重入与恢复 | 已持久化 Attempt 的显式 retry/Load 不运行 Compiler/Provider；全局启动只做 Store-only ledger/lease 校验并将原 PENDING CAS 为 UNKNOWN，不读取 RAG artifact、Config、Authority、Registry 或 Provider | PASS |
| 完整备份 | backup → verify → restore → reopen 后 source artifact、compilation 与 MODEL_REQUEST 逐字节闭合，检索计数保持一次 | PASS |
| 多 Instance | 同一 artifact+adapter 可供多个 Instance 共用实现，结果仍返回当前冻结 Binding 的完整 Provider identity | PASS |
| 独立复审 | 首轮发现的 1 个 P0 与 4 个 P1 已修复；二次只读复审未发现 P0/P1 | PASS WITH RECORDED P2 |

截至 2026-08-05，“不同冷 artifact 的首次 Loader 串行”这一 P2 已由
`EXACT_ADAPTER_LAZY_MATERIALIZATION = ACCEPTED_DEVELOPMENT_OPTIMIZATION_SLICE` 收口：必需
Model Adapter 仍 eager 构造；可选 exact Adapter 只在已有授权链选中的 Binding 首次使用时
物化。同一 `ArtifactDigest + AdapterIdentity` 合并一个 in-flight Loader，不同精确键并行；
失败、取消、typed nil 与 panic 不留下成功缓存或永久卡键。若 leader Context 结束，仍有效的
等待者只接力其自身原始精确解析；这不执行模块、外部效果或 UNKNOWN 语义重放。

cache/in-flight 不持久化、不备份，也不保存 Catalog、Trust、Authority 或 current Activation
结论；lazy `IsRegistered` 只表示已物化。Bootstrap Activation 继续使用独立 eager 精确注册
视图，production Host 仍在 Registry 前完成冻结 Binding、Authority 与 current Activation
检查。当前剩余非阻塞 P2 是：多个待恢复 Run 可能重复校验同一 artifact；新 RAG Attempt 在
Loop 编译后由 Store 再用同一纯 Compiler 做一次入库证明；Model Begin 与 Host 各执行一次
deny-only 检查以封住竞态。这些优化不得通过可过期授权缓存、第二运行期 Registry、第二恢复器
或弱化首次入库证明来实现。

本优化切片的确定性并发测试覆盖 same-key 多 waiter、digest/adapter 两个键字段、异键并行、
eager hit/无关 probe、Context handoff、普通错误不 handoff、typed nil、panic fan-out 与后续
重试。补充收口后，Knowledge、Memory、TextStats 与 MCP 的生产 Loader 均把 Context 传入
有界构件扫描；遍历与每 32 KiB 文件块检查取消，失败不返回部分文件。MCP initialize、
tools/list、tools/call 与全包校验保留 cancellation/deadline cause，即使 SDK 在关闭竞态中
丢失 cause，本地 child Context 仍可供 `errors.Is` 识别。Module Conformance 与 Current
Backup 复用同一可取消扫描核心，不改变 ArtifactDigest、包格式或备份内容。
Current Backup 自己读取 artifact `module.yaml` 与 bundle `manifest.json` 的最后两个
无 Context 边界也已改为 32 KiB 分块读取；取消/超时原样返回且不带 partial，普通错误
仍分别归入既有 artifact integrity 或 invalid bundle 分类，成功字节与摘要保持不变。

Windows 全仓测试/vet/module verify、Linux/WSL 100 轮乱序 Registry Race 及受影响 production
composition、MCP、Module Conformance、Current Backup 链 Race 均通过；没有模型、网络、
Store writer、Schema、Public Stage 或 release evidence 变化。

### 4.7 S2 Memory 当前树证据

本切片只增加 Agent 轻量 Memory 的普通 `context.provide/v1` 消费者、一种
`MEMORY_SNAPSHOT` 内容和一张 append-only revision 表；不建设 Memory Runtime、Ledger、
后台 worker、缓存或第二事实源：

| 范围 | 当前证据 | 判定 |
|---|---|---|
| 最小公开合同 | `memory-context-binding/v1`、Authority、Snapshot、Request、Output 与 Evidence 均 strict canonical；大小、条目、TTL、计数和文本有界 | PASS |
| Agent/Workspace 隔离 | Snapshot 归属 Tenant + stable AgentID，entry 按 Workspace visibility 过滤；精确查询身份拒绝 `*`，跨 Agent/Workspace 不可见 | PASS |
| 轻量确定性更新 | 只统计 canonical `TASK_INPUT`；配置摘要变化开启新计数序列；4-byte 下限也至少保留一个完整 UTF-8 码点 | PASS |
| Begin 与恢复 | `BEGIN IMMEDIATE` 验证读取 revision 仍为当前 head；Memory evidence、MODEL_REQUEST、PENDING Attempt、Usage、Frame/Event 原子写入；之后只恢复冻结 revision | PASS |
| 成功终态单写者 | 只有模型首次 `SUCCEEDED` 终态事务可基于提交时最新 head 追加 revision；失败、UNKNOWN、重入不写，并发 Run 逐个 rebase | PASS |
| Pure Chat 轻量路径 | 默认 Profile 不绑定 Memory；即使 Catalog 存在 Memory，也不读取 Memory Store/artifact、不解析 Adapter、不调用 Host、不执行更新脚本 | PASS |
| 启动与完整备份 | 全局启动只做 Store-only ledger/lease 校验，不读取 Memory artifact/adapter/config/authority/revision；backup → verify → restore → reopen 后，显式 Run continuation 再闭合完整 revision 链与冻结 compilation 字节 | PASS |
| 独立复审 | 公共第二写者、最小 UTF-8 摘要和通配符精确身份问题均已修复；未以新 Port、第二 Store 或模块名分支换取修复 | PASS |

### 4.8 S2 Action/Gateway 当前树证据

本切片只增加 `action.provider/v1`、一张通用 `dispatch_attempts` 表、Core 所有的
Proposal/Result content 与 Gateway 私有 executor；该 Action/Gateway 批次的独立消费者
是本地 `text.stats`，当时未把 MCP、远程执行或第二外部效果账本算入其证据。后续窄
MCP 复用同一合同的增量证据单列于 §4.9：

| 范围 | 当前证据 | 判定 |
|---|---|---|
| Admission 与 Pure Chat 隔离 | 显式 Action Profile 才 materialize Definition；同 Catalog 存在两个可用 Provider 时，Pure Chat 的 loader、Registry、materializer、Describe/Prepare/executor 与 Action SQL 仍为零，模型请求逐字节不变 | PASS |
| 原子 Begin 与 permit | 模型一、Usage、Proposal、Action PENDING、Frame/Event 单事务提交；五个写入点逐项故障注入均全回滚；并发提交和 32 路 permit 消费只有一个获准 | PASS |
| Gateway 安全门 | PENDING 后再次校验 lease/fencing、撤权、Authority、Effect、预算与 deadline；任一漂移都在 executor 前失败，ActionID 解析后不 fallback | PASS |
| 终态与对账 | SUCCEEDED/FAILED/UNKNOWN 投影、精确终态幂等、不同终态/身份/receipt 冲突和 UNKNOWN 原 Attempt CAS 对账均由直接 Store 合同测试闭合 | PASS |
| 模型二与污染边界 | AVAILABLE 结果只作为固定前缀的不可信 USER 数据追加到冻结模型一请求；source Action Attempt、RequestDigest 与 Usage 序列闭合；第二 Action 明确拒绝 | PASS |
| 真实生产纵链 | `TestProductionActionCompositionRunsModelActionModelChain` 通过真实 seed/artifact/composition 执行 `model-1 → text.stats → model-2`，重入不重复执行 | PASS |
| 崩溃与终态写失败 | 真实 Process.Kill 及 SQLite 终态 UPDATE 故障均发生在 effect ledger 已 fsync 之后；Store-only 启动事务不加载 Action 内容，只把同一 PENDING 原位 CAS 为 UNKNOWN，effect 始终一次 | PASS |
| 启动与完整备份 | MODEL_READY_AFTER_ACTION 重启不抢跑模型二；SUCCEEDED/UNKNOWN、Proposal/Result/receipt/reconciliation evidence、冻结 source 链经 create→verify→restore→reopen 逐字节/语义闭合，executor 计数不变 | PASS |
| 独立复审 | 初审指出的 Gateway 负例、Begin 中途回滚、终态矩阵、reconciliation evidence 与 Pure Chat 统一零访问证据均已补齐；未发现需修改生产实现的 P0 | PASS |

### 4.9 S2 窄 MCP 本地 Tool 当前树证据

本切片只把 MCP `2025-11-25`、官方 Go SDK `v1.6.0` 的
`LOCAL_PROCESS + stdio + Tool-only` 接为现有 `action.provider/v1` Adapter；仍使用既有
Describe/Prepare、Gateway、`dispatch_attempts`、Universal Loop 和完整备份。它没有
MCP Runtime、Store、Ledger、Port 或常驻 session，也不等于完整 MCP 或生产就绪：

| 范围 | 当前证据 | 判定 |
|---|---|---|
| 显式信任与惰性加载 | 无 Operator exact ArtifactDigest grant、错 grant 或未选择 Binding 时拒绝/不加载；只有选中的 Describe/Execute 在进程启动前有界验证完整 artifact | ACCEPTED DEVELOPMENT SLICE |
| 有界 Describe 与纯 Prepare | 精确 initialize/version/tools capability、全分页和工具/定义聚合字节/帧/进程上限；`TestAdapterRejectsInvalidDiscoveryAndStdoutPollution` 的 `aggregate-overflow` 子测试拒绝定义总量溢出；Prepare 不启动进程、不读写 Store | ACCEPTED DEVELOPMENT SLICE |
| Tool-only 单向能力面 | `TestAdapterRejectsInvalidDiscoveryAndStdoutPollution` 的 `unsolicited-notification` 与 `unsolicited-request` 子测试拒绝 server 发起的动态通知、Sampling 等 request，不能借 MCP SDK 扩大当前能力面 | ACCEPTED DEVELOPMENT SLICE |
| Gateway 唯一执行 | 模型一原子写 Action PENDING 后，Gateway 才建立一次短生命周期 stdio 会话并发送一次 `tools/call`；无 retry、fallback、替代进程或同 Attempt 第二次调用 | ACCEPTED DEVELOPMENT SLICE |
| 终态与 UNKNOWN | 精确 server wire JSON-RPC error 和合法 `isError=true` 为 FAILED；发送后 timeout/EOF/crash/local client-closing 或无法证明未执行为原 UNKNOWN | ACCEPTED DEVELOPMENT SLICE |
| 进程生命周期 | deadline/取消/关闭会关闭 stdin，并在有界 grace 后终止/强杀和回收受管进程树；关闭或 kill 不被用作“工具未执行”证明 | ACCEPTED DEVELOPMENT SLICE |
| Cleanup 与终态归属 | `TestAdapterReportsConnectAndDescribeCleanupFailures` 证明 Connect/Describe cleanup 失败不会被丢弃；`TestAdapterConfirmedExecutionKeepsTerminalWhenCleanupFails` 证明 wire 已确认的执行终态保持终态，cleanup 只作为有界诊断 | ACCEPTED DEVELOPMENT SLICE |
| 启动、重入与完整备份 | Store-only 启动事务不加载 MCP；UNKNOWN 经 restart 与 backup→verify→restore 仍是原 Attempt，initialize/list/call 计数不增加 | ACCEPTED DEVELOPMENT SLICE |
| 隔离边界 | 空 env、exact digest、受管进程树不是 OS 文件系统/网络 sandbox；当前只允许 Operator 显式批准且完全信任的本地开发模块，不可信第三方与生产级隔离延后 | ACCEPTED DEVELOPMENT SLICE / NOT PRODUCTION READY |

主要聚焦证据入口包括
`TestProductionMCPCompositionRequiresGrantAndRunsOnceAcrossRestart`、
`TestProductionMCPUnknownIsNeverReplayedAcrossBackupRestore`、
`TestHistoricalMCPUnsettledIsZeroAccessDuringPureChatStartup`、
`TestAdapterDescribePrepareExecuteRealStdio`、
`TestAdapterJSONRPCErrorIsConfirmedFailedWithBoundedDiagnostic`、
`TestAdapterCrashAfterCallIsAmbiguousAndNotRetried`、
`TestAdapterBlockedToolCallWriteReachesDeadlineWithoutRetry`、
`TestCommandTransportCanceledBlockedWriteClosesAndReaps`、
`TestVerifyArtifactDirectoryDigestDetectsWholePackageDrift`、
`TestScanArtifactDirectoryEnforcesPathFileAndByteLimits`、
`TestAdapterRejectsExecutableDriftWithoutStartingProcess`、
`TestAdapterRejectsNonExecutableArtifactWithoutChangingMode`、
`TestAdapterRejectsDescriptorAndSiblingDriftWithoutStartingProcess`、
`TestAdapterRejectsInvalidDiscoveryAndStdoutPollution`、
`TestAdapterReportsConnectAndDescribeCleanupFailures` 与
`TestAdapterConfirmedExecutionKeepsTerminalWhenCleanupFails`。这些聚焦证据接受开发切片，
不替代第 5 节在最终源码静止后必须重新执行的全仓、跨平台、Race、许可证、文档与公开树
门禁。

### 4.10 S2 Channel 当前树证据

本切片只增加默认关闭、Workspace-scoped 的 loopback Channel Port、Cursor 与 ingress
service；模型回复和 Action 后模型二仍由同一个 Universal Loop 产出，发送仍复用唯一
`dispatch_attempts` 与 Gateway。它不建设 Channel Runtime、第二 Store、Outbox 或隐式
后台发送，也不把第一方 loopback 开发适配器冒充公网 Provider：

| 范围 | 当前证据 | 判定 |
|---|---|---|
| 默认关闭与显式启用 | 未提供显式 Channel 开关时不构造 adapter；启用时必须精确选择一个 Tenant/Workspace/Endpoint，匹配 Operator 预置 Cursor，通过 Store 语义闭包，并确认该 Endpoint 不存在未对账 Channel UNKNOWN | ACCEPTED DEVELOPMENT SLICE |
| Workspace ingress 与 Cursor | Cursor seed、单调 CAS、事件 identity/hash 去重和 Workspace 权限均由 Current Store 所有；ACCEPTED receipt 与原 Run Admission 单事务提交，REJECTED 保留有界 canonical envelope 且不能把当前有效身份任意拒绝 | ACCEPTED DEVELOPMENT SLICE |
| 重入与当前权限 | 相同事件只返回原 receipt/Run；TERMINATED 与 `WAITING_RECONCILIATION` 重入只读返回原持久结果，不重验当前 route、不恢复执行、不再次发送；仍可运行的旧 Run 在继续前执行当前 Endpoint/Identity/ACL/Binding deny-only 复核 | ACCEPTED DEVELOPMENT SLICE |
| 唯一效果账本 | 最终 assistant reply、History、Channel Proposal、`CHANNEL_SEND` PENDING 与一次性 permit 原子提交；模型一→一个 Action→模型二→Channel 和无 Action 的模型→Channel 均只通过同一 Loop/Gateway；歧义外部结果为原 Attempt UNKNOWN，禁止语义重放 | ACCEPTED DEVELOPMENT SLICE |
| 严格回复目标与传输 | `account_id`、`conversation_id` 必须同时存在并精确匹配冻结 Workspace Endpoint；Decode、Prepare 与 Execute 均在 Secret/网络之前拒绝漂移。loopback 只接受 literal loopback 目标，单次 POST，无 proxy、隐式重定向、retry 或 fallback | ACCEPTED DEVELOPMENT SLICE |
| 优雅关停 | 关停线性化点先关闭新 Core/Channel Admission，再排空已接收 handler；排空后、关闭 Store 前，以有界 detached Store-only recovery 将遗留 Channel PENDING 原位收口为 UNKNOWN，不加载 adapter 或发起网络请求 | ACCEPTED DEVELOPMENT SLICE |
| 完整备份与恢复 | Cursor、ACCEPTED/REJECTED receipt、Run/Frame/Event/History、`CHANNEL_SEND` Attempt、receipt/evidence 和 WAITING/terminal 投影均进入同一 bundle 语义闭包；restore/reopen 不构造 adapter、不访问 Secret、不发送网络请求 | ACCEPTED DEVELOPMENT SLICE |
| Pure Chat 零访问 | 默认 Pure Chat 不读取 Channel config、Cursor、receipt、SecretRef 或 artifact，不构造 adapter，不开放 Channel ingress；共享账本的 Store-only 完整性扫描不等于加载 Channel | ACCEPTED DEVELOPMENT SLICE |
| Provider 边界 | 当前只验收本机 loopback 适配器；公网 Provider、远程凭据生命周期、平台 webhook 认证与真实第三方互操作延后 | ACCEPTED DEVELOPMENT SLICE / NOT PRODUCTION READY |

上述结论接受 Channel 原子开发切片；它是 Composite 之前的历史树证据。包含后续
Composite 修改的当前树门禁在第 5 节单独记录，不以本节聚焦证据推算测试数量。

### 4.11 S2 Parent/Child + Composite 当前树证据

本切片只增加同 Workspace、depth-1 的一个 Parent coordinator 与 2..8 个 Child
specialist。每个 Run 仍只有一个 Member；它复用同一 Assembly Compiler、Universal Loop、
Current Store、Context Compiler、Model Attempt/Usage 和完整备份，不增加 Multi-Agent
Runtime、Store、Scheduler、Reviewer 或 Learning 状态。

| 范围 | 当前证据 | 判定 |
|---|---|---|
| 原子 family Admission | Parent/Child snapshots、manifests、frames、events 与四个 nullable run 投影在一个事务全成或全败；每个权威写点故障注入零残留，同 Root 并发只产生一个 Created，不同 Root 隔离 | ACCEPTED DEVELOPMENT SLICE |
| 身份与冻结图 | Child admission/run/member/recovery 身份仅由 Parent intent digest + slot 派生；Parent→Child ref 与 Child→Parent manifest digest 单向闭合，无摘要环 | ACCEPTED DEVELOPMENT SLICE |
| Context 与污染隔离 | assignment 仅进受保护 SYSTEM；Child result 仅进固定前缀的 untrusted USER envelope；50% pool 按权重和 slot 稳定分配，完整 envelope 超限在 permit 前失败 | ACCEPTED DEVELOPMENT SLICE |
| `ALL_REQUIRED` 与 Core failure | FAILED 优先于 UNKNOWN/PENDING；两类确定性失败使用 `CORE_DETERMINISTIC_FAILURE`，无 Model/Dispatch Attempt、Usage、History 或伪输出；全成功只 merge 一次 | ACCEPTED DEVELOPMENT SLICE |
| dispatch cap 与 UNKNOWN | family cap 固定 N Child + 1 merge；PENDING/MODEL_UNKNOWN 消耗 slot，不退款、不重放，不创建替代 Attempt | ACCEPTED DEVELOPMENT SLICE |
| 统一取消 | `cancel-request/v1` 只经 `RequestRunCancellation` 安装 first-write-wins latch；family 原子覆盖冻结图，Child 不可直接取消；cancel-vs-Model/Action/Channel permit 只有一个线性化赢家 | ACCEPTED DEVELOPMENT SLICE |
| family Usage/Cost | `GetCompositeFamilyUsageProjection` 只读派生每 Run 事实；NULL 不伪造为零，三类成本独立，混合币种不换算且稳定排序 | ACCEPTED DEVELOPMENT SLICE |
| 重启与完整备份 | Admission、部分 Child、merge PENDING→UNKNOWN 重开均不重新授予 permit；family/result/cancel/Core-failure closure round trip 字节保真，删改或孤立事实 fail-closed | ACCEPTED DEVELOPMENT SLICE |
| 生产入口与 Pure Chat | `chat --composite` 通过真实 production composition 有界并行推进；exact retry 返回同一 family。未选择 Composite 时 optional family wire 省略、三个 parent 列为空且请求 golden bytes 不变；普通 Run 仍可按统一 run-scope 合同设置取消引用 | ACCEPTED DEVELOPMENT SLICE |
| 延后边界 | 该 S2 Composite 切片不含 Reviewer、公平 Scheduler、动态图、Learning、跨 Workspace、Composite 中的 Memory/Action/MCP/Channel；公平 Scheduler 与单次 Reviewer 审核门后续分别按 S3-A/S3-B 验收 | NOT INCLUDED IN THIS S2 SLICE |

## 5. 当前树发布验证

S1 批次 `s1-final-20260802-052440` 继续作为已验收的历史封存，不证明 S2.1 当前树。
S2.1 在仓内修改静止后重新执行并把输出写入外部批次
`s2.1-context-20260803-034909`：

- [x] Windows 全仓 `go test -count=1 -timeout=30m ./...` 与 `go vet ./...`。
- [x] WSL2/Linux 对受影响 Context/Core/Loop/Store 闭包运行 Race Detector。
- [x] WSL2/Linux 运行包门禁与 `linux/amd64` production build。
- [x] 当前树 Secret、许可证、Docs、Capability、Branding 与 PublicTree 扫描。
- [x] 仓库外 Public Stage、manifest、Stage 复核、测试日志和 source seal；最终摘要只
      保存在外部证据批次，不写回 Source。

S2.1 没有声称重新执行“全仓 WSL Race”或全部平台交叉构建；本切片只对受影响的
生产闭包运行 Linux Race，并保留 S1 的更广历史结果。技术证据与第 6 节 operator
方案 A 声明仍保持分离，不能相互替代。

ModelProfile 修改静止后重新执行并把输出写入外部批次
`s2-model-profile-20260803-052323`：

- [x] Windows 全仓 `go test -count=1 -timeout=30m ./...` 与 `go vet ./...`。
- [x] WSL2/Linux 对 ModelProfile 影响闭包运行 Race Detector，0 data race。
- [x] WSL2/Linux 运行 `linux/amd64` production build。
- [x] 当前树 PublicTree、Secret、License、Docs、Capability 与 Branding 扫描。
- [x] 仓库外 Public Stage、manifest、Stage 复核、release gate 和 source seal；最终摘要
      只保存在外部证据批次，不写回 Source。

该历史批次只证明 ModelProfile 最小纵链及其对既有 Context/Store/backup 的影响；它
不证明外部模型互操作、自动评测、自动选模、RAG 或 Memory。RAG 仅由下述后续当前树
证据独立验收。

S2 RAG 修改静止并完成两轮安全复审后，对当前源码树执行：

- [x] Windows 全仓 `go test -count=1 ./...` 与 `go vet ./...`。
- [x] Module Host/Adapter、Store/Loop、production composition/backup 影响闭包连续
      3 轮通过。
- [x] WSL2/Linux 全仓测试与 `go vet ./...` 通过；CoreLoop、CurrentStore、ModuleHost、
      ExactAdapter 和 production composition 的 Race Detector 通过，0 data race。
- [x] Docs、License、Capability、Branding 与 PublicTree 门禁通过；许可证清单为
      26 个 Go dependency、33 个 distributed asset，公开树未发现阻塞项。
- [x] 首轮独立审计的 1 个 P0 与 4 个 P1 全部修复；二次独立复审无 P0/P1。

S2 Memory 修改静止并完成独立安全复审后，对当前源码树执行：

- [x] Windows 全仓 `go test -count=1 ./...` 与 `go vet ./...`。
- [x] WSL2/Linux 全仓测试与 `go vet ./...` 通过；MemoryCore、ContextCompiler、
      CurrentStore、CurrentBackup、ExactAdapter、CoreLoop 和 production composition
      的 Race Detector 通过，0 data race。
- [x] 真实 Memory seed/composition、最小 UTF-8 摘要、Agent/Workspace 隔离、
      成功终态单写者、并发 rebase、重入/UNKNOWN 不写和完整 backup/restore 通过。
- [x] Docs、License、Capability、Branding 与 PublicTree 门禁通过；测试缓存和临时
      产物在验证后删除，受保护资产未触碰。

S2 Action/Gateway 修改静止并完成缺口复审后，对当前源码树执行：

- [x] Windows 全仓 `go test -count=1 -timeout=30m ./...`、`go vet ./...` 与 production
      build 通过。
- [x] WSL2/Linux 全仓测试与 `go vet ./...` 通过；ActionGateway、ActionMaterializer、
      CoreLoop、CurrentStore、CurrentBackup、BootstrapSeed、LocalChat 和 production
      composition 的 Race Detector 通过，0 data race。
- [x] 真实 Action seed/artifact/composition、Pure Chat 零 Action 访问、五点事务回滚、
      一次性 permit、三类终态、UNKNOWN 对账、模型二、Process.Kill、效果后终态写失败
      和完整 backup/restore 通过。
- [x] License 分发资产清单已纳入 `text.stats` artifact、Action seed、更新后的示例和
      18 表 migration；Docs、Capability、Branding 与 PublicTree 门禁通过。

S2 窄 MCP 开发切片完成第 4.9 节的聚焦纵链、故障与零重放证据后，根任务又对当时的
最终静止树执行了以下门禁；这些结果是 Channel 修改前的历史证据：

- [x] Windows 全仓 `go test -count=1 -timeout=30m ./...`、`go vet ./...` 与 production
      build。
- [x] WSL2/Linux 全仓测试与 `go vet ./...`；至少对 ModuleAPI、MCP stdio、CurrentStore
      与 production composition 影响闭包运行 Race Detector，并完成 Linux production
      build。
- [x] License、Docs、Capability、Branding、PublicTree 与 release workflow 自检；确认
      18 表 fingerprint、migration SHA、依赖/分发资产锁和脚本摘要一致。
- [x] 源码树生成物扫描与清理：只删除本轮可再生的临时 DB、二进制、日志、bundle、
      coverage 和 sandbox；保留并只读隔离已声明的 Legacy/Temp/snapshot/evidence 资产。

窄 MCP 静止树当时的实测结果：Windows 与 WSL2/Linux 的 19 个包全仓测试及两端 `go vet`
通过；ModuleAPI、MCP stdio、CurrentStore 与 production composition 的 Linux Race
Detector 通过；`CGO_ENABLED=0` 的 Windows/Linux/Darwin × amd64/arm64 六种真实构建
通过。License（35 个 Go dependency、41 个 distributed asset）、Docs（33 份
Markdown）、Capability（49 项、0 stable）、Branding、PublicTree 及五组 release
workflow 自检均通过。源码树目标残留扫描结果为 0；六平台构建的唯一外部临时目录已
核验并删除，声明的只读资产未被访问、移动、改写或删除。

S2 Channel 聚焦合同、真实 loopback 长链路、关停与完整备份证据已形成
`S2_CHANNEL_ACCEPTED_DEVELOPMENT_SLICE`。以下是 Composite 修改前的 Channel 静止树，曾由根任务重新
取得以下真实输出，不沿用上一棵 19 包树的数量或 seal：

- [x] Windows 与 WSL2/Linux 的 21 包全仓测试及两端 `go vet`；Windows 同时通过普通与
      `-shuffle=on` 两轮测试。
- [x] Channel/Store/Gateway/Loop/HTTP 生命周期在 WSL2/Linux 的 21 包全仓 Race Detector
      中通过，0 data race；同时通过
      Windows/Linux/Darwin × amd64/arm64 构建。
- [x] 19 表 fingerprint 为
      `e1ad672d56a992ee35f20f8b2e269cd182131313c2e04ca0feac0c6310c092ae`；migration
      为 27,867 bytes / SHA-256
      `11516a8588afa8fc5a19e8139da9b4b2c78b2a65d291eb2b271c23b17826c8d5`。License
      35 个 Go dependency / 41 个 distributed asset、Docs 33 份 Markdown、Capability
      49 项 / 0 stable、Branding、PublicTree 以及 12 组 release workflow/扫描器自检均通过。
- [x] 源码树残留扫描为 0：没有临时 DB、二进制、日志、bundle、coverage、profile 或
      sandbox。受保护 Legacy、Temp、snapshot、evidence 资产未被移动、改写或删除；
      仓外本轮专用工具缓存、Stage 和构建产物在最终取证后精确删除。
- [x] 文档定稿后的仓外 Public Stage、manifest 前后复核与 Stage 内 `release-gate -SkipRace`；
      `-SkipRace` 只能与上述已经通过的 WSL2/Linux 全仓 Race 证据配对，不能单独冒充证明。

该 Channel 历史树首轮定稿 Stage 的 pre/post manifest、permanent gates、21 包 shuffle 测试、vet 与六平台
构建均已通过。为避免本次勾选形成自引用缺口，本清单要求从包含本段的最终 Source 再
生成一次 Stage 并通过相同门禁；最终 manifest、构建摘要和 release 输出只在本轮临时批次
中取证，随后按方案 A 清理，不把自引用 seal 写回 Source。

S2 Parent/Child + Composite 修改静止后的历史静止树按以下独立证据收口；本段随 Source
先冻结，再从包含本段的 Source 创建仓外 Public Stage，因此 Stage seal 不反写 Source：

- [x] Windows 与 WSL2/Linux 的 21 个包全仓测试全部通过，两端 `go vet ./...` 通过。
- [x] WSL2 原生 ext4 工作副本使用只读本地 Go module proxy 运行全仓
      `go test -race -count=1 -timeout=30m ./...`，21 个包全部通过，0 data race；不采用
      Windows DrvFS 的权限语义或临时公网 proxy 结果充当 Race 证明。
- [x] `CGO_ENABLED=0` 的 Windows/Linux/Darwin × amd64/arm64 六种生产构建全部通过。
- [x] 该 S2 历史 Store 当时仍为 19 表；fingerprint 为
      `bf2c20dbed0316a829b0a96535e8b816c628036535fe499cf824a25b7d470245`，migration
      为 29,055 bytes / SHA-256
      `7d7162110f972bdde54c6147e4e89544246b83d18a25e96469fb6ffe51c6fd8a`。
- [x] Docs（33 份 Markdown）、License（35 个 Go dependency / 41 个 distributed
      asset）、Capability（49 项 / 0 stable）、Branding 与 PublicTree 门禁通过。
- [x] 文档冻结后的最终 Public Stage 通过独立 manifest 复核以及 Stage 内
      `release-gate -SkipRace`；该开关只与上述独立 Linux 全仓 Race 证据配对，不单独
      冒充 Race 证明。最终 manifest 与 seal 只在任务交付结果中报告，不写回 Source。
- [x] 最终取证后仅删除本轮可再生的构建、Race 与 Stage 临时副本；方案 A 声明的
      Legacy、Temp、snapshot 与 evidence 保护资产保持原位且未被移动、改写或删除。

此前 Action 批次没有创建或改写外部 release-evidence、snapshot、Legacy 或受保护
Temp；也没有生成 Public Stage/source seal，因此不把该批次冒充 `RELEASE_READY`。
Action 批次测试只使用 `t.TempDir()` 或不落入源码树的 Go 缓存；该批次取证后未在源码树
遗留临时数据库、测试二进制、日志或 effect sandbox，受保护资产未触碰。Channel 最终
清理已经由上述 S2 Composite 历史静止树清单覆盖，不再保留待办状态。

旧 `internal/store/sqlite`、backend/transfer 与旧 Skill 证明链继续只存在于仓库外 S0
离线归档。当前候选依赖闭包为第 4.3 节的 31 个自有包，没有恢复 Legacy Runtime、第二
Store、兼容 CLI 或跳过列表。

## 6. Operator 方案 A 确认与适用边界

以下事实不能由代码测试代替，已经由项目所有者在当前任务中于
`2026-08-02T18:09:03Z` 明确确认：

- [x] FreeAgent 从未正式部署。
- [x] 本机 Legacy 工作区、受保护 Temp E2E、S0 snapshot 和 release-evidence 均为
      开发、测试、演练或证据资产，不是生产路径；精确绝对路径只保存在仓库外
      operator-audit 中，不进入公开源码树。
- [x] 不存在远端或其他未申报 writer。
- [x] 不存在待对账的外部效果。
- [x] 上述留档资产继续保留并只读隔离；本确认不授权删除、移动或改写。
- [x] 每轮测试完成并封存所需证据后，删除仅可再生且不再需要的临时数据库、构建
      二进制、Stage 工作副本、测试日志副本和工具缓存；不得把该清理扩大到 Legacy、
      受保护 Temp E2E、snapshot 或任何已声明 evidence。
- [x] 因不存在正式部署，生产停机窗、正式部署身份和首个正式 Admission 在本轮均为
      `NOT_APPLICABLE`。
- [x] 允许从封存并复核后的 S1 开发基线进入 S2。

本机只读审计为上述声明提供技术佐证，但不冒充其权威来源：审计窗口内没有观察到
FreeAgent 进程、服务、计划任务、监听、运行中 WSL/容器 writer；识别出的 FAC1
证据库中 `PENDING=0`、`MODEL_UNKNOWN=0`、非空 lease owner 为 `0`。完整证据保存在
仓库外 operator-audit 目录。

这次确认只关闭“当前开发基线能否进入 S2”的门禁。未来一旦配置首次正式部署，
必须重新提供部署路径、全部 writer、停机/启动窗口、备份、外部效果与首个正式
Admission 证据；不得引用本节把未来部署自动判为 GO。

## 7. 未来首次正式部署顺序

本轮没有正式部署，因此以下步骤没有执行，也不应伪造完成记录。未来首次部署只有
在重新完成当时的发布验证、部署清单和 operator gates 后才能执行：

1. 再次验证当前 plan、两份规格、语义矩阵、Seed、Schema 与 S0 归档哈希。
2. 停止并复核全部 writer；再次运行外部效果清零查询。
3. 归档已枚举的旧开发数据库和 artifact，然后由操作员显式删除开发目标；应用
   启动不得自动删除或迁移。
4. 执行 `freeagent init --db ... --artifact-root ... --seed ...`，记录 Seed、artifact
   locks、FAC1 identity 和 schema fingerprint。
5. 执行 `freeagent backup` 与 `backup-verify`，并向全新空目标做一次 `restore`。
6. 启动只连接 FAC1 Current Store 的候选二进制；Channel 默认关闭，除非另行完成精确
   Workspace/Endpoint/SecretRef、Cursor、UNKNOWN 清零与 Provider 部署授权门禁。
7. 分别通过真实 CLI `chat` 和 loopback HTTP `POST /v1/chat` 完成 Pure Chat，并验证
   相同 request identity 的 retry 返回原 Run。
8. 验证 Usage、History、ModelDispatchAttempt 和终态可在重启后读取；UNKNOWN 不
   自动重放。
9. 再次运行 production dependency closure，确认没有旧 Runtime、Store、migration
   或 Legacy gate。
10. 记录首个正式 Admission 的 RunID、UTC 时间、Store instance ID、current
    PublishedBasis 和备份 manifest digest。

第 10 步完成后形成不可逆时间边界：禁止恢复旧 Runtime 或旧 Store 继续写入。此后
只能在 Current Store 上向前修复，或重新满足同一安全门禁后显式重建开发库；不允许
双写、运行时 fallback、Legacy decoder 或 migration feature flag。

## 8. 当前开发基线判定矩阵

### 8.1 当前源码结构事实

- 生产入口为一个 `cmd/freeagent` composition root。
- 当前闭包包含一个 Assembly Compiler、一个 Universal Loop 和一个 FAC1 Current Store。
- W1 在该 Store 内增加一张 `conversations` 表和三项 `runs` 投影；该历史快照为 21 表，且未增加
  Session 实体、第二 History/Usage、第二 Loop 或第二效果账本。W4-L1A 随后新增唯一的
  `learning_proposals` 表，W4-L1B 复用该表接入静态 Skill Draft；已验收 W4-L2 只在同一表
  增加 unique `review_run_id`/`reviewer_attempt_id` 与 state/revision 约束，W4-L3 继续在同一行
  保存无权限 Version；W4-L4 新增 Schedule/Task 两张窄表后达到 24 表，W5-D1/X1 没有新增表；
  W2-U2 新增五张 Source/Snapshot observation fact 表达到历史 29 表，W2-U3 再新增三张不可变
  Candidate/Review/Decision fact 表达到历史 32 表；W6-2 durable receipt Schema 再增加一张 append-only
  `control_operation_receipts`，达到历史 33 表；W6-3 的八张 observation closure 表达到历史 41 表；W6-5
  的 `module_artifacts` 与 `module_artifact_admissions` 使当前总数达到 43 表。
- 显式 init 与正常 chat/serve 分离；loopback Channel 是默认关闭、按 Workspace 显式
  装配的可选入口/出口，公网 Provider 未接线。
- 声明式 Context、Role 和静态 Skill 共用 `context.provide/v1`，没有 Skill 专用 Runtime。
- 可选 ModelProfile 只冻结到成员快照并收紧 ContextPolicy，没有画像 Port、路由器或表。
- 可选本地 RAG 仍使用 `context.provide/v1`；知识正文位于共享 artifact，证据嵌入唯一
  Context Compilation，没有 RAG Runtime、表、ContentKind 或可变索引事实源。
- 可选本地 Memory 同样使用 `context.provide/v1`；只有一张 append-only revision 表和
  一个 `MEMORY_SNAPSHOT` ContentKind，终态模型事务是非 genesis revision 的唯一写者。
- 可选本地 Action 使用 `action.provider/v1` 的公开 Describe/Prepare 与 Gateway 私有
  executor；通用 `dispatch_attempts` 是唯一副作用账本，Core/模型/SDK 不能直接执行。
- 可选窄 MCP 本地 Tool 只作为同一 Action Adapter；固定 MCP `2025-11-25` 与官方 Go SDK
  `v1.6.0`，Operator exact ArtifactDigest grant 默认关闭，不增加 MCP Runtime/Store/
  Ledger/Port。它没有 OS 文件系统/网络 sandbox，只适用于 Operator 完全信任的开发模块。
- 可选 loopback Channel 通过 Workspace Channel Port/Cursor、`channel_ingress_receipts` 与
  通用 `dispatch_attempts` 接入同一 Loop/Store/Gateway；无 Channel Runtime、Outbox、
  retry 或隐式发送，公网 Provider 延后。
- 可选 Parent/Child + Composite 通过 `ControlSnapshot.CompositeAgents` 和显式
  `chat --composite` 接入同一 Assembly/Loop/Store；每 Run 一个 Member、同 Workspace、
  depth 1、2..8 Child；该 S2 Composite 修改只让 `runs` 增加四个 nullable family/cancel
  投影列，该 S2 历史树当时仍为 19 表。S3-A 历史开发树随后只新增第 20 张
  `workspace_scheduler_state` 表；W1 历史 21 表值以本文开头与第 8.4 节为准，W4-L1A 历史
  22 表值见第 8.7 节，W4-L1B 历史 22 表值见第 8.8 节，W4-L2 历史 22 表值见第 8.9 节；
  W4-L3 历史 22 表值见第 8.10 节，W4-L4 历史 24 表值见第 8.11 节；W5-X1/W5-F1 的历史
  24 表 identity 分别见第 8.12/8.13 节，W2-R1/R2 的历史 24 表 identity 见第 8.5.7/8.5.8 节；
  当前 W2-U3 identity 以本文开头、第 3.3 节与第 8.5.13 节为准。
- 生产依赖闭包是第 4.3 节按 `cmd/freeagent/dependency_test.go` 列出的 35 个自有包；历史 Channel
  新增 `internal/channelservice` 与 `internal/loopbackchannel`，S3-A 新增 `internal/runscheduler`；
  当前闭包另含 exact `internal/remoteactionhttp`、`internal/wasmaction`、`internal/modulesource`、
  `internal/moduleupgrade` 与 `internal/modulehandler`，且未包含列出的旧生产包族。

除已明确标为历史树的表数外，这些是当前源码与依赖列表事实，不替代行为测试、Race、
发布扫描或外部封存。

### 8.2 当前技术证据

本节按原验收日期保留各批次事实，其中 W1 条目中的“真实 DeepSeek 尚未执行”是
2026-08-06 开发切片的历史结论；2026-08-09 的 W1 完成与 W3 集中验收只追加于 §8.6，
W4-L1A/L1B Proposal Store 增量验收只追加于 §8.7/§8.8；W4-L2 增量验收记录只
追加于 §8.9，均不回写本节历史证据。

- [x] S1 的 PublishedBasis、Install/Activate/Bind、Pure Chat、UNKNOWN、关停、恢复、
      backup/restore 与长链路由 S1 外部批次历史验收。
- [x] S2.1 的精确 85%/100% 边界、污染防护、最小 Drop、原子 Attempt、重启恢复和
      compilation-aware 完整 backup/restore 由当前树重验。
- [x] ModelProfile 的 exact binding、只收紧、无画像零发现、nil-compilation 门禁与
      backup/restore 字节保真由当前树重验。
- [x] RAG 的四级 scope、命中/零命中、低水位 evidence、污染隔离、deny-only 撤权、
      无 RAG 零知识读取、冻结 artifact/source 恢复和 backup/restore 字节保真由当前树重验。
- [x] Memory 的 Agent/Workspace 隔离、任务输入专属计数、有界摘要、Begin head 校验、
      成功终态原子 revision、并发 rebase、失败/UNKNOWN/重入不写和完整恢复由当前树重验。
- [x] Action 的显式 Admission、Pure Chat 零访问、Core Proposal、Action PENDING、一次性
      permit、Gateway 私有执行、模型二、UNKNOWN/对账、真实 Kill/终态故障和完整恢复
      由当前树重验。
- [x] 窄 MCP 的 exact grant、真实 stdio model→tool→model、单次 call、FAILED/UNKNOWN、
      受管进程回收、Pure Chat/startup 零 MCP 与 UNKNOWN backup/restore 无重放已由聚焦
      测试验收为开发切片。
- [x] Channel 的显式启用、Cursor/去重/原子 Admission、终态与 WAITING 重入只读、同一
      DispatchAttempt/Gateway/UNKNOWN、模型一→Action→模型二→Channel、严格目标、单次
      POST、优雅关停、完整备份与 Pure Chat 零访问已验收为开发切片。
- [x] Composite 的原子 family Admission、身份/摘要闭包、污染隔离、50% 分配、
      `ALL_REQUIRED`、attempt-free failure、N+1 cap、统一取消、family usage、重启、完整
      backup/restore 与 `chat --composite` 已验收为开发切片。
- [x] Composite 修改后的 S2 历史静止树已通过 Windows/Linux 21 包全仓测试、两端 vet、
      Linux 全仓 Race 与六平台 build；发布扫描和清理结果按 §5 历史批次记录，不借用
      Channel 之前的树或 seal。
- [x] S3-A 当前开发切片已通过受影响包的 Windows 测试/vet、WSL Race、三层并发与公平
      claim、UNKNOWN 禁止重放、优雅关停以及 Scheduler backup/verify/restore；S3-D 的
      全仓 Release、六平台 build、Public Stage 与最终 seal 尚未执行。
- [x] S3-B 当前开发切片已通过受影响包的 Windows 测试/vet 与 WSL Race；覆盖 optional
      Reviewer 原子 Admission、严格 Context/Verdict、canonical MODEL_RESULT、max_tokens
      ceiling、APPROVE-only merge、REJECT/FAILED/INVALID 无 merge、UNKNOWN 零重放、
      cancellation/Usage/Scheduler、Reviewer-disabled bytes，以及包括 APPROVE 后尚未
      merge 合法中间态的 backup/verify/restore。S3-C 真实模型效果与 S3-D 发布门禁尚未执行。
- [x] W1 Conversation 开发切片已实现 `conversations`/runs projections、head/revision CAS、
      完整 USER+ASSISTANT predecessor 恢复、真实本地两轮入口、阈值行为和
      backup/verify/restore 后续聊；Windows/WSL 全仓 `go test`、`go vet` 与 `go mod verify`
      均通过。确定性本地 `TestRunConversationFiftyTurnsAcrossReentryBackupRestore` 也在 Windows
      21.964s、WSL 15.594s 通过；loopback HTTP create/chat 与服务重启 E2E、两个正常退出
      OS 进程 25+25 轮和 DeepSeek 冻结估算费用同事务落账也已补齐。真实 DeepSeek 2/50 轮
      因凭据注入安全边界未执行，W1 整体仍未完成。

W4-L2 已有合同、Store、application service、恢复语义测试和显式真实 DeepSeek runner，
并由 §8.9 记录集中测试、真实结果、完整 Backup 语义门禁与 accepted 裁决。

第 4、5 节明确区分 S1、S2.1 与后续 S2 历史封存、S3-A/S3-B 当前开发切片证据；任何一方
都不能借用另一棵树的日志或 seal。

### 8.3 Operator 结论

- [x] 方案 A 已确认：从未正式部署，当前没有生产路径可枚举。
- [x] 方案 A 已确认：不存在远端/未申报 writer 或待对账外部效果。
- [x] 方案 A 已确认：Legacy、Temp、snapshot 与 evidence 保留并只读隔离。
- [x] 生产停机窗与首个正式 Admission 在本轮标记为 `NOT_APPLICABLE`，没有伪造
      执行记录。
- [x] S2 开发入口已获授权。

因此当前唯一诚实结论是：

```text
S1_ACCEPTED_DEVELOPMENT_BASELINE
S1_ACCEPTED = YES
S2.1_CONTEXT_COMPILER = ACCEPTED_DEVELOPMENT_SLICE
S2_MODEL_PROFILE = ACCEPTED_DEVELOPMENT_SLICE
S2_RAG = ACCEPTED_DEVELOPMENT_SLICE
S2_MEMORY = ACCEPTED_DEVELOPMENT_SLICE
S2_ACTION_GATEWAY = ACCEPTED_DEVELOPMENT_SLICE
S2_MCP_LOCAL_PROCESS_STDIO_TOOL = ACCEPTED_DEVELOPMENT_SLICE
S2_CHANNEL = ACCEPTED_DEVELOPMENT_SLICE
S2_PARENT_CHILD_COMPOSITE = ACCEPTED_DEVELOPMENT_SLICE
S3_FAIR_SCHEDULER = ACCEPTED_DEVELOPMENT_SLICE
S3_COMPOSITE_REVIEW_GATE = ACCEPTED_DEVELOPMENT_SLICE
W1_CONVERSATION = COMPLETE_REAL_DEEPSEEK_50
W3_RAG_MEMORY = COMPLETE_ACCEPTED_DEVELOPMENT_SLICE
W4_L1A_KNOWLEDGE_PROPOSAL_STORE = ACCEPTED_DEVELOPMENT_SLICE
W4_L1B_STATIC_SKILL_PROPOSAL_STORE = ACCEPTED_DEVELOPMENT_SLICE
W4_L2_LEARNING_REVIEW = ACCEPTED_DEVELOPMENT_SLICE
W4_L3_LEARNING_VERSION = ACCEPTED_DEVELOPMENT_SLICE
W4_L4_LEARNING_CYCLE = ACCEPTED_DEVELOPMENT_SLICE
W4_COMPLETE = ACCEPTED_DEVELOPMENT_SLICE
W5_D1_COLLABORATION_REPAIR = ACCEPTED_DEVELOPMENT_SLICE
W5_X1_WORKSPACE_TRANSFER = ACCEPTED_DEVELOPMENT_SLICE
W5_F1_COLLABORATION_STABILITY = ACCEPTED_DEVELOPMENT_SLICE
W5_COMPLETE = ACCEPTED_DEVELOPMENT_SLICE
W2_R1_REMOTE_ACTION_HOST = ACCEPTED_DEVELOPMENT_SLICE
W2_R2_WASM_HOST = ACCEPTED_DEVELOPMENT_SLICE
W2_R3_UNTRUSTED_MODULE_ISOLATION = NEXT
NEXT_FUNCTION_ENTRY = W2_R3_UNTRUSTED_MODULE_ISOLATION
S2_ENTRY = AUTHORIZED
CUTOVER = NOT_APPLICABLE_NEVER_DEPLOYED
CUTOVER_GO = NOT_APPLICABLE
FIRST_FORMAL_ADMISSION = NOT_YET_OCCURRED
FIRST_PRODUCTION_DEPLOYMENT = NOT_APPROVED
RELEASE_READY = NOT_CLAIMED
```

### 8.4 W1 Conversation 开发切片历史验收（2026-08-06 快照）

本节记录的当时裁决为 `W1_CONVERSATION_ACCEPTED_DEVELOPMENT_SLICE`，不是 W1 整体完成；
当前 W1 状态已由 §8.6 更新。按从未部署的
Schema Draft 方案 A，实现在同一 `0001_current.sql` 中增加唯一的 `conversations` 表和三项
`runs` 投影；该 W1 历史快照 Store 为 21 表，fingerprint
`812c8c2c35f8c7eb4426aee6362d568718140d70f7b851b9847e9fcab913c748`，migration 为
31,710 bytes / SHA-256 `b9bd51c0afcd3f2563013c5ead5389194f62d3028c2d94329ebffd921c5ee496`。
没有增加 runtime migration、双写、第二 Store、第二 Loop、第二 History/Usage 或 Conversation
专用效果账本；本文其他 19/20/22 表和旧 hash 继续标识各自历史树。W4-L4 的 24 表值仍只作为
第 8.11 节的历史证据；W5-F1 收口时的 24 表 identity 以第 8.12 节与第 8.13 节为准。

已闭合的开发范围：

- 聊天持久对象只叫 Conversation；Session 仍只作为客户端当前选择。Conversation 固定
  Tenant、Principal、Workspace、Agent、Profile 稳定 ID，每轮建立一个装配不可变的新 Run；
- head NULL/revision 0 创建、精确重入、并发 head/revision CAS 与一轮一 Run 已闭合；只有最终
  回答成功的 head 可以继续，PENDING、MODEL_UNKNOWN/其他 UNKNOWN、FAILED、CANCELLED 均
  fail-closed，不跳过、不分叉、不自动建立替代 Run；
- predecessor 按最旧到最新恢复完整 `TASK_INPUT(USER) + final ASSISTANT MODEL_RESULT` pair，
  不复制旧 `history_entries`；Context Compiler 保持 pair 不可分割，`<85%` 不压缩，
  `>=85% 且 <100%` 最多一次确定性摘要，初始 `>=100%` 只 Drop 达到恢复水位所需的最小
  完整最旧前缀；
- CLI 已支持 Conversation 创建、读取与续聊；同步 `POST /v1/chat` 可以用精确 revision/head
  继续既有 Conversation；loopback `POST /v1/conversations` 严格冻结 scope，首次创建返回
  201、相同 ID/scope 精确重试返回同一记录和 200、scope 漂移返回冲突，且不创建 Run 或
  调用模型；
- 本地真实两轮 Echo 验证第二轮请求恰为 USER1、ASSISTANT1、USER2；精确 retry 不重复调用，
  stale head 不能分叉。soft/full watermark、备份→验证→恢复→第三轮续聊和篡改拒绝均通过；
- 确定性本地 `TestRunConversationFiftyTurnsAcrossReentryBackupRestore` 在 Windows 21.964s、
  WSL 15.594s 通过：闭合 50 Run/Attempt/Usage，turn 25 backup→verify→restore 后继续，
  turn 25/50 精确重入，最终 bundle 复核，并证明 Action/Memory/Channel 零访问；该测试不是
  真实 DeepSeek 50 轮；
- `TestRunServeLoopbackAndGracefulShutdown` 覆盖 HTTP 创建、精确重试、第一轮、服务重启、
  旧请求重入与第二轮；`TestRunConversationFiftyTurnsAcrossNormalProcessRestart` 由两个正常退出
  的独立 OS 进程分别完成 1..25 与 26..50 轮，最终仍是 50 Run/Attempt/Usage，且
  Action/Memory/Channel 零访问；
- 显式启用的正常 DeepSeek Provider 已从历史实验入口分离，固定官方 endpoint，Secret 只在
  dispatch 时解析；生产 Adapter 的 mock E2E 已覆盖一次 POST、Usage/cache/reasoning 与
  UNKNOWN 零重放；
- DeepSeek 成功终态现在只按 Attempt 冻结 PriceSnapshot 与完整 token 派生
  `estimated_cost`，并与原 Usage 同事务持久化；未知价格/token 保持 NULL，reasoning 不重复
  收费，Provider 未报告的 `provider_reported_cost` 和未对账的 `reconciled_cost` 保持 UNKNOWN；
- Windows 与 WSL `go test ./... -count=1`、`go vet ./...`、`go mod verify` 均通过；
- Pure Chat 只增加 Core Conversation 必要读取，对未装配 Role/RAG/Memory/Skill/MCP/
  Action/Channel/Persona/Team 继续零访问。

该 2026-08-06 历史快照中尚未闭合的 W1 条件：真实 DeepSeek 2 轮与连续 50 轮调用因凭据注入安全边界未执行；
Conversation 与 Composite 明确互斥；Conversation+Action、
Conversation+Channel 和公网 Channel 均未在本切片声明成熟。上述剩余项不得用 mock、本地
Echo 或旧 S3-C 数据替代，也不得把本切片状态提升为 `W1_COMPLETE`、`RELEASE_READY` 或生产
可用。

### 8.5 W2-A/B1/B2 与 W2-C 验收记录

当前裁决接受 W2-A/B1/B2 的停机态统一 Apply 与 current-only 模块操作面，并接受 W2-C 本地
只读 Knowledge Apply 开发切片。它们都不表示 W2 通用装配、在线控制面或公开发布完成：

```text
W2_A_UNIFIED_MODULE_APPLY = ACCEPTED_DEVELOPMENT_SLICE
W2_B1_MODULE_OPERATIONS = ACCEPTED_DEVELOPMENT_SLICE
W2_B2_MODULE_DRY_RUN = ACCEPTED_DEVELOPMENT_SLICE
W2_C_KNOWLEDGE_APPLY = ACCEPTED_DEVELOPMENT_SLICE
W2_GENERAL_ASSEMBLY = NOT_COMPLETE
```

- `module-list` 与 `module-inspect` 只使用 immutable/query-only `ReadOnlyObserver`；显式 tenant、
  完整 PublishedBasis、Profile/Binding 语义顺序、同 Port index、canonical 空数组、当前 Catalog
  Instance、artifact/Manifest 复验和默认脱敏输出已经闭合；
- 两个查询不取得 writer fence、不 recovery、不启动模型/MCP/网络/Secret resolver，也不创建
  Run、Host、Attempt 或外部效果；数据库字节不变且不产生 sidecar，预存 WAL/SHM 时拒绝；
- 独立 `module-disable` 只接受 canonical DISABLED plan，并复用 W2-A 的 startup recovery 与唯一
  Control/Catalog CAS；ENABLED plan 在 mutation 前拒绝，exact retry 返回 ALREADY_APPLIED；
  Installation、Activation、artifact 与历史事实保留；
- `module-dry-run` 复用同一判断/候选冻结逻辑，但只使用 ReadOnlyObserver 和系统 TEMP；完整
  observed/candidate Basis、`PROJECTED_NOT_RESERVED`、变化枚举和 PENDING 恢复需求布尔已经
  闭合；UNKNOWN-only 不重放，Store/source/artifact root/sidecar 不变，MCP 不启动进程；
- dry-run→Apply 配对覆盖 ENABLE/DISABLE、Catalog ADD/RETAIN/REMOVE、NO_CHANGE、
  ALREADY_APPLIED、orphan final 精确复用/冲突和 cleanup fail-closed；candidate 不是授权、
  reservation、receipt 或在线提交对象；
- W2-C 没有新增 Port，而是由 exact Config 将同一 `context.provide/v1` 区分为声明式 Context 或
  本地确定性只读 Knowledge。Knowledge Manifest 固定为
  `TRUSTED_IN_PROCESS + go-in-process/v1`，只提供该 Port、无 Requires/permissions；
- Knowledge Apply/Dry-run 在写入前闭合 exact Artifact/Config/Authority/source/scope，要求
  artifact entrypoint、Config 和 Authority 指向同一个 `knowledge-source/v1`，并由当前 Control
  校验 Tenant 与非通配 Workspace/Agent scope；Binding 固定 `REQUIRED` 且无 StaticContext refs；
- 本地策略固定选择 `freeagent.adapter.knowledge.lexical/v1`，模块包不能授予 Trust、选择 Adapter
  或扩大 Authority。Apply/Dry-run 都不加载包内代码；dry-run 不调用 Knowledge provider，也不
  启动 MCP、模型、网络或 Secret resolver；
- W2-C 聚焦验证已覆盖 dry-run `WOULD_APPLY`→Apply `APPLIED` 的 exact Basis、生产 Chat 的真实
  检索消费、精确重试 `ALREADY_APPLIED`、等价计划 `NO_CHANGE`，以及 source/scope/Context
  顺序冲突在 Store 写入前拒绝；禁用后新 Run 不再产生 Knowledge evidence，禁用前 Run 的请求
  与 evidence 仍可读取且逐字节不变；
- 聚焦证据包括 `TestModuleListReportsExactCurrentBindingsWithoutChangingStore`、
  `TestModuleListReturnsCanonicalEmptyBindings`、
  `TestModuleInspectReverifiesCurrentArtifactAndRedactsPolicyBodies`、
  `TestModuleDisableUsesExactApplyAndRejectsEnabledPlansBeforeMutation` 与
  `TestOpenReadOnlyObserverRejectsSQLiteSidecars`；B2 另覆盖
  `TestModuleDryRunDeclarativeWouldApplyMatchesApplyBasisWithoutWrites`、
  `TestModuleDryRunApplyConflictAndPreflightOrderParity`、
  `TestModuleDryRunReenableInactiveActivationDoesNotReserveRevision`、
  `TestModuleDryRunCleansExactTempAfterSuccessErrorAndCancellation` 与
  `TestModuleDryRunPreservesOwnerLockAndExactSchema`；
- Windows `go test ./... -count=1`（122.2s）、`go vet ./...`、`go mod verify` 已通过，独立
  focused 与完整 `cmd/freeagent + internal/currentstore` 复跑同样通过；Store 仍为 21 表，
  没有 schema、migration 或 backup 格式变化。
- W2-C 最终 Windows `go test ./... -count=1`（125.1s，`cmd/freeagent` 122.874s）、
  `go vet ./...` 与 `go mod verify` 通过；Docs（38 份 Markdown）、Capability Matrix
  （49 项/0 stable）、License（35 个 Go dependency/55 个 distributed asset）、Branding 与
  PublicTree 门禁通过，独立静态复审未发现新的 P0/P1。PublicTree 首轮只发现测试伪密钥哨兵
  命名，保持原脱敏测试语义改名后专项测试和全树复扫均通过；没有真实凭据进入仓库。

W2-C 复用既有 Knowledge Runtime、Store、Loop、artifact 与通用 backup；Knowledge Context 不绕行
副作用 Gateway，也没有新增 Gateway、表、migration 或 backup schema。该切片没有进入 W3 的
Collection 标签、计数、重复检索免检、Memory、
远程向量库或知识更新。在 W2-C 收口当时，disabled/history 查询、在线热查、自动发现/升级、
稳定公共控制面和更多 Port 尚未完成；该历史事实不覆盖其后的 W3-M1 与 W2-D，当前状态见下一节。

### 8.5.1 W2-D 本地显式装配与 exact history 更新（2026-08-09）

当前状态为 `W2_D_LOCAL_ASSEMBLY_ACCEPTED_DEVELOPMENT_SLICE`。该 accepted 只覆盖冻结的本地显式
装配 v1；三层成熟度仍严格分开：

```text
W2_LOCAL_ASSEMBLY_V1 = ACCEPTED_DEVELOPMENT_SLICE
OPERATOR_MODULE_APPLY = EXPERIMENTAL
GENERAL_MODULE_ASSEMBLY = PLANNED
```

- 本地 v1 范围只包括声明式 Role/静态 Skill、本地只读 Knowledge、W3-M1 已验收的本地有界
  Memory 与 Operator 完全信任的本地 MCP stdio Action；Memory 合同只是同步 W3-M1 既有事实，
  不改写实现归属；
- `module-history` 由调用方给出同一 Tenant 的 exact Control revision 与 Catalog generation。
  它通过 immutable/query-only Observer 恢复 exact Control/Catalog，再通过既有窄 Installation
  投影读取、解析 stored Manifest，以闭合 Source、Activation、Port 和当时的 Binding refs；
- history 不读取 artifact root、不输出 Manifest 正文或摘要、不构造 historical PublishedBasis，
  也不枚举 revision、计算 diff 或派生 DISABLED 状态。enable 快照与 post-disable 快照在
  backup→verify→restore 后保持 exact 输出逐字节一致；
- 集中验收 `TestW2DLocalV1UnifiedModuleAssembly` 只走 production Apply、Assembly、Chat、Gateway、
  Store、Backup 与 history 路径，闭合五类本地模块、双 Profile/Workspace 隔离、权限拒绝、
  Context/Memory 证据、真实 Gateway Action、恢复后继续使用、Disable 与 exact history；
- 两项 P1 已闭合：共享包缓存仍逐 Catalog Entry 复验 ArtifactDigest、ExecutionClass 与 exact
  Provides；`module-inspect` 对未绑定 Catalog Entry 也必须先闭合 stored Manifest 与完整 Provides。
  每 Entry 篡改和 unbound incomplete-Provides 负例均已通过；
- 最终 Windows `go test -count=1 ./...` 通过（188.9 秒）。WSL2 ext4 环境探针先确认并修正
  `GOTMPDIR` 路径，随后以有效 `START/COMPLETE` 包围的全仓重跑通过（128.1 秒）；探针不记作
  代码失败。`go vet ./...`、`go mod verify`、全仓 `gofmt -l`、License（35 个 Go dependency/
  55 个 distributed asset）和 PublicTree（`PUBLIC_TREE_PASS`）均通过；
- Channel、任意第三方 Action、多 Port/Requires/permissions、REMOTE/WASM、自动发现、不可信代码
  隔离和在线控制面继续属于广义 `planned` 范围。只有 MCP Action 的真实副作用经过唯一 Gateway；
  Role、Skill、Knowledge 与 Memory 不因此绕行副作用 Gateway。

### 8.5.2 W2-E2 受信 `text.stats` 统一 Apply 验收（2026-08-10）

本节只追加 E2 当前证据，不改写 §8.5.1 的 W2-D 历史范围、数字或成熟度。当前状态为
`W2_E2_TRUSTED_TEXT_STATS_APPLY_ACCEPTED_DEVELOPMENT_SLICE`：

- `ENABLED module-apply-plan/v1` 的 module 必须携带 exact
  `expected_runtime_request:{mode,protocol}`。它只表达 Operator 对 Manifest runtime request 的
  预期；Core 使用 exact Port、该 request 与已验证 consumer schema 选择唯一 handler。Plan 和
  Manifest 都不能授予 Trust、ExecutionClass、AdapterIdentity、Authority 或 handler identity；
- E2 验收当时 Core-owned 表恰有 5 个 exact handler tuple：声明式 Context、Knowledge Context、Memory
  Context、MCP Action 与 compiled `text.stats` Action。这里不是 5 种 protocol；当前只有三组
  mode/protocol pair；
- MCP handler 只接受候选摘要匹配的 `--allow-local-mcp-artifact`，compiled `text.stats` handler
  只接受匹配摘要的 `--allow-trusted-in-process-artifact`。两者互斥；声明式 Context、Knowledge
  与 Memory 禁止 grant。该边界不允许任意第三方进程内代码；
- 网络无关的 `TestW2E2TextStatsApplyAcceptanceV1` 从普通 Pure Chat seed 与官方 unpacked
  `text.stats` artifact 开始，不使用 Action seed。production dry-run 保持零写和零发布，Apply
  本身不创建 Run/Attempt；未绑定 Profile 仍是 Pure Chat 且不加载 Adapter；绑定 Profile 后只走
  原 `model-1 → ActionProposal/DispatchAttempt/Gateway → model-2`；
- exact retry 不新增 Run/ModelAttempt/DispatchAttempt。完整 backup→verify→restore 后，历史
  model/action/model 证据逐字段不变，恢复后的新 Run 仍可调用同一 compiled handler；Disable 后
  历史账本保持不变，新 Run 回到单模型 Pure Chat 且零 Action Attempt；
- E2 代表性 Module Apply、Backup/Restore 与聚焦回归均为网络无关测试；没有使用真实 API Key，
  不产生模型效果、缓存命中率、价格或生产 SLA 证据。

E2 没有新增 Runtime、Store、Loop、Gateway、Port、表、执行账本或 backup schema。MCP 与
`text.stats` 的真实 Action 调用都必须经过唯一 Gateway/DispatchAttempt；只有 MCP 启动外部进程。
Operator Module Apply 与 Module Conformance 继续 `experimental`，任意第三方进程内 Action、
`module.general-assembly`、W6/W7 与公开 Beta 继续 `planned`。

### 8.5.3 W2-E3 Workspace Channel Apply 集中验收（2026-08-10）

本节只追加 E3 当前证据，不改写 §8.5.1/§8.5.2 的历史范围和当时数字。当前状态为
`W2_E3_WORKSPACE_CHANNEL_APPLY_ACCEPTED_DEVELOPMENT_SLICE`：

- Core-owned 当前表由 E2 当时的 5 个扩为 6 个 exact handler tuple；新增且唯一的第六项是
  `channel.transport/v1 + TRUSTED_IN_PROCESS/go-in-process/v1 + channel-binding-config/v1`。
  Channel Binding target 固定为 `WORKSPACE_CHANNEL_ENDPOINT`，不能绑定 Profile；
- `ENABLED` Channel Apply 在同一 `BEGIN IMMEDIATE` 内提交下一 Control、Catalog 与 revision-0
  `CURSOR_SEED`。`module-dry-run` 不写 Store、不解析 Secret、不联网；Apply 本身也不构造
  Channel Adapter 或发送。exact retry 即使当前 Cursor 已继续推进，仍读取并逐字节验证原始
  revision-0 seed，不把当前最大 revision 冒充发布 seed；
- Disable 只从当前 Control 删除目标 Workspace Endpoint，保留 Cursor、Run、DispatchAttempt、
  immutable Control/Catalog、Module History 和 evidence。多个 Endpoint 可引用同一 Instance；仅当
  最后一个 current 引用消失时才从下一 Catalog 删除该 Instance；
- 集中验收 `TestW2E3ChannelMultiWorkspaceIsolationBackupV1` 使用同一 Agent/Profile、同一共享
  Channel Instance，分别绑定两个 Workspace/Endpoint。A 的成功流与 B 的 Channel `UNKNOWN` 流在
  Cursor、Run、receipt、终态和 Module History 上保持隔离；两类 exact duplicate 都不再调用 Loop
  或二次发送，B 的未对账 `UNKNOWN` 不阻塞 A 重新 composition 并继续运行；
- Disable B 后 A 仍引用共享 Instance，当前 Catalog entry 保留，A/B 历史 Module Binding 投影均
  不变。完整 backup→verify→restore 保存 5 条 ingress receipt、2 个 Cursor scope、0 个 Channel
  PENDING 与 1 个 Channel `UNKNOWN`；恢复后 A Cursor 从 revision 2 推进到 3，B 保持 revision 1，
  历史 Module Binding 投影跨恢复逐字节一致；
- 新集中验收、5 项相邻 Channel/Module Apply 回归、3 项 Current Backup 回归、3 项 Channel
  Service 回归，以及 `go vet ./cmd/freeagent` 均通过。该批次只构造 Channel delivery `UNKNOWN`，
  未单独构造 `MODEL_UNKNOWN`。

E3 没有新增 Runtime、Store、Loop、Gateway、Port、表、执行账本或 backup schema。Operator Module
Apply 与 Module Conformance 继续 `experimental`；公网或任意第三方 Channel Provider、任意第三方
进程内代码、广义通用装配、W6/W7、公开 Beta、生产部署和 `RELEASE_READY` 继续为 `planned` 或未获准。

### 8.5.4 W2-E4 DeepSeek Model 替换集中验收（2026-08-10）

本节只追加 E4 当前证据，不改写 §8.5.1–§8.5.3 的历史范围和当时数字。当前状态为
`W2_E4_DEEPSEEK_MODEL_REPLACEMENT_ACCEPTED_DEVELOPMENT_SLICE / W2_E5_NEXT`：

- Core-owned handler 表从 E3 的 6 个扩为 7 个 exact tuple；新增且唯一的第七项是
  `model.generate/v1 + TRUSTED_IN_PROCESS/go-in-process/v1 + model-binding-config/v1`，只接受
  exact DeepSeek built-in、同一 artifact/adapter/provider Instance 和 Flash↔Pro replacement；
- `module-apply-plan/v1` 是该 Model Binding 的完整期望状态。Apply 原子替换 Config、
  Authority 和 optional ModelProfile；省略 Profile 明确清除旧 ref，提供时必须精确匹配新
  Model Binding 并且只能收紧 ContextPolicy。Model `DISABLED` 本切片禁止；
- 新 Apply 必须使用 strict `model-authority-ceiling/v1`。Authority 内 exact SecretRef 只由命令
  边界的 transient local grant 授予，不持久化；SecretRef identity 不匹配在 credential lookup
  和 HTTP 前 fail-closed，只允许官方 DeepSeek endpoint；
- PriceSnapshot 必须预先存在并与 Config 的 provider/model/billing 精确匹配。Store
  publication 拒绝未知 Authority Schema；legacy 只允许 exact byte-equal deny-all 兼容身份。
  Backup 重验尚无 Run 的 current Model Binding 对 Config/Authority/PriceSnapshot/optional
  ModelProfile 的闭包；
- 替换只影响新 Run。旧 Run 保留冻结 Config/Authority/ModelProfile/PriceSnapshot；原
  `MODEL_UNKNOWN` 在 replacement、rollback 或 exact retry 后不新建 Attempt、不调用新
  Provider、不 fallback 也不语义重放。同一 Agent 可在不同 Workspace/Profile 使用各自冻结
  Model Binding，不串流。

主要新验收名称：

- `TestRestoreModuleApplyPlanV1ModelReplacementBoundaries`；
- `TestModuleApplyDeepSeekModelReplacementDryRunRetryRollbackAndOldRunFreeze`；
- `TestModuleApplyDeepSeekModelPreflightFailsClosedBeforeMutation`；
- `TestModuleApplyDeepSeekModelProfileBackupRestoreExactClosure`；
- `TestModuleApplyDeepSeekModelProfileCanBeClearedByExplicitReplacement`；
- `TestModuleApplyDeepSeekModelBackupGateRejectsCurrentMissingPriceWithoutRun`；
- `TestModuleApplyDeepSeekModelNewRunUsageAndUnknownNeverSubstitute`；
- `TestModuleApplyDeepSeekModelSameAgentDifferentWorkspaceProfiles`；
- `TestEnvironmentDeepSeekResolverRejectsSecretRefBeforeCredentialLookup`；
- `TestPublishControlCatalogRejectsUnknownModelAuthoritySchemaWithoutWrites`；
- `TestPublishControlCatalogRejectsModelPriceSnapshotClosureWithoutWrites`。

已通过命令：

```text
go test -count=1 ./cmd/freeagent -run 'ModuleApplyDeepSeekModel|RestoreModuleApplyPlanV1Model|EnvironmentDeepSeekResolver' -timeout 10m
go test -count=1 ./internal/currentstore -timeout 15m
go test -count=1 ./internal/coreloop -timeout 15m
go test -count=1 ./cmd/freeagent -timeout 20m
go test -count=1 ./internal/currentbackup -timeout 20m
go test -count=1 ./sdk/moduleapi ./internal/deepseekmodel ./internal/modulehost ./internal/localchat ./internal/channelservice -timeout 15m
go test -count=1 ./... -timeout 25m
go vet ./...
go mod verify
```

其中 `internal/currentstore` 为 84.252s、`internal/coreloop` 为 7.805s、完整
`cmd/freeagent` 为 145.811s、`internal/currentbackup` 为 89.202s；其余上列受影响包全部
通过。后续全仓测试、`go vet ./...`、`go mod verify` 与全仓 gofmt 门禁也均通过。

E4 没有新增 Store Schema/migration、表、Runtime、Loop、Gateway、Port 或执行账本。Operator Module Apply
与 Module Conformance 继续 `experimental`；通用 Model Provider、自动选模/路由、任意第三方进程内
代码、公开 Beta、生产部署和 `RELEASE_READY` 仍未获准。下一唯一状态是 `W2_E5_NEXT`。

### 8.5.5 W2-E5-A Requires 与 permission grant 集中验收（2026-08-10）

本节只追加 E5-A 当前证据，不回写 §8.5.4 的 E4 历史状态、命令、耗时或当时的
`W2_E5_NEXT`。当前状态统一为
`W2_E5_A_REQUIRES_PERMISSION_GRANT_ACCEPTED_DEVELOPMENT_SLICE / W2_E5_B_NEXT`：

- `sdk/moduleapi` 中的共享 classifier 是 Apply、Dry-run、生产加载、Current Store publication、
  exact retry、公开 Verify 与 Backup/Restore 的同一 Knowledge Manifest 形状判定。它只接受
  immutable legacy `{requires:[], requested_permissions:[]}`，或 governed
  `{requires:[model.generate/v1], requested_permissions:[knowledge.read]}`；permission-only、
  require-only、重复、重排、额外 Port/permission 和其他部分声明全部拒绝；
- governed Knowledge 仍只提供一个 `context.provide/v1`。Require 必须解析到目标 Profile 内唯一
  `model.generate/v1` Binding；缺失、歧义或只在其他 Profile 存在都 fail-closed。依赖继续沿
  `Binding → Catalog → exact Activation → Installation → canonical Manifest` 验证 identity、
  digest、runtime 与 Provides，不从调用方或模型正文拼装第二张依赖图；
- `knowledge.read` 只是 Manifest 请求。有效 grant 由 Core 从 exact Knowledge Config、Authority、
  同一 source、Tenant、Control 内 Workspace/Agent scope 与读取上限的交集派生；Manifest、Plan 和
  Provider 不能自授权，也没有新增 grant 表、grant Store 或运行时 fallback；
- Apply 的 writer view 与 Dry-run 的 immutable observer 在判断 `ALREADY_APPLIED`、`NO_CHANGE`，
  创建 TEMP/staging、发布 artifact 或首次 Store 写入之前，都先运行完整 current publication
  closure。损坏的 Installation/Manifest、Activation、Catalog、Binding、Requires 或 permission
  闭包不能通过 fast-path 冒充成功，失败保持零 candidate publication 与零新 Store/Artifact 写入；
- governed Knowledge 已通过真实本地 RAG compilation、Config/Authority limit narrowing、exact
  retry、Disable、完整 Backup→Verify→Restore→reopen 及历史 dispatch canonical bytes 不变验收。
  Restore 不调用 Provider、模型、网络或 Secret；Disable 不删除旧 Run、Attempt、retrieval 或
  immutable module facts；
- 原有 UNKNOWN 合同未改变：Apply outcome unknown 不映射到 Model/Action/Channel Attempt；现有
  `MODEL_UNKNOWN`、Action/Channel UNKNOWN 只允许对账原 Attempt，不得语义重放、换 Provider 或
  创建替代 Attempt。本批没有真实 API 调用。

该 E5-A 历史验收当时的树证据为：`go test ./...` 的 `30` 个仓内 Go package 全部通过，完整运行耗时 `221.9s`；另有
`1` 个 external compatibility package 由 `sdk/moduleapi` 嵌套测试在临时独立 module 中编译验证；`go vet ./...`、
`go mod verify` 与全仓 gofmt 门禁通过；Docs 为 `38` 份 Markdown，Capability Matrix 为 `49` 项/
`0 stable`，License 为 `35` 个 Go dependency/`57` 个 distributed asset，Branding 与 PublicTree
门禁均通过。E5-A 没有新增 schema、migration、表、Port 或执行账本，也没有新增第二
Runtime、Store、Loop 或 Gateway。

该验收不等于完整 E5。它只允许一个 Knowledge provider 的单一 provided Port、一个 exact
same-Profile Model Require 和一个受 Core 裁剪的 permission request；通用 multi-Port provider/
consumer、多个 ProviderBinding 合并、REMOTE/WASM、任意第三方或不可信包内代码、自动发现、
在线热装配、Beta、生产部署与 `RELEASE_READY` 仍未获准。下一唯一状态是 `W2_E5_B_NEXT`。

### 8.5.6 W2-E5-B 固定 Document Insight 双 Port 集中验收（2026-08-11）

本节只追加 E5-B 当前证据，不回写 §8.5.5 的 E5-A 历史状态、`221.9s`、License `35/57`、
当时门禁或 `W2_E5_B_NEXT`。当前状态统一为
`W2_E5_B_DOCUMENT_INSIGHT_DUAL_PORT_ACCEPTED_DEVELOPMENT_SLICE`：

- 唯一受理模块固定为 `freeagent.builtin.document-insight@1.0.0`，ArtifactDigest 为
  `838ff9ddd45186f0cdb26021d16902b2bfd7581c2dc0d7b72014cf4f48d0f7ea`，covered size 为
  `1097` bytes，Adapter identity 为 `freeagent.adapter.document-insight/v1`。Manifest 必须请求
  `TRUSTED_IN_PROCESS/go-in-process/v1` 与 `content/source.json`，按 canonical 顺序恰好提供
  `[action.provider/v1, context.provide/v1]`、只 Require `[model.generate/v1]`、只请求
  `[knowledge.read]`；
- 既有 7 个 protocol handler tuple 未被泛化。Core 在其前增加两个只属于上述
  Module/Version/Digest 的保留 selector，分别闭合 Knowledge Context consumer schema 与 Action
  consumer schema。ID、版本、摘要、Port、runtime、entrypoint、Provides 顺序、Require 或 permission
  任一漂移都 fail-closed，不得回退到普通 Knowledge 或 `text.stats` handler；
- 启用必须按 Context→Action 执行两个显式 `module-apply-plan/v1` CAS。Action-first 的 Apply 与
  Dry-run 在 TEMP/staging、artifact publication 和 Store 写入前以 `TARGET_CONFLICT` 零写拒绝；
  Context 成功后，Action Dry-run/Apply 复用同一 Installation、Activation、Catalog Instance、
  Artifact 与 Adapter。两步只接受同一固定摘要的 trusted-in-process artifact grant，该窄例外不能
  授权普通 Knowledge、Memory、任意第三方 Action 或包内代码；
- 一个已绑定 Run 同时冻结并消费两个使用完整相同 `ActivatedModuleRef` 的 Document Insight
  Binding；Context PortPlan 另保留 `context.basic`。该 Run 完成真实本地 RAG、
  Gateway `text.stats`、两个 Model Attempt、一个 Action Attempt，以及非零且可验证的
  `ActionResultReservation`。未绑定 Profile/Workspace 保持零加载；exact Chat retry 不新增 Run、
  Attempt 或效果；
- 完整 Backup→Verify→Restore 后，RAG、Action 与 MemberExecutionSnapshot canonical bytes 不变，
  恢复后的新 Run 继续消费两个 Port。禁用必须按 Action→Context：先移除 Action 后 Context/RAG
  仍可用；Action 尚在时 Context-first Disable 以 `PUBLICATION_FAILED` 零写拒绝；最后移除 Context
  后 current Catalog Instance 消失，新 Run 回到 Pure Chat。

已通过的当前代码证据为：

```text
go test ./cmd/freeagent -run '^TestW2E5BDocumentInsightProductAcceptanceV1$' -count=3
go test -count=1 ./cmd/freeagent -timeout 20m
go test -count=1 ./... -timeout 25m
go vet ./...
go mod verify
```

网络无关产品 E2E 连续 `3/3` 通过；独立 `cmd/freeagent` package 输出为 `185.746s`、墙钟为
`186.968s`。`30` 个仓内 Go package 的全仓墙钟为 `217.539s`，其中 `cmd/freeagent` package 为
`215.004s`；另有 `1` 个 external compatibility package 由 SDK 嵌套测试编译验证。vet、mod verify
与全仓 gofmt 门禁通过；Docs 为 `38` 份 Markdown，Capability Matrix 为 `49` 项/`0 stable`，License
为 `35` 个 Go dependency/`59` 个 distributed asset，Branding 与 PublicTree 均通过。本切片没有调用真实 API。

E5-B 没有新增 Schema、migration、表、Port、第二 Runtime/Store/Loop/Gateway 或效果账本。它只
证明一个受信、编译进 Core、身份固定的双 Port 产品纵链；不代表任意第三方进程内代码、通用
multi-Port、多个 ProviderBinding 合并、REMOTE/WASM、动态注册、自动发现或在线热装配已经完成。
Operator Module Apply 与 Module Conformance 继续为 `experimental`，广义通用装配继续为
`planned`；公开 Beta、生产部署与 `RELEASE_READY` 仍未获准。

### 8.5.7 W2-R1 REMOTE Action Host 集中验收（2026-08-11）

本节只追加 R1 当前证据，不回写 §8.5.6 的 E5-B 历史状态、耗时、License、门禁或当时边界。
当前状态统一为：

```text
W2_R1_REMOTE_ACTION_HOST = ACCEPTED_DEVELOPMENT_SLICE
W2_R2_WASM_HOST = NEXT
PRODUCTION_DEPLOYMENT = NOT_APPROVED
PUBLIC_BETA = NOT_APPROVED
```

R1 只验收默认关闭的 exact `action.provider/v1 + REMOTE/freeagent-action-http/v1` 开发切片：

- Manifest 仍只能提出运行请求。entrypoint 必须是 ArtifactDigest 覆盖的 canonical `content/`
  descriptor；descriptor 只声明有界 Action definitions，不能携带 endpoint、Secret、proxy、redirect、
  retry、fallback 或动态发现指令。未知 REMOTE 协议可以通过离线格式验证，但没有 exact Core
  handler 与 Operator grant 时不能 Activate、Bind 或执行；
- `module-apply` 与 `module-dry-run` 使用第 8 个 exact handler，并分别要求 exact artifact、HTTPS
  endpoint 与 SecretRef grant。两者只冻结和发布事实，不解析 Secret value、不启动 Provider、
  不发 POST。生产 runtime 默认关闭，必须显式启用 REMOTE Action 并提供 exact
  SecretRef→环境变量映射；
- production 路径固定为 `Catalog → lazy loader → covered digest/size validation →
  remoteactionhttp.NewFromArtifact → native Adapter → Gateway → ExecutePrepared`。Describe/Prepare
  保持离线，generic `ModuleHost.Invoke` 被禁止；只有 Gateway 能在原 DispatchAttempt 已持久为
  `PENDING` 后进入执行，模型和模块 SDK 均不能直接调用执行入口；
- 原生传输只允许一次 HTTPS/1.1 POST，TLS 最低 1.2；不使用 proxy、redirect、keep-alive、HTTP/2、
  retry 或 fallback，连接前拒绝 private、loopback、link-local、multicast、NAT64 与其他 special-use
  地址。Secret 只在 dispatch 时晚绑定，Store、Backup、日志与错误只保存 SecretRef identity；
- POST 前的确定性拒绝进入原 Attempt `FAILED`；进入传输后无法证明结果时只进入原 Attempt
  `UNKNOWN`，禁止语义重放、换 Provider 或创建替代 Attempt。合法 Provider `FAILED + HTTP 422`
  只接受一次 RoundTrip，不把业务失败误判为可重试传输失败。

集中代码证据包括：

```text
TestW2R1NativeRemoteProductionChainFailsClosedBeforePOSTV1
TestW2R1ProductionLazyLoaderRejectsTamperedArtifactV1
TestW2R1DisabledRemoteProviderHasZeroProductionAccessV1
TestExecutePreparedAcceptsProviderFailedResponseExactlyOnce
TestW2R1RemoteActionProductionLongChainBackupRestoreV1
TestW2R1RemoteActionPendingReopenBecomesUnknownWithoutReplayV1
```

production composition 证据在原 Attempt `PENDING` 之后、POST 之前确定性 fail-closed，未向生产
composition 注入测试 transport；篡改 descriptor 连续加载均被拒绝且失败不缓存，Secret resolver
调用为 0；Disable 后删除 artifact 再运行 Pure Chat，REMOTE artifact、Adapter、resolver 与 Action
Attempt 访问均为 0。原生 Adapter/transport 的分层 hermetic 证据覆盖一次成功或业务失败、歧义
`UNKNOWN`、终态持久化失败、重启及完整 Backup/Restore，但本轮没有真实公网 HTTPS Provider E2E，
也没有调用真实外部 API。

上述新增生产路径测试、相关包测试、`go vet` 与格式门禁均通过；完整 `cmd/freeagent` package 测试
通过，耗时约 `160.837s`。当前 Store 在同一从未部署的 Draft 上重新冻结：

```text
ordinary tables = 24
schema fingerprint = dcad8f8837ecc5832f444acbc2680a2c2debff5161319710f60e7f649e4aede5
migration bytes = 44237
migration SHA-256 = fd7270130b5b13e5dfe9e3bb0a3bc934d46eb0c3b4a0647b1a3fddd81773e665
```

R1 没有新增普通表、第二 Runtime/Store/Loop/Gateway、Catalog pointer 或效果账本。该 accepted
development slice 不代表生产就绪、公开 Beta、真实第三方互操作、其他 REMOTE 协议、WASM、
不可信第三方隔离、签名/发现/升级、通用 multi-Port/多 Binding 或广义通用装配已经完成；Operator
Module Apply 与 Module Conformance 仍为 `experimental`。当前唯一下一入口为
`W2_R2_WASM_HOST`，不得以 R1 的网络 Host 证据替代 WASM/不可信本地隔离验收。该句保留 R1
收口时的历史状态，当前状态以下一节为准。

### 8.5.8 W2-R2 WASM Action Host 集中验收（2026-08-11）

本节只追加 R2 当前证据，不回写 §8.5.7 的 R1 历史状态、耗时、License、门禁或当时边界。当前
状态统一为：

```text
W2_R1_REMOTE_ACTION_HOST = ACCEPTED_DEVELOPMENT_SLICE
W2_R2_WASM_HOST = ACCEPTED_DEVELOPMENT_SLICE
W2_R3_UNTRUSTED_MODULE_ISOLATION = NEXT
PRODUCTION_DEPLOYMENT = NOT_APPROVED
PUBLIC_BETA = NOT_APPROVED
```

R2 只验收默认关闭的 exact
`action.provider/v1 + WASM/freeagent-action-wasm/v1 + action-binding-config/v1` 开发切片：

- Core-owned Operator 表从 8 个增加到 9 个 exact handler；Enable/Dry-run 需要与候选完全相同的
  `--allow-wasm-action-artifact`，且 LOCAL_PROCESS/TRUSTED/REMOTE/WASM 四类 artifact grant 互斥。
  Candidate、staged final 和 current-state 均重验摘要、descriptor、固定 ABI 与静态资源上限；Apply/
  Dry-run 不实例化、不执行 guest；
- production runtime 默认关闭，只有显式 `--enable-wasm-actions` 才允许 exact Catalog lazy loader
  逐次重验构件并物化 native Adapter。开关不携带 Secret、endpoint、HTTP client、文件或网络
  capability；generic `ModuleHost.Invoke` 固定拒绝；
- Host 固定使用 wazero `v1.12.0` interpreter、Core WASM 1.0、wasm32，只允许 `memory`、
  `freeagent_alloc_v1`、`freeagent_execute_v1` 三个导出，零 imports/WASI/Host Module/start/额外导出。
  module、request、result、memory、table、声明数量、code/local/data/element/custom section、validation/
  execution wall clock 与全进程并发 instance 均有 Core-owned 上限；
- 真实 production composition 从 Pure Chat Store 经统一 Apply，穿过 model→ActionProposal→原 PENDING
  Attempt→唯一 Gateway→private ExecutePrepared→native WASM→model。成功 result 与 canonical usage
  receipt 进入原 Attempt；receipt 明确 `instruction_metering=UNSUPPORTED`，没有 token/cost/price；
- guest trap、timeout/cancel、ABI、指针、超限或 output 错误均为确定性 FAILED。guest 已返回但终态
  transaction 失败时，原 Attempt 留在 PENDING；启动 safety transaction 只把它原位恢复为
  `UNKNOWN/RECOVERED_PENDING_AFTER_CRASH`。删除 artifact 后 exact retry 仍等待对账，零新 Run、零新
  Attempt、零 lazy load，证明 UNKNOWN 禁止语义重放、换 Provider 或替代 Attempt；
- 完整 Backup create→verify→restore 逐字节保留 WASM Activation、descriptor、guest bytes、Binding、
  Action result/receipt 与 UNKNOWN。Backup/Verify/Restore 不编译、不实例化、不执行 guest；恢复后删除
  artifact 的 terminal exact retry 只返回冻结事实。

安全专项还覆盖 imports/WASI/start/导出、memory/table、type/function/global、body/code/local、data/
element/custom section、`br_table`、输入/输出、trap、超时、取消、并发 instance 与关闭路径的负例。
R2 未调用真实 API，也没有新增第二 Runtime/Store/Loop/Gateway、Port、Catalog pointer 或效果账本；
唯一 schema 变化是既有 Activation execution-class 枚举加入 `WASM`：

```text
ordinary tables = 24
schema fingerprint = 7d2e0850a0253a630d5e2264c720b10fbac3adfdb6f9e53fcd722c2dd2615a8
migration bytes = 44257
migration SHA-256 = 2520299390885b4c23df85d2ff217629f6a458c368735e5bdc0df2eca37b8f2f
```

wazero 依赖已按 Apache-2.0 的 LICENSE/NOTICE、module selection、`go.sum` 与完整 source SPDX scan
纳入发布许可锁；许可清单当前为 36 个 Go module。R2 的接受边界是无 Host capability 的窄纯计算
WASM Host，不是 OS/container，也不声称生产级恶意多租户隔离、任意第三方包、撤权清场、签名/
发现/升级、通用 multi-Port/多 Binding、在线控制面、公开 Beta 或 `RELEASE_READY`。Role、Persona、
Memory、RAG、Skill、MCP、Channel、REMOTE 与 WASM 继续都是可选外挂；Agent/Workspace 轻量、共享
授权 RAG 和 Pure Chat 零外挂边界不变。下一唯一入口为
`W2_R3_UNTRUSTED_MODULE_ISOLATION`。

### 8.5.9 W2-R3 不可信第三方 WASM 纯计算与停机撤权验收（2026-08-12）

本节覆盖 §8.5.8 的“R3 下一入口”历史口径，不改写 R2 的 ABI、上限、耗时或当时证据。当前状态为：

```text
W2_R2_WASM_HOST = ACCEPTED_DEVELOPMENT_SLICE
W2_R3_UNTRUSTED_MODULE_ISOLATION = ACCEPTED_DEVELOPMENT_SLICE
NEXT = SIGNING_SOURCE_POLICY_DISCOVERY_UPGRADE_DEFAULT_OFF
PRODUCTION_DEPLOYMENT = NOT_APPROVED
PUBLIC_BETA = NOT_APPROVED
```

R3 只收口显式授权的非 builtin 第三方 WASM `Effect=none` 纯计算模块与 deny-only 撤权：

- `chat/serve` 必须同时显式设置 `--enable-wasm-actions` 和至少一个 exact
  `--allow-wasm-runtime-artifact <sha256>`；allowlist 不持久化，非 canonical、重复或非当前 Catalog
  artifact 均启动失败，恢复旧 Backup 也不会仅凭全局开关自动恢复运行资格；
- WASM Adapter 只缓存 ModuleID、Version、ArtifactDigest、ExecutionClass 与 AdapterIdentity，不缓存
  Tenant、Workspace、InstanceID、ActivationRevision、Config、Authority 或 Binding 权限。同一 artifact
  的多个合法 Activation 可共享只读 Adapter；
- Gateway 在 lazy resolution 前与私有 executor 前做两次 current Activation 检查。第二次成功是
  execution-admission linearization point；此前撤权确定性 FAILED 且 guest=0，此后已经接纳的
  `Effect=none` guest 可在既有 5 秒 ceiling 内收口；terminal/UNKNOWN exact retry 零 guest 重放；
- 生产撤权只支持停机顺序：关闭 Admission、排空/取消、Store-only PENDING recovery、进程退出销毁
  Registry cache、离线 `module-disable` exact CAS、从 disabled Catalog 重启。活动 composition 占用 Store
  时 Disable 返回 `STORE_BUSY` 且 Control/Catalog 零变化；
- 非 builtin `example.wasm.text-stats` 已通过真实 Apply→production composition→Gateway→WASM 长链。
  Disable 保留 immutable Installation/Activation 与历史 terminal Run/Attempt；禁用后 backup→verify→
  restore 仍保持 disabled current。删除源/恢复目录的历史 artifact 后，旧 exact retry 只读冻结事实，新
  Pure Chat 成功且 Action Attempt、loader、Adapter 和 guest 均为零；
- first-check 前、两次检查之间与 second-check 后三类撤权竞态，以及 20 次 shuffle 的 WASM/Gateway
  安全专项均已通过；当前环境 `CGO_ENABLED=0`，未把 race 结果伪报为已执行。

最终静止树 `go test -count=1 -timeout=30m ./...` 的 32 个仓内 package 全部通过，墙钟
`183.240s`，`cmd/freeagent` 为 `181.228s`；`go vet ./...`、`go mod verify` 与 `gofmt -l .` 通过。
Docs Gate 为 38 份 Markdown，Capability Matrix 为 49 项、稳定级 0 项，License 为 36 个 Go dependency/
59 个 distributed asset，Branding 与 PublicTree 均通过。本轮未调用真实 API。

R3 没有新增普通表、列、Runtime、Store、Loop、Gateway、Catalog pointer、Port、revocation ledger 或
效果账本；24 表 Store identity 与 R2 保持不变。它不声称防御 Go/wazero/OS 漏洞，不提供硬 CPU、fuel、
instruction、RSS 上限、热撤权、运行中 guest 强杀、OS/container、seccomp、namespace、restricted token
或生产恶意多租户隔离。OS 级 sandbox 属于 Beta 后候选；下一入口是默认关闭的模块签名、来源策略、
发现与升级。

### 8.5.10 W2-U0 模块签名、来源、发现与候选纯合同验收（2026-08-12）

本节覆盖 §8.5.9 的供应链下一入口，但只验收纯合同，不提前声明 Source I/O、持久发现或升级 Apply：

```text
W2_U0_SIGNING_SOURCE_CONTRACT = ACCEPTED_DEVELOPMENT_SLICE
NEXT = W2_U1_SIGNING_SOURCE_POLICY
STORE_SCHEMA = UNCHANGED
NETWORK_OR_REAL_MODEL_CALL = NONE
PRODUCTION_DEPLOYMENT = NOT_APPROVED
PUBLIC_BETA = NOT_APPROVED
```

U0 在 `sdk/moduleapi` 增加并冻结七份 authority-free canonical wire：

- `module-publisher-key/v1`、`module-signature/v1`：v1 只允许 Ed25519，Publisher Key ID 由 exact
  32-byte 公钥派生，Signature 只覆盖域分隔后的 exact ArtifactDigest；错误 key、错误 digest、截断
  signature 与 key-ID substitution 均失败；
- `module-source-policy/v1`：Operator-owned Source kind、origin/root digest、network mode、signature/key、
  Module ID prefixes 和 Core-owned resource limits；Manifest/Index 不能自授权；
- `module-discovery-index/v1`、`module-discovery-snapshot/v1`：允许显式空 observation；entries 稳定排序，
  PackagePath 最多 4 KiB，Snapshot 证明 exact Policy/Index parents；
- `module-upgrade-candidate/v1`、`module-candidate-decision/v1`：Version 为 exact opaque string，不解释
  SemVer/`latest`。Candidate 必须存在于 exact Snapshot，并按 `{SourceID, ModuleRef, ArtifactDigest}`
  派生跨刷新稳定 review key；Decision 只记录 APPROVE/REJECT 与非授权归因；
- 七份 wire 均拒绝 unknown field、非 RFC 8785 canonical、错误 content ID、非法 UTF-8/控制字符和
  超限输入，返回集合/bytes/pointer 均与 caller 隔离；七份 canonical/content-ID 及 ReviewKey 均有
  hard-coded canary。

最终静止树 `go test -count=1 -timeout=30m ./...` 的 32 个仓内 package 全部通过，墙钟
`243.957s`，`cmd/freeagent` 为 `228.807s`。`sdk/moduleapi` 的
`go test -count=20 -shuffle=on` 在 `15.360s` 通过。当前 Go 工具链为 `CGO_ENABLED=0`，因此
`-race` 明确未执行，不把不可用结果冒充通过。`go vet ./...`、`go mod verify`、`gofmt -l .`、
Docs Gate（38 Markdown）、Capability Matrix（49 项/0 stable）、License（36 dependency/59 asset）、
Branding 与 PublicTree 均通过。第一轮全仓只暴露旧文档守卫仍要求 R3-next 文案；守卫更新为
U0 accepted/U1 next 后，定向与最终全仓均通过。

U0 没有修改 migration、普通表、Runtime、Store、Loop、Gateway、Catalog、Port、Attempt、Usage 或
效果账本。Current Store 仍为 24 表，fingerprint
`7d2e0850a0253a630d5e2264c720b10fbac3adfdb6f9e53fcd722c2dd2615a8`；migration 仍为
44,257 bytes，SHA-256 `2520299390885b4c23df85d2ff217629f6a458c368735e5bdc0df2eca37b8f2f`。
本轮没有真实 API、Provider、Module Host、Artifact、Installation、Activation、Apply 或外部效果。

W2-U1 已把签名验证与本地来源策略接入现有 `module-verify` 和非持久候选预检，且不接入或执行
staging。U1 收口时的唯一入口是 `W2_U2_DISCOVERY_SNAPSHOT_NEXT`。Candidate 的轻量 Snapshot parser 只证明
canonical/content-ID 自洽；U2/U4 持久化准入必须从唯一 Store 读取已经完成
SourcePolicy→Index→Snapshot parent closure 的事实，并在
跨 Source/Snapshot 聚合层拒绝同一 Module ID + exact Version 的不同 ArtifactDigest。W6、自动发现、
自动升级、广义通用装配、生产部署与 `RELEASE_READY` 仍未获准。

### 8.5.11 W2-U1 已验收边界登记

本节记录已经通过阶段门禁的 U1 narrow slice；验收只限下述非持久 observation：

```text
W2_U1_SIGNING_SOURCE_POLICY = ACCEPTED_DEVELOPMENT_SLICE
SOURCE_KIND = LOCAL_DIRECTORY
NETWORK = DENY
STORE_SCHEMA = UNCHANGED
RESERVATION_OR_STAGE = NONE
APPLY_AUTHORITY = NONE
```

- 无 supply flag 的 `module-verify` 必须保持 legacy 路径的成功 JSON 与失败边界逐字节兼容；任一
  supply flag 都显式切换 governed mode，partial tuple 失败且不得回落；
- Policy、Publisher Key、detached Ed25519 Signature 的 canonical 文件和 exact IDs 全部从 artifact
  外输入；签名只绑定域分隔 exact ArtifactDigest。是否要求签名、点分段 Module ID prefix 和 package
  ceiling 只由 Policy 决定，Manifest/Key/Signature 不授予 runtime、Trust、Authority 或 Effect；
- Policy `max_package_bytes` 同时约束 first scan 与 final scan；最终 digest/size 和 source/artifact
  directory identity 必须复验。unsigned direct verifier 拒绝 unanchored Key、Signature 或 revocation；
- U0 unsigned Snapshot entry 的 `signature_id` 仍可为空或携带语法有效的 authority-free observation
  ID；Snapshot 不验证签名。U1 direct preflight 没有 Snapshot parent 锚点，不能据此接受 unsigned
  Policy 下的 Key/Signature/revocation side input；
- revoked Publisher Key IDs 只形成 invocation-scoped immutable deny snapshot。它不是持久 current
  revocation，不改写历史 Installation、Activation、Run 或 Attempt；
- U1 拒绝 HTTPS Policy，应用不创建 Source 网络 transport。Windows 在本地文件访问前拒绝
  UNC/device namespace，并拒绝 ADS、symlink/reparse；不声称检测映射到普通盘符的网络盘；
- 成功输出仍是 `freeagent.module-package-verification/v1`，且不会创建 reservation、grant、stage、
  Store fact、Installation、Activation 或 Binding，也不能作为 `module-apply` authority。手工 exact-grant
  Apply 和九个 exact handler 保持原语义。

U1 收口时尚未实现的 U2/U4 才负责从唯一 Store 闭合
SourcePolicy→Index→Snapshot parent、current revocation，并在真正 staging 前进行第二次 current 检查；
随后仍须经过 Operator review、现有 `module-dry-run`、expected pointer revision 与唯一 Apply/CAS。

### 8.5.12 W2-U2 显式 Source observation 与不可变 Snapshot 验收（2026-08-12）

本节覆盖 §8.5.11 当时的 U2 下一入口，但只验收 observation-only 纵链：

```text
W2_U2_DISCOVERY_SNAPSHOT = ACCEPTED_DEVELOPMENT_SLICE
NEXT = W2_U3_UPGRADE_REVIEW
DEFAULT = DISABLED
PACKAGE_FETCH = NONE
CANDIDATE_OR_DECISION = NONE
STAGE_OR_APPLY = NONE
NETWORK_OR_REAL_MODEL_CALL = HERMETIC_TEST_ONLY
PRODUCTION_DEPLOYMENT = NOT_APPROVED
PUBLIC_BETA = NOT_APPROVED
```

- `sdk/moduleapi` 的 Index parser/restore、Snapshot parent 与 owned canonical bytes 已闭合，所有 entry
  声明 package size 总和受 Core-owned 1 GiB ceiling，精确边界和 overflow-like 输入均有负例；
- `module-source-register`、`module-source-refresh` 与 `module-publisher-key-revoke` 都要求显式
  `--enable-module-discovery`。关闭、参数不完整、Local/HTTPS 混用、错误 content ID 或 stale revision
  均在依赖/I/O/Store mutation 前失败关闭；输出只含稳定安全投影，refresh 使用 `entry_count`；
- Local Provider 只读固定 `root/index.json`，双次有界读取并复验目录/文件 identity；拒绝 UNC/device
  namespace、ADS、symlink/reparse 与 drift；
- HTTPS Provider 另需 `--enable-https-module-discovery` 与 exact HTTPS origin allowlist。Policy
  OriginDigest 绑定 exact canonical index URL；请求固定为单次 HTTP/1.1 GET，不使用 proxy、redirect、
  retry、HTTP/2、keep-alive、compression、cookie、credentials 或 conditional request。DNS 上限 16，
  任一 special-use/non-public 地址使整次拒绝；测试仅使用 hermetic loopback seam，不调用真实公网；
- Source Provider 只返回 authority-free Index/Snapshot canonical bytes，不能写 Store、获取 package、读取
  entry package path、验证 detached Signature、访问 Secret、启动 Host 或调用 Apply；
- Store 在 Source I/O 前读取 exact refresh basis，I/O 后于同一 `BEGIN IMMEDIATE` 内重验 Source/
  Policy/Publisher Key revision 与全局不可逆 revocation。SourceID 不能改绑 Kind/OriginDigest；exact
  retry 返回相同 Snapshot/revision/timestamp，A→B→A 不冒充 retry；
- 全局 `ModuleID + opaque exact Version → ArtifactDigest` 冲突门禁同时覆盖跨 Source Snapshot、既有
  Installation 与 materialized Learning Version；同 ref/同 digest 可共享，异 digest 两个方向都拒绝；
- Backup create/verify/restore 在同一只读数据库闭包内重建 Key/Policy/Index/Snapshot、parent/head/
  revisions、ordered entries 与全局 ModuleRef。全链零网络、零 Source/包读取，Restore 不触发 refresh；
- Pure Chat、普通启动和恢复未显式选择 U2 命令时对 Local/HTTPS Provider 与 discovery state 零访问。

U2 在从未部署的 Schema Draft 上增加五张 typed fact 表，当前 identity 为：

```text
ordinary tables = 29
schema fingerprint = 6d2bded477e7b1d2c755bf47f5b41f63f1b2f7b568fd72496fc43fbceac3a5f1
migration bytes = 49972
migration SHA-256 = d2bcc27bff2e17a058165f7c544b0c97cd1c99efca3264ab401631477611263e
```

新增表仅为 `module_publisher_keys`、`module_sources`、`module_discovery_snapshots`、
`module_discovery_module_refs` 与 `module_discovery_entries`。R1/R2/R3/U0/U1 的 24 表 identity 与阶段
记录继续只属于各自历史切片，不得被本节全局替换。U2 未创建 Candidate/Decision/reservation/stage/
Installation/Activation/Binding/Control/Catalog/Run/Attempt/Usage，也没有改动现有九个 exact Apply
handler。下一入口 `W2_U3_UPGRADE_REVIEW_NEXT` 必须另行验收 Candidate/review；U4 才能在 staging
前复验 current facts 并进入既有 Dry-run/Apply/CAS。

### 8.5.13 W2-U3 离线 Upgrade Review 历史验收（2026-08-13）

本节覆盖 §8.5.12 的 U3 下一入口，但不提前执行 U4 Apply；完整历史收口 marker 是
`W2_U3_UPGRADE_REVIEW_ACCEPTED_DEVELOPMENT_SLICE / W2_U4_OPERATOR_APPLY_NEXT`：

```text
W2_U3_UPGRADE_REVIEW = ACCEPTED_DEVELOPMENT_SLICE
NEXT = W2_U4_OPERATOR_APPLY  # historical marker
DEFAULT = DISABLED
CANDIDATE_KIND = EXACT_VERSION_CHANGE_ONLY
PACKAGE_FETCH = NONE
TARGET_INPUT = EXPLICIT_LOCAL_UNPACKED_ARTIFACT
STAGE_INSTALL_ACTIVATE_GRANT_BIND_APPLY = NONE
HOST_RUN_ATTEMPT_USAGE_EXTERNAL_EFFECT = NONE
REAL_API_CALL = 0
PRODUCTION_DEPLOYMENT = NOT_APPROVED
PUBLIC_BETA = NOT_APPROVED
```

- `module-upgrade-review` 与 `module-upgrade-decide` 都要求显式
  `--enable-module-upgrade-review`。关闭、partial flags、非法 scope、Windows UNC/device/ADS 路径、错误
  content ID 或非 canonical identity 都在 Store/file/Registry/Host/network 之前失败关闭；
- 首片只接受同一 Module ID、不同 opaque exact Version、不同 ArtifactDigest 和不同 target Instance 的
  `EXACT_VERSION_CHANGE`。U0 `INSTALL` Candidate 仍只是纯合同；Core 不解释 SemVer、`latest` 或
  newer/downgrade，Operator rollback 只能显式写入 reason；
- Store 从 exact current Snapshot 重建 SourcePolicy→Index→Snapshot→entry，从 exact selected Binding
  重建 PublishedBasis→Catalog Instance→Activation revision→Installation，并列出同 current Instance 的
  全部 published Binding impacts。调用方自带 canonical/digest/“latest”不能冒充权威事实；
- target package 只来自 Operator 显式本地 unpacked artifact，使用同一有界 verifier 做两次 evidence scan；
  参与 ArtifactDigest 的 canonical Manifest 必须逐字节一致。signed Source 另需 exact live Publisher Key 与
  detached Signature；系统不读取 Index `PackagePath`、不联网、不下载 package；
- target canonical Manifest 以既有 `content_records` 的 content-addressed record 保存，并由 Review 的
  `manifest_ref` 精确引用。Review/CLI 只含安全 Manifest summary、完整 runtime request、diff、handler、
  published Binding refs、PublishedBasis 与 digest-only grant requirements；不含本地主机路径、URL、
  Signature bytes、Secret、Config/Authority 正文；
- `module-upgrade-review/v1` 结论只允许 `WOULD_APPLY/CONFLICT/UNSUPPORTED`。target port removal、Require
  变化、requested permission 扩大、unknown/conflicting handler、target Instance collision 与额外 grant
  必须确定性进入 diff/reason；该首片没有 `NO_CHANGE`；
- Candidate 是全局不可变 supply fact；Review 按 Tenant 与 exact scope 冻结 current/target evidence；
  Decision 终态绑定 exact `review_id`。`APPROVE` 只允许 current basis 仍一致的 `WOULD_APPLY`，且不产生
  authority。`REJECT` 必须显式 `--confirm-tenant-wide-reject`，按 `{tenant_id, review_key}` 跨同 Tenant
  scope 抑制重复候选，不污染另一 Tenant；
- exact Review/Decision retry 先核对原 immutable fact 与完整 semantic closure，不重建、重放或替换
  Review。parent/Snapshot/Manifest evidence/publication/Decision tamper、stale current、current Key revocation、
  same-version/digest drift、跨 Tenant 或并发异 Decision 均失败关闭；
- Backup create/verify/restore/reopen 保留 Candidate、Review、Decision、target Manifest evidence 与 U2
  Source parents。历史 signed Review 在 Publisher Key 后续撤销后仍可验证；新的 Review/APPROVE 必须按
  current revocation 失败。Backup/Restore 零 Source、零网络、零 package、零 Host；
- Pure Chat 零访问三张 U3 表、target artifact、signature、Source Provider 与 handler assessor；
  Installation、Activation、Control、Catalog、Run、Attempt、Usage 和外部效果保持零变化。

U3 在唯一 Schema Draft 中加入且只加入：

```text
module_upgrade_candidates
module_upgrade_reviews
module_candidate_decisions

ordinary tables = 32
schema fingerprint = 37258c1939308be83f21a109e59a19e32946734d1c53b1b262f876dc36e47dbd
migration bytes = 57652
migration SHA-256 = e4f1047eb527b65ab050d444062f3d286cc2ad87d56e60b1cd7f4e96df87652d
```

U2 的 29 表、R1/R2/R3 的 24 表 identity 继续只属于各自历史切片。当前 `go list ./...` 为 35 个
package，production dependency allowlist 同为 35，新增依赖边界只包含 `internal/modulesource`、
`internal/moduleupgrade` 与唯一 `internal/modulehandler` 的生产接线；没有第二 Runtime、Store、Loop、Gateway、Catalog、handler 表或
效果账本。当前 distributed-assets manifest SHA-256 为
`1894061e74995bf1a6f3a69efe51d6a2bccf46ba72e722dced0e7c1989467a88`。

U4 的入口门没有再写一套审核器：原 CLI 私有的 9 条 generic/Model policy 与 2 条 Document Insight reserved
exact selector 已提取为 Review 与 Apply 共用的唯一 `internal/modulehandler` 实现，禁止复制第二张 handler
表或让两个判断漂移。其“下一原子片重验 exact APPROVE/current basis 并进入既有 Apply/CAS”是当时的
历史说明；完成结果见 §8.5.15。

### 8.5.14 W2-U4 共享 Handler 入口门历史验收（2026-08-13）

本节只收口 U4 的前置重构，不提前声明 approved Apply 已完成：

```text
W2_U4_SHARED_HANDLER_GATE = ACCEPTED_DEVELOPMENT_SLICE
GENERIC_HANDLER_POLICIES = 9
RESERVED_EXACT_SELECTORS = 2
STORE_SCHEMA = UNCHANGED_32_TABLES
APPROVED_APPLY = NOT_YET_CONNECTED  # true at this historical gate only
NEXT = W2_U4_OPERATOR_APPLY         # historical marker
```

- 新增纯 application/domain 包 `internal/modulehandler`，它只依赖 `sdk/moduleapi` 与标准库，不打开 Store、
  artifact、Secret、网络、进程、Host、Registry 或 guest；项目内依赖白名单精确限制为该包自身与 SDK；
- 唯一不可变表由 9 个 generic/Model Handler policy 与 2 条 Document Insight reserved exact selector 组成。
  Apply 的 exact resolver、Binding policy 与 U3 Review 的 `SUPPORTED/CONFLICT/UNSUPPORTED`、digest-only grant
  projection 都调用同一实现；reserved identity 发生 version/digest/Port/runtime/schema 漂移时不得 generic fallback；
- 共享身份测试逐项核对实际 DeepSeek、MCP、REMOTE、WASM、loopback、Document Insight 与 `text.stats`
  Adapter 常量；REMOTE 的 artifact/endpoint/SecretRef 三项 grant、Model 的 artifact/SecretRef 两项 grant 与
  U3 Review 七种 GrantKind 均有闭包测试；
- 真实 Binding 矩阵覆盖 9 条 generic/Model policy 与 2 条 reserved exact selector，另覆盖已知专用 Binding 冲突、
  unknown tuple、损坏 canonical wire、grant 顺序和无 fallback。代表性 Dry-run/Apply 长链覆盖 Declarative、
  Knowledge、Memory、MCP、DeepSeek、`text.stats`、Channel、Document Insight、REMOTE 与 WASM，并保留 Pure Chat；
- 入口门没有新增或修改 Store migration。该入口门验收当时 identity 仍为 32 tables / fingerprint
  `37258c1939308be83f21a109e59a19e32946734d1c53b1b262f876dc36e47dbd` / migration 57,652 bytes /
  SHA-256 `e4f1047eb527b65ab050d444062f3d286cc2ad87d56e60b1cd7f4e96df87652d`；
- 本门不消费 APPROVE、不生成 Apply plan、不 stage/Install/Activate/Bind、不授予 artifact/runtime/network/
  Secret/Authority，也不改变 U3、Operator Module Apply 或 Module Conformance 的成熟度。下一原子片仍须从
  Store 重建 exact Review+APPROVE，重验 current Source/Key/Snapshot/PublishedBasis、target artifact、Signature、
  shared Handler 与全部瞬时 grants，再进入既有 Dry-run/Apply/CAS。

### 8.5.15 W2-U4 approved Declarative Profile Context Apply 验收（2026-08-13）

本节完成 §8.5.14 的历史 NEXT，但不把 Operator Module Apply 整体提升为 accepted：

```text
W2_U4_OPERATOR_APPLY = ACCEPTED_DEVELOPMENT_SLICE
COMMAND = module-upgrade-apply
DEFAULT = DISABLED
APPROVAL = EXACT_TENANT_REVIEW_APPROVE_DECISION
SOURCE = EXPLICIT_LOCAL_DIRECTORY_DENY
GRANTS = ALL_FLAGS_EXPLICIT_AND_EMPTY
IMPACT = ONE_PROFILE_CONTEXT_DECLARATIVE_STATIC_V1
REPLACEMENT = EXACT_ORDINAL_IN_PLACE
STORE_SCHEMA = UNCHANGED_32_TABLES
UNKNOWN_REPLAY = FORBIDDEN
DURABLE_APPROVAL_RECEIPT = NONE
NEXT = W6_0_CONTROL_API_CONTRACT
```

- `module-upgrade-apply` 必须显式设置 `--enable-module-upgrade-apply`、closed Store、artifact root、
  `--source-root`、exact Tenant/ReviewID/DecisionID、target artifact 和显式 `--signature`（unsigned 为空，
  signed 为 detached Signature）。关闭、
  partial flags、非法 canonical identity 或任何非空 grant 必须在可见 mutation 前失败；
- exact Decision 必须是该 Tenant/Review 的 APPROVE；Review 必须仍是 current `WOULD_APPLY`，只含一个
  PROFILE `context.provide/v1` BindingImpact。首片只允许 `DECLARATIVE + static/v1 +
  TRUSTED_INSTRUCTION + deny-all Authority`；Model、Channel、Action、`SINGLE`、shared-current、fanout、
  多 impact 与任意 grant 都确定失败关闭；
- Source 必须是 current `LOCAL_DIRECTORY + DENY`，显式 `--source-root` 与 frozen OriginDigest/Policy
  匹配；实现不读取 Index `PackagePath`、不接受 HTTPS、不联网或下载。所有 LOCAL_PROCESS、
  TRUSTED_IN_PROCESS、REMOTE、WASM、endpoint 与 SecretRef grant flags 均必须显式出现且为空；
- production 顺序为 historical U1 verify → current Store approval/source/binding revalidation → 重建并
  逐字节确认 same exact canonical plan → current U1 verify → pre-stage revalidation → 既有 Apply/CAS。
  Review 与 Apply 复用唯一 `internal/modulehandler` evaluator；没有第二 handler 表、独立 dry-run admission、
  approval authority 或旁路 publication；
- `module-apply-plan/v1` 的 exact replacement seam 只在原 PortPlan ordinal 把 old Instance 换成 target。
  其他 Binding canonical bytes 与顺序逐字不变；旧 Run 的 MemberExecutionSnapshot/static context 不改写，
  新 Run 才冻结 target；
- 相邻 current exact retry 返回原 PlanDigest/Pointer/Control/Catalog publication，零 source I/O、零 staging、
  零新 Store mutation。这只是 adjacent plan idempotency，不是 durable approval-bound receipt；一旦出现
  后续 publication，旧 approval 重试返回 `POINTER_CONFLICT`；
- Apply 无法证明终态时只保留原 UNKNOWN 结论；禁止重新执行、换 Provider、生成替代 Attempt 或语义
  replay。Backup/Verify/Restore 保存 Source/Review/Decision/Manifest/final publication 的 exact closure，
  恢复不读取 source、不联网、不重新 Apply；Pure Chat 仍零 Source/Review/artifact/stage/handler 访问；
- U4 不新增 Schema/table/Runtime/Store/Loop/Gateway/Catalog pointer/handler table/effect ledger。Current Store
  继续是 32 tables / fingerprint
  `37258c1939308be83f21a109e59a19e32946734d1c53b1b262f876dc36e47dbd` / migration 57,652 bytes /
  SHA-256 `e4f1047eb527b65ab050d444062f3d286cc2ad87d56e60b1cd7f4e96df87652d`。

该 U4 历史状态是
`W2_U4_OPERATOR_APPLY_ACCEPTED_DEVELOPMENT_SLICE / W6_0_CONTROL_API_CONTRACT_NEXT`。该历史 NEXT 只授权
后续控制 API 合同设计；自动 upgrade worker、Model/Channel/Action upgrade、广义通用装配、在线 Apply、
公开 Beta 和生产部署仍未批准。

### 8.5.16 W6-0 控制 API 纯合同历史验收（2026-08-13）

W6-0 当时状态是
`W6_0_CONTROL_API_CONTRACT_ACCEPTED_DEVELOPMENT_SLICE / W6_1_APPLICATION_SERVICES_READ_API_NEXT`。

- `internal/controlapicontract` 冻结 `control-session/v1`、`control-scope/v1`、
  `control-view-snapshot/v1`、`control-operation-request/v1`、`control-operation-receipt/v1` 与
  `control-event-cursor/v1` 六份 strict canonical wire，均有 closed enum、独立 digest domain、
  restore/canonical canary 与 defensive-copy；
- Web view 的 schema/digest 是 `control-view-snapshot/v1` / `freeagent.control-view-snapshot/v1`；Core
  原有 `control-snapshot/v1` / `freeagent.control-snapshot/v1` 保持不变；
- Operation request 显式冻结 `DRY_RUN/MUTATE` intent，动态 request ID 与时间不进入 semantic request。
  `UNKNOWN` 只能读取原 exact receipt，禁止语义重放、换 operation 或生成替代 receipt；
- `internal/controlapipolicy` 只冻结 transport-neutral 的 strong ETag/If-Match、keyset pagination、
  idempotency digest、body/结构 ceiling、并发与 loopback threat policy；W6-0 不含 listener、HTTP handler、
  session store、SSE、UI 或 production `cmd/freeagent` wiring；
- W6-0 不新增 Schema、migration 或 receipt table。当时证据保持 39 份 Markdown、37 个源码 package、
  35 个 production dependency-closure package与 32 表 Store；Store fingerprint、57,652-byte migration
  与 SHA-256 仍为本清单第 3.3 节的当前 identity；
- `control-plane.online` 在该历史切片继续 `planned`。`W6_1_APPLICATION_SERVICES_READ_API_NEXT` 当时只
  授权增加默认关闭的 read/Dry-run application services。

### 8.5.17 W6-1 Application Services / Read API 验收（2026-08-13）

历史收口是
`W6_1_APPLICATION_SERVICES_READ_API_ACCEPTED_DEVELOPMENT_SLICE / W6_2_CONTROLLED_MUTATIONS_AUDIT_NEXT`。

- `freeagent serve` 的 Control 默认关闭；关闭路径不创建 Control listener、handoff、session/cursor
  registry 或 Control Application Services。仅提供 `--control-handoff-path` 会在 Store/listener I/O 前拒绝；
- 显式启用时验证非管理员身份，复用唯一 Composition/Store，构造 Modules 与 `MODULE_DISABLE` Dry-run
  services 和 startup-closed Admission；随后绑定 Chat listener 与 exact `tcp4 127.0.0.1:0` Control
  listener，从实际 socket 得到 origin，再构造 scope/session/cursor/HTTP handler。两个 server 在 Admission
  关闭时启动，owner-only exclusive handoff 与 readiness 完成后才一次性开放；失败零请求、关停共享 30 秒
  budget 且只执行一次 Store recovery/close；
- bootstrap 5 分钟且一次性，session absolute 8 小时/idle 30 分钟。除 bootstrap exchange 外，Modules
  GET 与非安全方法都必须同时携带 exact session cookie 和 session-bound CSRF header；Cookie 不绑定端口，
  因此 GET 缺失或错误 CSRF 必须返回 401 且不得调用 Application Service；
- `GET /control/api/v1/modules`、`GET /control/api/v1/modules/{instance_id}` 已闭合 scope-filtered DTO、
  keyset cursor、strong ETag/If-None-Match 与跨 Workspace/敏感字段负例；
- `POST /control/api/v1/modules/disable/dry-run` 只接受 exact If-Match、Origin、session+CSRF 与 canonical body，
  并拒绝 `Idempotency-Key`/confirmation header。它只计算 effect-free `MODULE_DISABLE` projection，返回
  process-local `DRY_RUN` receipt；无 domain mutation、无 receipt 持久化，且不调用既有停机 CLI
  Apply/Disable；
- 该 W6-1 历史原子当时的证据为 39 份 Markdown、44 个源码 package、44 个 production dependency-closure package。
  Store 当时是 32 tables / fingerprint
  `37258c1939308be83f21a109e59a19e32946734d1c53b1b262f876dc36e47dbd` / migration 57,652 bytes /
  SHA-256 `e4f1047eb527b65ab050d444062f3d286cc2ad87d56e60b1cd7f4e96df87652d`；
- 没有 Control mutation、durable receipt/table、SSE、UI、第二 Store/writer、后台 worker 或主动预热。
  `W6_2_CONTROLLED_MUTATIONS_AUDIT_NEXT` 仅是审计入口；首个 mutation 仍须单独批准 receipt 与第一条
  domain persisted fact 的同事务闭包。

### 8.5.18 W6-2 受控操作审计与 confirmation 纯合同验收（2026-08-13）

该历史原子的权威状态是
`W6_2_CONFIRMATION_CONTRACT_ACCEPTED_DEVELOPMENT_SLICE / W6_2_DURABLE_RECEIPT_SCHEMA_NEXT`；audit
历史收口为 `W6_2_CONTROLLED_MUTATIONS_AUDIT_ACCEPTED_DEVELOPMENT_SLICE /
W6_2_CONFIRMATION_CONTRACT_NEXT`。

- 六项 audit 只留下 TENANT/PROFILE、exact `context.provide/v1`、`OPTIONAL`、`DECLARATIVE`、
  trusted-instruction config 与 deny-all Authority 的 `MODULE_DISABLE` 首候选。Upgrade Review 因
  caller-owned artifact ingress，Learning 两项因 reservation/async Attempt/Task/Run，Upgrade Apply 与
  Module Apply 因 staging/install/activation/grant 长链 deferred；
- `ControlConfirmationStatementV1` 稳定绑定 principal/capability/MUTATE/operation/scope/key/input/
  `operation_evaluation_digest`/expected ref；`ControlOperationRequestV1.confirmation_digest` 必须由 exact
  Statement 重建，raw proof、Boot/session/auth revision/time 不进入稳定 digest；
- `ModuleDisableEvaluationV1` 以 64 KiB canonical ceiling 冻结完整候选投影并关闭 disposition/basis/
  Binding/catalog 矩阵；它只计算，不发布；
- process-local proof 为 32 random bytes、TTL 最多 2 分钟且不超过当前 session absolute expiry、全局 256、每 session 8。registry 只存
  domain-separated proof digest 与当前 authority/binding/expiry/state；proof/proof digest 不进入 Store、
  Backup、日志、URL 或错误。只有已证明零效果才可 release，可能效果或 commit ambiguity 后必须 consume；
- 该 W6-2 confirmation 历史原子当时的证据为 39 份 Markdown、45 个源码 package、44 个 production dependency-closure package；Store
  继续是上述 32 表 identity。无 confirmation HTTP endpoint、durable receipt row、mutation route、
  Schema/migration/Backup 变化、SSE/UI/worker，纯 SQLite 首片 `UNKNOWN` 不可达；
- 下一原子仅可设计一张 append-only receipt 表的 32→33 rebuild-only 候选：exact Request/input/evaluation/
  Receipt/pre-post basis/domain canonical，Request 8 KiB、input 8 KiB、evaluation 64 KiB、receipt 16 KiB、
  pre/post 各 8 KiB、domain 128 KiB、row canonical 总量 256 KiB；Tenant 1024/Store 8192，容量满不驱逐；
  unique identity 为 principal+scope+operation+key digest，receipt/domain/publication/pointer CAS 同一事务并
  纳入 Backup semantic closure。本切片尚未实现该表或任何写路由。

### 8.5.19 W6-2 durable receipt Schema 历史验收（2026-08-13）

该历史原子的验收状态是
`W6_2_DURABLE_RECEIPT_SCHEMA_ACCEPTED_DEVELOPMENT_SLICE /
W6_2_MODULE_DISABLE_MUTATION_WIRING_NEXT`。confirmation 历史 marker
`W6_2_CONFIRMATION_CONTRACT_ACCEPTED_DEVELOPMENT_SLICE / W6_2_DURABLE_RECEIPT_SCHEMA_NEXT`
继续保留。

- 该 W6-2 历史原子中的 Store 经首次发布前显式 rebuild 从 32 表变为 33 表，只新增 STRICT、append-only
  `control_operation_receipts`。当时 fingerprint 为
  `51be9081a815e6379380b93e745db03d33ca2f358962f85ace63bce2534dcc10`，migration 为 67,998 bytes /
  SHA-256 `8feea38beb505ab4f371afe32eecd877744062bd149d0d24982839c5450fc952`；
- `module-apply-plan/v1`、full `PublishedBasisRefV1` freeze/restore 与
  `module-disable-publication-receipt/v1` 已 strict canonical 冻结；candidate/publication/receipt IDs 使用各自
  独立 digest domain，domain receipt 排除 principal/request/key/confirmation/proof/session/time；
- Store resolver 只从 exact Request canonical+digest 派生 identity；同 request digest 返回 original exact
  receipt，同 identity 不同 digest typed conflict，absence typed not-found；
- 唯一公开 commit 只允许 neutral evaluator 重放确认的 `NO_CHANGE`，并在 `BEGIN IMMEDIATE` 内复验 current
  basis、quota、identity 后 append。它不能 publication、不能插入 `APPLIED`、不能创建或认领 domain effect；
- SQL 与 semantic verifier 关闭 `NO_CHANGE/APPLIED` 矩阵、same-Tenant Control/Catalog parents、
  Catalog→Control pair、identity/domain uniqueness、append-only、canonical caps 8/8/64/16/8/8/128 KiB、
  row 256 KiB、Tenant 1,024 与 Store 8,192 quota；
- Backup Create 的 source/staged snapshot、Verify、Restore staged/final 与 standalone semantic gate 都在 coherent
  snapshot 内验证现存 receipt，严格 Restore canonical、核对 immutable parents 并重放 neutral evaluator。
  单一 receipt 表没有外部 completeness anchor，因此本门禁不声称能发现整行删除；
- 在该历史原子中，`APPLIED` 只有 strict contract、DDL/restore/verifier 形状，没有公开 insert/publication
  seam；其下一原子必须由唯一 Store publication owner 在同一事务内提交 domain receipt、Control receipt、Control/Catalog 与
  pointer CAS；不得事后 backfill；
- 该历史原子的证据为 39 份 Markdown、47 个源码 package、46 个 production dependency-closure package、
  33 表 Store；当时没有 mutation route/handler、confirmation endpoint、APPLIED public insert、SSE/UI/worker 或第二
  Store/writer。

### 8.5.20 W6-2 MODULE_DISABLE mutation wiring 验收（2026-08-13）

该历史原子的收口状态是
`W6_2_MODULE_DISABLE_MUTATION_WIRING_ACCEPTED_DEVELOPMENT_SLICE /
W6_3_WEB_SHELL_READ_ONLY_OVERVIEW_NEXT`；receipt Schema 的
`W6_2_MODULE_DISABLE_MUTATION_WIRING_NEXT` 只保留为历史入口。

- 生产 composition 只在显式 `--enable-control` 分支用已经打开的同一个 Current Store 构造 confirmation
  registry、coordinator 与同一 HTTP adapter；没有第二 enable flag、Store、writer 或 recovery owner。
  default-off E2E 证明 registry 未初始化且 mutation surface 不存在；
- 只注册 `POST /control/api/v1/modules/disable/confirmation` 与
  `POST /control/api/v1/modules/disable/mutate`，并只接受 TENANT/PROFILE exact
  `context.provide/v1`、existing Binding `OPTIONAL`、`DECLARATIVE static/v1`、trusted-instruction config、
  deny-all Authority；其他 scope/port/policy/operation 都失败关闭；
- confirmation 保持 effect-free。mutation lookup 在 proof/current-basis 前：same request 返回原 stable receipt，
  identity conflict 不触发 proof/evaluation，miss 才 claim proof、服务端重建 evaluation 并在 commit 前复验
  current authorization/basis；proof 在确定零效果时才 release，commit 调用或 ambiguity 后 consume；
- `NO_CHANGE` 与 `APPLIED` durable receipt 均验收；APPLIED 的 domain receipt、Control/Catalog publication 与
  pointer CAS 由唯一 Store owner 在同一 `BEGIN IMMEDIATE` transaction 提交。E2E 覆盖 pointer exact +1、
  proofless exact retry、重启/新 session retry、NO_CHANGE、identity conflict、并发不同 key 最多一个 APPLIED、
  invalid/used proof 与零 Provider/Gateway/artifact 效果；纯 SQLite `UNKNOWN` 为 0；
- Backup APPLIED bundle Create→Verify→Restore 后 exact receipt 不变，并拒绝 parent/replay/domain tamper。
  verifier 无 external completeness anchor，不声明能发现任意整行 receipt 删除。Store 的 forced receipt-insert
  failure 测试证明 publication、domain/Control receipt 与 pointer CAS 全事务 rollback；已存在 candidate
  publication 却无 receipt 失败关闭且不得 backfill；
- 该历史原子当时为 39 份 Markdown、48 个源码 package、48 个 production dependency-closure package；
  Store 为 33 表，fingerprint/migration 为
  `51be9081a815e6379380b93e745db03d33ca2f358962f85ace63bce2534dcc10`、67,998 bytes /
  `8feea38beb505ab4f371afe32eecd877744062bd149d0d24982839c5450fc952`。没有真实 API 或费用，没有其他
  mutation、SSE/UI/worker、第二 Store/writer、自动升级或预热。

### 8.5.21 W6-3 Web Shell / read-only Overview 验收（2026-08-13）

该切片的历史收口状态是
`W6_3_WEB_SHELL_READ_ONLY_OVERVIEW_ACCEPTED_DEVELOPMENT_SLICE /
W6_4_MODULES_CONFIGURATION_UI_NEXT`；`W6_3_WEB_SHELL_READ_ONLY_OVERVIEW_NEXT` 只保留为 W6-2 的历史入口。

- production composition 只在显式 `--enable-control` 分支用同一 Current Store 构造 Overview service 与
  exact embedded static resolver；default-off 不初始化这两个依赖且相关 routes 全部不可达；
- `GET /control/api/v1/overview` 每次要求 exact Origin/session/session-bound CSRF 与 TENANT/WORKSPACE Permit，
  在单一只读事务中有界投影 Workspaces、Runs、UNKNOWN、Learning、Module Candidates 与 Usage；读取后到编码前
  再验证授权、scope ownership、状态机、排序、caps、current basis、section/projection digests 与 strong ETag；
- React/TypeScript Web Shell 只从 server-authorized scopes/current Workspace refs 构造 selector，TanStack Query
  key 含完整 scope；rotating resume credential 只进入 tab-scoped `sessionStorage`，CSRF 仅驻内存。搜索、detail
  drawer、deep link 均只消费已授权 Overview，并明确区分 loading/empty/error/stale/permission-denied；
- exact static paths、GET/HEAD、conditional ETag、安全 headers、路径穿越/source-map 拒绝、default-off 和完整
  production listener E2E 已闭合；没有真实 API 调用、费用或外部效果；
- 该 W6-3 历史切片 Store 为 41 tables / 23 indexes / 56 triggers，fingerprint
  `87d63a2e468ee27ee2c0e1918a92e580532825bde85dbb872b9c795bf577f2c1`，migration 143,588 bytes /
  SHA-256 `5bd9743e988b82750f785c015f07c11fbcf6025da70148405aec11106b4efb22`。新增 8 张 observation 表只由既有
  lifecycle 同事务维护；schema/full semantic、Backup Create/Verify/Restore 与 closed-file tamper 门禁已通过；
- W6-3 当时无业务 mutation UI、SSE、worker、durable client cache、第二 Store/writer、Provider/Gateway dispatch、
  自动升级或预热；`W6_4_MODULES_CONFIGURATION_UI_NEXT` 是其历史下一入口。

### 8.5.22 W6-4 Modules configuration UI 验收（2026-08-16）

该历史切片状态是
`W6_4_MODULES_CONFIGURATION_UI_ACCEPTED_DEVELOPMENT_SLICE /
W6_5_SERVER_OWNED_MODULE_ARTIFACT_INGRESS_NEXT`。

- 只复用既有 Modules list/detail 与 `MODULE_DISABLE` dry-run/confirmation/mutate；没有新 operation、route、
  Application Service、Store seam、Schema、writer 或 publication owner；
- TypeScript transport 对 UTF-8 scope/detail carrier、schema、nested digest、strong ETag、cursor、basis、evaluation、
  statement、receipt 与 exact retry 全部严格复验；Go HTTP/Application/contract 三层保持同一 ID 与 admission 合同；
- UI 只在 TENANT/PROFILE OPTIONAL `context.provide/v1` 窄候选上显示 Review，固定执行
  `WOULD_APPLY → short-lived confirmation → inert summary → second explicit click → MUTATE`；proof 首次发送即清除；
- raw proof、idempotency key 与 CSRF 不进入 URL/hash、DOM、log/error、query cache 或 durable storage；不可判定
  结果只允许无 proof exact retry，可信 APPLIED/NO_CHANGE 只清状态并 verified refetch，禁止 optimistic publication；
- deterministic embedded build、ControlWeb/License/Go 定向矩阵、Job/Control/release-gate selftests 与真实 loopback
  browser handoff → r2/r3 mutation/refetch 均闭合；目标 module 从重新读取的列表中消失，console error 为零；
- 该 W6-4 历史切片 Store identity 保持 41 tables / 23 indexes / 56 triggers；无 artifact ingress、其他 mutation、SSE、worker、
  第二 Store/writer、远程 Control、自动升级或生产成熟度提升。

`W6_5_SERVER_OWNED_MODULE_ARTIFACT_INGRESS_NEXT` 是 W6-4 收口时的历史下一入口；W6-4 不授权
path/URL/signature ingress、staging/install/activation 或 grant。

### 8.5.23 W6-5 server-owned module artifact ingress 验收（2026-08-17）

当前权威状态是
`W6_5_SERVER_OWNED_MODULE_ARTIFACT_INGRESS_ACCEPTED_DEVELOPMENT_SLICE /
W6_6_SERVER_OWNED_MODULE_UPGRADE_REVIEW_NEXT`。

- 唯一新入口是默认关闭、可信本地 Operator CLI `module-artifact-ingress`；必须显式
  `--enable-module-artifact-ingress`。关闭或参数失败路径不打开 Store、不读 Source、不发布 artifact；
- `source-root` / `artifact-root` 是瞬时可信 CLI 输入；caller 只选择 exact Source/Snapshot/Module/
  ArtifactDigest，不提供 package path、URL、signature、HTTP upload 或 UI 输入。Store 重建 current Snapshot
  entry，并只允许 unsigned `LOCAL_DIRECTORY + DENY`；
- 文件系统先完成 conformance/digest/size/file-count 复验，以 hidden stage、sync、digest-addressed
  no-replace 方式 durable publish；Store 随后在一个 `BEGIN IMMEDIATE` 重验 basis并提交 immutable
  `module_artifacts` 与 append-only `module_artifact_admissions`；
- Source/artifact/Backup tree 必须以 held-parent-handle 相对遍历，拒绝 link/reparse、hardlink、跨设备以及
  identity/change/namespace 漂移；artifact root、children 与全部 ancestors 必须可信私有。跨进程 root lease
  覆盖 crash-stage recovery、publish、sync 与 root 级 256 trees / 512 MiB covered content / 32,768 paths /
  16,384 files / 16 MiB path-name bytes 硬预算；该预算不声称等于实际 allocation blocks；
- 新 ingress 的同 digest 目标缺失时必须以目录 `0700`、文件 `0600` 发布。既有同 digest 目标只有在完整
  bytes/mode 复验后才可复用，且只接受两种 root-global 闭包：全目录 `0700` / 全文件 `0600`；或全目录
  `0700`，仅 canonical `LOCAL_PROCESS + mcp-stdio/2025-11-25` descriptor 精确绑定的唯一 executable 文件
  为 `0700`，其余文件 `0600`。两者均拒绝 special/setid/sticky 与 group/world 权限。跨 Store 共享 artifact
  root 的模式判定不得依赖任一单独 Store 的 Installation；后一种只表示既有物理兼容，不授予当前 Store
  Installation/Activation/execution authority。这里的 inert 是无 Store authority 且本切片不执行，不等同于
  没有 OS executable bit；
- commit 结果不明时保留无 authority 的有界 inert bytes；只有 durable exact Admission 可在 head 前进后恢复
  原 selector，未提交 stale selector不能采用，后来 current 合格 selector仍须完整复验；
- Artifact/Admission 保持 inert；没有 Installation、Activation、Binding、grant、Review/Decision、Apply、
  Host、Run/Attempt 或 execution，也不新增 Control operation/route、SSE、worker 或第二 writer；
- Backup artifact closure 是 Installation 与 ingress 的 digest 去重并集；Source 删除、零 Installation 的
  round-trip 仍完全离线，同一 digest installed+ingressed 时 bundle 只含一份 bytes；临时 DB/bundle/restore tree
  只在可信私有 staging 中创建，并同步 publish/cleanup 涉及的 parents；
- 当前 Store 为 43 tables / 25 explicit indexes / 64 triggers，fingerprint
  `47981b9bf147ec81c6e98382f281db0d5395187a1f090f7ce183c116fba82a0d`，migration 150,301 bytes /
  SHA-256 `6def433a59fa8f4876894572f1610cae499abc5b389ab5930ea697055419ca86`。

`W6_6_SERVER_OWNED_MODULE_UPGRADE_REVIEW_NEXT` 只允许下一原子设计消费 server-owned inert Artifact；
W6-5 不创建 Upgrade Review/Decision，也不 Install、Activate、Bind、grant、Apply 或 execute。

### 8.6 当前 W1 完成与 W3 集中验收更新（2026-08-09）

本节是对前述历史切片的只追加当前状态，不修改旧 hash、seal、日志或当时的成熟度判断：

```text
W1_COMPLETE_REAL_DEEPSEEK_50 = ACCEPTED
W3_M0_M1_M2 = ACCEPTED_DEVELOPMENT_SLICE
W3_K0_K1_K2A_K2B_K3 = ACCEPTED_DEVELOPMENT_SLICE
W3_RAG_MEMORY_WORK_PACKAGE = COMPLETE_ACCEPTED_DEVELOPMENT_SLICE
W4_LEARNING = PLANNED_AWAITING_REVIEW
PRODUCTION_DEPLOYMENT = NOT_APPROVED
PUBLIC_BETA = NOT_APPROVED
```

W1 的真实 50 轮证据独立于本轮单次 smoke：50/50 个首次 Model Attempt 成功，Conversation
最终 revision 为 50；turn 25 的 backup→verify→restore 与 turn 25/50 在移除运行时 Secret 后的
exact retry 均只读同一 Attempt，没有增加模型调用。权威 Usage 合计 input `41,642`、cached
input `37,632`、uncached input `4,010`、output `710`，reasoning token 为 50/50 UNKNOWN；token
加权缓存命中率为 `90.3702992171%`，请求级命中为 `46/50 = 92%`，冻结估算费用为
`0.00618264 CNY`。该 W1 验收不启用 RAG、Memory、Skill、MCP、Action、Channel、Persona 或 Team。
`TestW1PureChatUnifiedZeroOptionalModulesAcceptanceV1` 还在这些 Provider 已安装/激活/Catalog-visible
但未被选择的更强夹具下，证明仅解析冻结 Model，动态 Context/Action 与所有可选状态均零增量。

W3 集中验收闭合以下同一纵链：

- `W3-M0` 让 Conversation 与动态 Knowledge/Memory 在 Store 新 Attempt 上恢复完整前驱 pair，
  并用唯一 Context Compiler 精确复编译；普通非 Conversation History 路径保持原语义；
- `W3-M1` 将既有 Memory 接入统一 Module Apply/Dry-run/Disable；不存在 Head 时只创建显式空
  Genesis，已有 Head 逐字节保留，Disable 只影响未来 Binding；
- `W3-K0/K1` 以默认关闭、旧 canonical bytes 不变的可选 routing 合同和无 I/O 纯决策函数完成
  标签路由；低置信、并列或证据不足安全回退 fresh，分类不授予权限；
- `W3-K2A/K2B` 用独立、Authority-only 的 `knowledge_shortcuts` 表达 `NOT_SELECTED`，并由唯一
  CoreLoop 在任何 Provider 调用前完成所有 Binding 的 config/protocol/Authority/scope/limit
  预检；shortcut 不伪装成 zero-hit retrieval；
- `W3-K3` 用独立 `knowledge_reuses` 实现同 Conversation 最新 exact-question 成功候选复用。
  Source revision、权限、TTL、Memory head、counter proof 或证据任一变化即 fresh，不穿透更老
  候选；合法 reuse 时 Knowledge Provider 调用为 0；
- `W3-M2` 只复用直接成功前驱现有 `ContextCompilationV1.Summary` 所覆盖的连续最旧原始
  USER/ASSISTANT pair，不允许 summary-of-summary。`<85%` 与 `>=100%` 忽略候选；
  `>=85% 且 <100%` 仅在候选范围与本轮最小摘要范围完全一致时复用，并重新计算本轮全部 token；
  满水位继续只 Drop 恢复到目标水位所需的最小完整旧前缀。Memory `TASK_SUMMARY` 仍只表示
  Agent 基础记忆，不能冒充 Conversation Summary；
- Backup 的三轮 Conversation create→verify→restore→reopen→continue 长链保持 source/current
  Compilation 与 `MODEL_REQUEST` canonical bytes，并证明备份/恢复不调用 Provider。新增确定性门禁
  从冻结 Run、History、Evidence、Policy、Profile、Memory snapshot 与 Compilation inputs 重编译
  Conversation compiler-owned model-1，并逐字节比较请求与 Compilation；协调重签后的重复 Summary
  或保留已摘要 raw prefix 均被拒绝。

最终 Windows `go test -count=1 ./...` 全仓通过（195.8s），`go vet ./...` 通过，`go mod verify`
返回 `all modules verified`。随后使用一次性 Test-0808 Secret 进行一次独立真实受控 DeepSeek
Pure Chat 回归：恰好一个 Provider Attempt，终态 `MODEL_SUCCEEDED`；权威 Usage 为 input `14`、
cached input `0`、uncached input `14`、output `5`，reasoning token 为 UNKNOWN，冻结价格估算为
`0.000024 CNY`，Provider reported 与 reconciled cost 保持 UNKNOWN。该一次 smoke 只证明 M2/P1
修改未破坏真实 Model/Usage 链，不替代上述 W1 真实 50 轮，也不证明远程 RAG/Memory。

Secret 未进入仓库、命令参数、Store、备份、日志或报告，模型正文未写入验收文档。测试结束后
临时环境、exe、SQLite、artifact、日志、Go cache、临时测试根目录与 smoke 目录均已删除；
仓库扫描为零 Key 形态、零测试二进制/数据库/缓存，且无残留 `go/freeagent` 进程。

非阻塞 P2：当前确定性 Backup 重编译只覆盖 Summary 唯一可能出现的 Conversation Attempt；把
相同 exact recompile 扩展到非 Conversation Pure Chat/Action/RAG/Memory/Composite 的全历史
compiler-owned Requests 是独立 Backup hardening 切片。在该 P2 完成前，不得声称“所有历史
`MODEL_REQUEST` 均由 Backup 重编译”；这不影响本轮 Conversation Summary 精确闭包或 W3 六项
验收条件。

本轮没有新增 Schema、表、ContentKind、Port、Runtime、Store、Loop、Gateway、缓存服务、后台
预热或外部效果账本；没有把 RAG、Memory、Role、Persona、Skill、MCP、Channel 或 Team 固化进
Core。Agent 继续轻量，领域知识继续位于共享授权 RAG，Memory 与 Summary 继续可选且语义分离，
Pure Chat 继续零可选模块访问。W4 Learning 仍为 `planned`，进入前必须重新复核最新计划。

### 8.7 W4-L1A Knowledge Proposal Store 增量验收（2026-08-09）

本节只验收 W4-L1A 的最小 Knowledge Proposal 持久化边界，不把它提升为完整 Learning：

```text
W4_L0_PROPOSAL_REVIEW_CONTRACT = ACCEPTED
W4_L1A_KNOWLEDGE_PROPOSAL_STORE = ACCEPTED_DEVELOPMENT_SLICE
W4_COMPLETE_LEARNING = PLANNED
REVIEWER_MODEL_ATTEMPT = NOT_IMPLEMENTED
IMMUTABLE_VERSION_AND_OPERATOR_PUBLICATION = NOT_IMPLEMENTED
SKILL_DRAFT_STORE = NOT_IMPLEMENTED
DEFAULT_24H_CYCLE_AND_COMPENSATION_SCAN = NOT_IMPLEMENTED
PRODUCTION_DEPLOYMENT = NOT_APPROVED
PUBLIC_BETA = NOT_APPROVED
```

以上 `NOT_IMPLEMENTED` 项是 W4-L1A 验收时的历史事实；其后的 W4-L2 当时状态由 §8.9
记录；其后 W4-L4/W4 complete 状态只由 §8.11 表达，W5-X1 当时状态由 §8.12 表达，当前
W5-F1/W5 complete 状态只由 §8.13 表达，均不能回填或扩大 L1A 裁决。

W4-L1A 在唯一 Current Store 中加入且只加入第 22 张 `learning_proposals` 表。本切片当时 ordinary
table count 为 `22`，schema fingerprint 为
`5afa72fce368a1997f7f37422ed8dcdf4fa2ad854a2534909463d1981de13624`，migration 为
`34,580` bytes / SHA-256 `dc48db067d107748895bef613886a366d07e74e2acdb32686057a4d32fde4157`。
此前 19/20/21 表及其 hash、seal 仍只证明各自历史树，不因本节而被重写。

本切片完成范围为：

- 只接受 L0 canonical 合同中的 Knowledge Proposal；Proposal、exact Draft、source/content/draft
  fingerprint、target ref 和 proposer lineage 逐字节或按冻结算法持久化；
- 提交事务从同 Tenant 的终态成功 proposer Run 反查 exact Manifest、Member、Model Attempt 与
  MODEL_RESULT；失败、UNKNOWN、非终态、跨 Tenant/Workspace/Agent 或不完整 lineage 均在写入前
  fail-closed；
- 在 Tenant 内分别冻结 source、content 和 target 三条唯一轴。相同 Proposal 的 exact retry 返回
  原记录；任一轴独立碰撞、同 target 版本重复或并发竞争不会创建第二条候选；
- 状态域当前只有 `SUBMITTED`，revision 固定为 `0`。Proposal 中的 wildcard scope 仍只是后续
  Authority 请求，不授予审核、安装、激活、发布、扩权或外部效果权限；
- 完整 backup/verify/restore 复用同一 semantic gate，重新验证 canonical bytes、全部 fingerprint、
  target、状态以及 Run/Member/Attempt/Result lineage；校验过程不调用模型、模块、网络、Secret 或
  外部效果。

聚焦验证已覆盖 exact retry、三轴重复、并发提交、Tenant 隔离、无效/失败/UNKNOWN lineage、
wildcard 不扩权、关闭 Store 拒绝、完整备份往返及篡改拒绝。该切片没有后台 Worker、自动提交、
主动预热或第二 Store/Loop/Gateway；也没有 Reviewer Model Attempt、Review 终态、不可变 Version、
Skill Draft Store、Operator 发布衔接、补偿扫描或默认 24 小时周期。`knowledge.learning-cycle`
因此继续为 `planned`，不得因 Proposal 已可保存而宣称 Agent 已能自主审核或更新知识库。

### 8.8 W4-L1B 静态 Skill Proposal Store 增量验收（2026-08-09）

本节只验收静态 Skill Draft 复用 L1A Proposal Store 的最小边界，不把它提升为完整 Learning：

```text
W4_L1B_STATIC_SKILL_PROPOSAL_STORE = ACCEPTED_DEVELOPMENT_SLICE
ORDINARY_TABLES = 22
SECOND_PROPOSAL_TABLE = ABSENT
REVIEWER_MODEL_ATTEMPT = NOT_IMPLEMENTED
IMMUTABLE_VERSION_AND_OPERATOR_PUBLICATION = NOT_IMPLEMENTED
DEFAULT_24H_CYCLE_AND_COMPENSATION_SCAN = NOT_IMPLEMENTED
PRODUCTION_DEPLOYMENT = NOT_APPROVED
PUBLIC_BETA = NOT_APPROVED
```

以上 `REVIEWER_MODEL_ATTEMPT = NOT_IMPLEMENTED` 是 W4-L1B 验收时的历史事实；其后的 W4-L2
当时状态由 §8.9 记录；其后 W4-L4/W4 complete 状态只由 §8.11 表达，W5-X1 当时状态由
§8.12 表达，W5-F1/W5 complete 后续状态由 §8.13 表达，W2-R1 历史状态由 §8.5.7 表达，当前
W2-R2 状态由 §8.5.8 表达，
均不能回填或扩大 L1B 裁决。

L1B 只把第 22 张 `learning_proposals` 的 kind 约束扩为 `KNOWLEDGE | SKILL`，表数不变。
本切片当时 schema fingerprint 为
`18722b0c8a18e9f6563e65cf30d0ebc50b723aa6f6465aa4b95d83ba001bc422`，migration 为
`34,592` bytes / SHA-256
`75b7fe488932e0f562c84073a659c9803ffcd4bfdb4efa500b19720c7648c47f`。§8.7 的 L1A 数值继续只
表示对应历史树，不回写为当前值。

本切片完成范围为：

- Skill Draft 只接受既有 exact `static-context/v1` canonical，不增加 Skill wire、Role 推断、
  Runtime、Port 或 ContentKind；完整 Draft canonical 超过 1 MiB 会在复制和事务前明确拒绝；
- Knowledge 与 Skill 使用两个窄入口，但共享同一 Store Admission、终态成功 proposer lineage、
  ProposalID、exact retry、Tenant+kind 的 source/content/target 三轴去重和 `SUBMITTED/0` 状态；
- 同正文换来源、target 或 version 仍由 Skill content fingerprint 拒绝；8 路并发 exact retry 只有
  一个创建者；Knowledge/Skill 跨 kind 去重隔离按当前正式规格冻结；
- 原始 SourceEvidence、URL、路径、revision material 与 Secret 不落库。Skill 提交不会新增
  STATIC_CONTEXT、Installation、Activation、Binding、Usage、Memory、Channel 或 Catalog 事实；
- 混合 Knowledge+Skill 的 backup→verify→restore→reopen 保持 Proposal、Draft、proposer
  Attempt、状态、revision 和双时间戳；kind、Draft 与 target 投影篡改均由统一语义门禁拒绝；
- Schema 门禁冻结 22 表、精确 kind CHECK、三组 Tenant+kind UNIQUE、当前 fingerprint 与迁移摘要。
- 最终 Windows `go test -count=1 ./...` 全仓通过（189.4s），`go vet ./...` 与
  `go mod verify` 通过；Docs（38 份 Markdown）、License（35 个 Go dependency/55 个
  distributed asset）、Branding 和 PublicTree 通过。仓库凭据形态、临时 exe/SQLite/日志/缓存/
  报告及 `go/freeagent` 残留进程均为 0。

L1B 没有新增第二 Runtime、Store、Loop、Gateway、Catalog pointer、Worker、Review 状态机或外部
效果账本，也没有让 Draft 进入 Context Compiler。它保持 Pure Chat 零可选模块访问、轻量 Agent、
共享授权 RAG、Workspace 隔离和 UNKNOWN 禁止语义重放。跨 kind/跨 Tenant 的 ModuleRef 与既有
Installation 冲突只允许在 W4-L3 物化和 Operator Apply 前 fail-closed，L1B 不提前保留发布权。
后续 L2 的审核实现不能回写该历史结论，也不能借用 L1B accepted 状态。

### 8.9 W4-L2 Learning Review 增量验收（2026-08-09）

本节只验收一次有界 Learning Review 纵链，不把它提升为完整 Learning：

```text
W4_L2_LEARNING_REVIEW = ACCEPTED_DEVELOPMENT_SLICE
W4_L3 = NEXT
ORDINARY_TABLES = 22
SECOND_REVIEW_TABLE = ABSENT
APPLICATION_SERVICE = PRESENT_BUT_NOT_PUBLIC_CLI_OR_HTTP
LIVE_DEEPSEEK_RUNNER = PRESENT_OPT_IN
ACCEPTANCE = RECORDED
IMMUTABLE_VERSION_AND_OPERATOR_PUBLICATION = NOT_IMPLEMENTED
DEFAULT_24H_CYCLE_AND_COMPENSATION_SCAN = NOT_IMPLEMENTED
PRODUCTION_DEPLOYMENT = NOT_APPROVED
PUBLIC_BETA = NOT_APPROVED
```

本 L3 历史切片的 22 表 schema fingerprint 为
`feed0dd35f98e4f177934092d93a1b3d7f834f0fae3b076e722aa6bcd4722484`，migration 为
`35,743` bytes / SHA-256
`715c2904f417ecf8732f554eda5c47af1a6c45eecbf8e21f408f51badb70519c`。第 22 张
`learning_proposals` 增加 nullable unique `review_run_id`/`reviewer_attempt_id`，状态形状固定为：

```text
SUBMITTED/0
  -> REVIEW_PENDING/1
  -> APPROVED/2 | REJECTED/2 | REVIEW_FAILED/2 | REVIEW_UNKNOWN/2
REVIEW_UNKNOWN/2 + same-Attempt reconciliation evidence
  -> APPROVED/3 | REJECTED/3 | REVIEW_FAILED/3
```

Reviewer Admission 原子发布一个与 proposer 同 Tenant、同 exact Workspace，但 Run/Member/
Agent logical ID/Profile logical ID 独立的普通 model-only Run，并同时推进
`SUBMITTED/0 → REVIEW_PENDING/1`。`learning-review-request/v1` 完整嵌入 Proposal/Draft，审核
Draft 上限 64 KiB，模型输出上限 1024 tokens；不得截断或摘要。唯一 Universal Loop、普通
`model_dispatch_attempts`、`model_usage`、`MODEL_RESULT` 与 `MODEL_UNKNOWN` 继续是唯一执行和
对账路径；Finalize 只从绑定 Run 的唯一 Attempt 派生 Proposal 终态，不接受调用方自报 Verdict。

`internal/localchat.LearningReviewService` 是显式构造的可选 application workflow；普通
ChatService 不会自动启用审核。仓库另提供
[`scripts/Invoke-W4L2LiveLearningReview.ps1`](../scripts/Invoke-W4L2LiveLearningReview.ps1)，
它只运行 opt-in `TestW4L2LiveDeepSeekLearningReview`：临时初始化 production composition，
执行真实 proposer 与独立 Reviewer，要求 Reviewer Attempt 为 DeepSeek `SUCCEEDED`、Proposal
终态为 `APPROVED` 或 `REJECTED`、Usage 有 input/output token，并验证 exact retry 零重放。
凭据只从固定 `FREEAGENT_DEEPSEEK_API_KEY` 进程环境读取，临时 Store/artifact 由测试目录清理，
输出仅为脱敏汇总。

2026-08-09 的真实 `TestW4L2LiveDeepSeekLearningReview` PASS（4.08s）：Proposal 为
`APPROVED` revision 2，Reviewer Attempt 为 `SUCCEEDED`；Usage 为 `PROVIDER_REPORTED`，input
1151、cached 0、uncached 1151、output 227、reasoning UNKNOWN，估算费用为 0.001605 CNY。
exact retry 返回原 Run/Attempt，未发起语义重放。

最终 Windows `go test -count=1 ./...` 全仓通过（168.4s），`go vet ./...` 与 `go mod verify`
通过；Docs（38 份 Markdown）、License（35 个 Go dependency/55 个 distributed asset）、
Branding 和 PublicTree 通过。完整 Backup create→verify→restore→reopen 覆盖全部审核状态，
Reviewer Run/Attempt/Result/Verdict/state/revision 篡改拒绝通过。

W4-L2 不新增 Review 表、Runtime、Loop、Gateway、Port、ContentKind、Worker、Queue、Usage
账本或外部效果账本；也不发布、安装、激活、绑定、扩权或启动周期任务。`APPROVED` 只是审核
结论，不是 Version、Catalog 或权限 grant。

### 8.10 W4-L3 不可变 Version 与 Operator 交接增量验收（2026-08-09）

```text
W4_L3_LEARNING_VERSION = ACCEPTED_DEVELOPMENT_SLICE
W4_L4 = NEXT
ORDINARY_TABLES = 22
SECOND_VERSION_TABLE = ABSENT
MATERIALIZATION_CLI = PRESENT_EXPLICIT_OFFLINE
AUTOMATIC_INSTALL_ACTIVATE_BIND = ABSENT
DEFAULT_24H_CYCLE_AND_COMPENSATION_SCAN = NOT_IMPLEMENTED
PRODUCTION_DEPLOYMENT = NOT_APPROVED
PUBLIC_BETA = NOT_APPROVED
```

本 L2 历史切片的 22 表 schema fingerprint 为
`a744ea62d0ead80b5eb0669409f0ab4cd0d297b662e67181b0a88f760b2c5024`，migration 为
`37,810` bytes / SHA-256
`025acc044f53a71c674d5963c171806e87095911025a129f65b4be33a0601070`。第 22 张
`learning_proposals` 只增加七项 all-or-none Version/artifact 投影；严格 scanner 将 `0/7` 视为
未物化、`7/7` 视为待完整重建，任何部分形状都失败关闭。Version 只接受 `APPROVED/2` 或同一
UNKNOWN Attempt 对账后的 `APPROVED/3`，精确绑定 Proposal、Review、Tenant/kind、三类 fingerprint
和全局 ModuleRef，不携带 Profile、Binding、Config、Trust、Authority、Secret 或发布授权。
`materialized_at` 不早于 Proposal 审核投影时间，exact retry 保留原 Version 与原时间。

Knowledge 与 Skill 分别确定性生成 `module.yaml + content/source.json` 和
`module.yaml + content/context.json`；ArtifactDigest/size 只覆盖 exact Manifest 与 Draft。
ModuleRef 在 Tenant 与 kind 之间保持全局唯一。Materialize 检查既有 Installation，InstallModule
也在任何 content/write 前检查既有 Version；两种顺序与并发下都只有 exact Manifest 和
ArtifactDigest 可共存，冲突零写。

显式 `learning-materialize` 命令在同一 Store 中冻结或读取 Version，关闭 writer 后才向与数据库/
活动 artifact root 分离的 Operator 目录导出。路径在物化前后重新解析并复验隔离；existing/concurrent
exact retry、只读文件、no-replace、安全 symlink/hardlink/特殊/超限/extra-file 拒绝与 staging
清理均已覆盖。生成包通过现有 `module-verify` 与 `module-dry-run`；前后数据库字节、sidecar 与活动
artifact root 一致，证明未改变 Installation、Activation、Control、Catalog、Run、Attempt、Usage、
Memory 或 Channel。

完整 Backup create→verify→restore→reopen 覆盖 Knowledge 与 Skill 未安装 Version：全字段、
canonical bytes 和 `materialized_at` 精确保留，不要求活动 artifact root 存在该 digest。恢复后由
真实产品 `learning-materialize` 重新导出相同 Manifest/Payload/entrypoint/digest/size；第一次只
创建 handoff、不重复创建 Version，第二次为零写 exact retry。若 Operator 以后显式 Apply，安装包
仍进入既有 Installation/artifact backup 闭包。

`TestW4ApprovedLearningMaterializedSkillApplyIsConsumedOnlyByNewRun` 补齐后续显式消费闭包：真实
proposer lineage → 独立 Reviewer APPROVE → inert materialize → stale Catalog CAS 零部分发布 →
Operator exact Apply → 新 Run 实际消费审批后的静态 Skill。旧 Run bytes/digest 保持不变；新 Run
冻结 exact Version/Artifact/Catalog，Binding 为 REQUIRED + deny-all Authority，Trust/Adapter 由
本地策略授予。该证据不改变自动 Review/Materialize/Apply 仍为 absent 的边界。

本切片没有模型、网络、Secret、Gateway 或不可逆外部效果，未使用真实 API Key，也不存在 L3
语义重放。它不生成 `module-apply-plan/v1`：Profile、Instance、exact Port、binding index、
`expected_runtime_request`、Config、Authority、FailurePolicy 与 expected pointer revision 仍由
Operator 现场决定。该切片保持唯一 Runtime/Store/
Loop/Gateway、Pure Chat 零可选模块访问、轻量 Agent 与共享授权知识设计；W4-L4 周期、报告和补偿
扫描仍未实现。

### 8.11 W4-L4 Learning Cycle 与 W4 完成验收（2026-08-09）

```text
W4_L4_LEARNING_CYCLE = ACCEPTED_DEVELOPMENT_SLICE
W4_COMPLETE = ACCEPTED_DEVELOPMENT_SLICE
W5 = NEXT
ORDINARY_TABLES = 24
EXPLICIT_TICK = PRESENT
BACKGROUND_WORKER = ABSENT
STORE_ONLY_RECONCILE = PRESENT
READ_ONLY_REPORT = PRESENT
AUTOMATIC_REVIEW_MATERIALIZE_APPLY = ABSENT
PRODUCTION_DEPLOYMENT = NOT_APPROVED
PUBLIC_BETA = NOT_APPROVED
```

当前物理 identity 已从 W4-L3 历史 22 表推进为：

```text
schema fingerprint = 9941c957b0e6a1a1e1770b38cd06605025d12df30067d1271fe8f7b30f2f0518
migration bytes = 44138
migration SHA-256 = 4cf260d368fb7b69e0a86dad99ff593bea216e9d0da341f607d000999e076be4
```

W4-L3 的 `22` 表、fingerprint、`37,810` bytes 与 migration SHA 继续只作 §8.10 历史证据。
L4 只增加 `learning_cycle_schedules` 与 `learning_cycle_tasks`：Schedule canonical 创建后不可变，
默认关闭，未指定周期时规范化为 24 小时；enable/disable 使用 exact revision CAS。Tick 必须显式
提供 exact Tenant/Schedule 和微秒精度 UTC observation；Pure Chat、Store open、Backup/Restore、
启动和关停恢复均不会自动扫描或执行。

到期窗口由 `(tenant_id,schedule_id,scheduled_for)` 和确定性 TaskID 冻结；错过多个周期只合并为
最新窗口，同一 Schedule 同时最多一个 `PENDING|RUN_ADMITTED`。普通 proposer Run 为 standalone、
single-member、model-only，只允许一个 required `model.generate/v1`；没有 Action、MCP、Channel、
动态 RAG/Memory 或第二执行 lease。Task 状态、revision 与 Run/Attempt/Result/Proposal 引用由 Store
派生；`UNKNOWN/2` 只能对账同一 Attempt，禁止新 Run/Attempt、换 Provider 或语义重放。

产品 CLI 已闭合：

- `learning-cycle-schedule-create/get/enable/disable` 只打开 Current Store；
- `learning-cycle-tick` 是唯一构造 production composition 的 Learning Cycle 命令；
- `learning-cycle-reconcile` 只收口既有 `PENDING|RUN_ADMITTED|UNKNOWN` Store 事实，零
  Provider/Loop/Gateway/Secret/网络调用；
- `learning-cycle-report` 生成指定 Schedule 和半开窗 `[start,end)` 的确定性 canonical 投影，
  不持久化、不写文件、不发 Channel、不调用模型；
- reconcile/report 的 1024 条上限会探测并拒绝第 1025 条，不静默截断；输出只含脱敏引用与可用的
  同 Attempt Usage，未知 token/费用保持 JSON `null`。

完整 Backup create→verify→restore→reopen 已覆盖 Schedule canonical/digest、enabled/revision/水位、
`PENDING`、`RUN_ADMITTED`、全部终态与 UNKNOWN Task，以及 exact Run/Attempt/Usage/Result/Proposal
closure。Restore/Open 不隐式 Tick；协调篡改、off-grid 水位、非法 state/ref、第二 open Task 或
孤立执行事实均失败关闭。report 是临时只读投影，不进入 Store 或 bundle。

首次真实 DeepSeek canary 的 Provider Attempt 成功，但输出不满足严格五字段
`learning-cycle-result/v1`，因此正确闭合为 `INVALID_RESULT`；该结果保留为合同门禁发现。固定
instruction skeleton 与纯陈述 objective 后进行唯一一次修正重跑，Task 为 `PROPOSAL_SUBMITTED`，
Usage 为 `PROVIDER_REPORTED`：input 614、output 90、冻结估算费用 `0.000794 CNY`；全链恰好一个
Run、一个 Model Attempt，同窗口 retry 零语义重放。测试凭据、模型正文、临时 Store/artifact、
日志和报告均未进入仓库。

定向门禁覆盖四个纯合同 wire、默认关闭/24h、到期边界、时钟回拨/前跳、并发 Tick、禁用竞态、
Knowledge/Skill/NO_CHANGE、三类重复、非法结果、崩溃窗口、UNKNOWN 同 Attempt 对账、Store-only
reconcile、确定性半开窗 report、1024/1025、Backup/Restore 与权限/安装/激活零变化。CLI 完整包
测试与 vet 通过。最终源码上的 `go test -count=1 ./...`、`go vet ./...` 与 `go mod verify` 全部
通过；Docs（38 份 Markdown）、Capability Matrix（49 项、0 stable）、License（35 个 Go dependency、
55 个 distributed asset）、Branding 与 PublicTree 门禁通过。仓库凭据形态以及 exe/SQLite/log/cache/
临时 artifact 扫描均为零残留。本节不把开发切片冒充后台 Worker、自动 Review/Materialize/Apply、
主动外部检索、生产部署或公开 Beta。W4 计划范围完成，下一工作包为 `W5_NEXT`。

### 8.12 W5-X1 Workspace Transfer 增量验收（2026-08-09）

```text
W5_D1_COLLABORATION_REPAIR = ACCEPTED_DEVELOPMENT_SLICE
W5_X1_WORKSPACE_TRANSFER = ACCEPTED_DEVELOPMENT_SLICE
W5_F1 = NEXT
ORDINARY_TABLES = 24
CROSS_WORKSPACE_CHILDREN = 1
REQUEST_TRANSFER = VERIFIED
RESULT_TRANSFER = VERIFIED
FIRST_PRODUCTION_DEPLOYMENT = NOT_APPROVED
RELEASE_READY = NOT_CLAIMED
```

X1 只在显式、默认关闭的 Composite 装配上启用 Transfer。未指定对端 Workspace 的普通
Composite 保持原路径；指定对端时，REQUEST 必须同时通过 source-send 和 target-receive，
RESULT 必须同时通过 target-send 和 source-receive。跨 Workspace 上下文只消费不可变
`workspace-transfer-envelope/v1` 引用：REQUEST 仅含 trusted compiler 生成的 `TASK_SUMMARY`，
RESULT 仅含严格 `SPECIALIST_RESULT`；不传递完整 History、Memory、知识库正文、Secret 或
SecretRef。

真实 `TestW5X1DLiveDeepSeekWorkspaceTransfer` 长链的脱敏证据为：

- 模型 HTTP 请求严格 `4/4` 返回 2xx，物理 family 为 `7` 个 Run；实际执行两个
  Specialist、一个 Reviewer 和一个 Root，共 `4` 个 Model Attempt。其中恰好一个
  Specialist Child 位于第二 Workspace；两个 repair Specialist 和 Reviewer1 这 `3` 个
  预冻结 repair-branch 物理节点均为 nil Attempt，没有额外模型调用；
- REQUEST envelope 和 RESULT envelope 均已验证；Reviewer 与 Root 消费同一个跨
  Workspace RESULT，RESULT 闭合到该 Child 的实际成功 Attempt，Root 只在 Reviewer
  `APPROVE` 后 merge；
- 同一请求 exact retry 返回原 family：新 HTTP 请求为 `0`，新 Model Attempt 为 `0`，
  持久化 Attempt identity 与 Usage 均未变；
- 四个 Attempt 聚合 Usage 为 input `3443`、cached input `768`、uncached input `2675`、
  output `1353`，reasoning token 保持 `UNKNOWN`；`768 / 3443 = 22.306128%` 缓存命中率；
- 按冻结 DeepSeek Flash PriceSnapshot 重算的 estimated cost 为 `0.00539636 CNY`；
  provider-reported cost 与 reconciled cost 均保持 `UNKNOWN`，没有用本地估算伪装 Provider 账单。

当前 Store identity 已在同一从未部署的 Schema Draft 上重新冻结，仍未增加第二 Store、
Runtime、Loop、Gateway 或效果账本：

```text
ordinary tables = 24
schema fingerprint = 98d658e68907f77516ce0366a7b08bfa3851600326850590ed79584f581fa588
migration bytes = 44215
migration SHA-256 = 0a6701405e471e1ee7f41ea89ed62b50e5a4723e71b320e50f073cdd3e6b727a
```

W4-L4 的 24 表 identity、W4 各阶段验收与 S3-C 历史实验仍是原切片的不可回填证据。
X1 没有将 Workspace 变成 Agent 的固定知识容器，也没有将 Transfer、Reviewer、Role、
Persona、RAG、Memory、Skill、MCP 或 Channel 固化进 Core；未授权跨 Workspace 仍 fail-closed。
唯一下一开发入口为 `W5-F1`，用于收口持续负载公平、取消、重启、UNKNOWN 原 Attempt 对账、
Backup/Restore 和权限负例。本节不提前进入 W6，且继续保持
`CUTOVER_NOT_APPLICABLE_NEVER_DEPLOYED`、`FIRST_PRODUCTION_DEPLOYMENT = NOT_APPROVED` 与
`RELEASE_READY = NOT_CLAIMED`。

### 8.13 W5-F1 Collaboration Stability 与 W5 完成验收（2026-08-09）

```text
W5_D1_COLLABORATION_REPAIR = ACCEPTED_DEVELOPMENT_SLICE
W5_X1_WORKSPACE_TRANSFER = ACCEPTED_DEVELOPMENT_SLICE
W5_F1_COLLABORATION_STABILITY = ACCEPTED_DEVELOPMENT_SLICE
W5_COMPLETE = ACCEPTED_DEVELOPMENT_SLICE
NEXT_FUNCTION_ENTRY = W6
ORDINARY_TABLES = 24
CUTOVER = NOT_APPLICABLE_NEVER_DEPLOYED
FIRST_PRODUCTION_DEPLOYMENT = NOT_APPROVED
RELEASE_READY = NOT_CLAIMED
```

F1 修复并冻结了一项真实 P0：公平 Scheduler 先前把 Decision family 中预冻结但 dormant/skipped
的 repair Run 当作未终止 sibling，可能使 Reviewer0 永远不可 claim。当前 Decision family 的
runnable 集合只能由既有 Store-derived `loadCompositeDecisionFrontier` 产生，并继续在同一
`BEGIN IMMEDIATE`、同一 `ClaimFairRun → RunClaimed → Universal Loop` 链内执行三层 cap、fenced
lease、`served_units` 与取消检查。dormant/skipped repair Run 保持 `Attempt=nil`、零 claim；
legacy Composite 继续走原 sibling 终态路径，没有第二套 Scheduler、Queue 或 Runtime。

持续负载公平证据使用三个始终 runnable 的 Workspace，共完成 `900` 次 claim，并在第 `450` 次
后关闭、重开同一 Store。每个 Workspace 最终恰好服务 `300` 次，首次服务不晚于第 `3` 次
claim，最大服务间隔不超过 `3`。这是单机、单 Store、tenant-bound 的确定性本地证据，不声明
分布式公平、跨 Tenant 配额或生产 SLA。

纵链与恢复验收还覆盖：

- Scheduler round-0 APPROVE、单槽 repair→round-1 APPROVE 与真实跨 Workspace Transfer；
  dormant/skipped repair 始终零 claim；
- Specialist 反向完成后仍按 frozen plan order 归位；关闭并重开 Store 后 Run/Attempt/Result
  identity、排序和 canonical bytes 不变；
- family cancellation 原子覆盖跨 Workspace 的完整 `2N+3` family；取消后不能 Begin 新 Model
  Attempt，且 Transfer payload/envelope 与其他新效果均为零；
- Transfer `MODEL_UNKNOWN` 只接受可靠 canonical reconciliation evidence 收口同一原 Attempt；
  `SUCCEEDED` 原子生成唯一 RESULT，`FAILED` 不生成 RESULT。无 evidence、stale revision、换
  Provider/Binding、换 Attempt 均 fail-closed、零部分写，exact Begin retry 不取得调用许可；
- 完整 Decision+Transfer bundle 通过 create→verify→restore→`OpenExistingCurrentStore`→family
  resolve；跨 Workspace PENDING 只恢复成同一原 Attempt 的 UNKNOWN，REQUEST 保留，exact retry
  与 Universal Loop guard 均为零 Provider replay，且不伪造 RESULT；
- 权限负例覆盖缺失/单边 grant、REQUEST/RESULT 四方向、Tenant、双方 Workspace exact version、
  grant revision、direction、kind/schema/ref、真实 ContentRecord bytes/ref/size、32/48 KiB
  ceiling 与撤权后的新 Admission。历史 exact retry 继续使用 Admission 时冻结的授权并返回原事实；
- Pure Chat 和未启用 Transfer 的 ordinary Composite 保持零 Transfer material、ContentRecord 与
  相关外部调用。

F1 独立真实 DeepSeek canary 的脱敏证据为：

- 模型 HTTP 请求恰好 `4/4` 返回 2xx；REQUEST/RESULT Transfer 均 verified；
- 同一请求 exact retry 的新 HTTP 请求和新 Model Attempt 均为 `0`；
- Usage 为 input `3,149`、cached input `768`、uncached input `2,381`、output `1,492`；`2/4`
  个请求报告 cache hit，token-weighted cache hit rate 为 `24.388695%`；
- 冻结 PriceSnapshot estimated cost 为 `0.00538036 CNY`；reasoning token、provider-reported
  cost 与 reconciled cost 均保持 `UNKNOWN`。

本 canary 是新的 F1 证据，不覆盖 §8.12 的 X1D 历史 Usage/费用，也不能外推为稳定缓存率、模型
质量、生产价格或生产 SLA。测试凭据、动态 Run/Envelope identity、模型正文、临时 Store、Backup、
日志、可执行文件和缓存均不得写入本文或仓库。

F1 没有新增 Runtime、Store、Loop、Gateway、Scheduler、Queue、Worker、Transfer/Usage Ledger、
表、migration 或协议；F1 收口时的 Store 继续使用 §8.12 的 24 表、fingerprint 和 migration
bytes/hash。Windows 全仓测试、`go vet ./...`、`go mod verify` 与 gofmt 门禁已通过。WSL ext4、
Go 1.26.5、GCC/CGO 的首轮七包 race 中，corecontract、controlcontract、assemblycompiler、
runscheduler、localchat 与 currentbackup 通过且为零 data race；currentstore 唯一失败为测试使用
1 秒墙钟 TTL，并非 data race，因此该首轮七包命令不得记为整体 exit 0。测试固定逻辑时间后，
定向 race 与单核慢调度通过；最终完整 `go test -race -count=1 ./internal/currentstore` exit `0`，
耗时 `466.241s`。

`W5_COMPLETE_ACCEPTED_DEVELOPMENT_SLICE` 只表示 W5 四个原子开发切片闭合，不改变最初的模块化
方向：Team、Reviewer、Transfer、Role、Persona、RAG、Memory、Skill、MCP 与 Channel 仍是可选
外挂，Agent/Workspace 继续轻量且可自由编排，领域知识继续位于共享授权 RAG，UNKNOWN 继续禁止
语义重放、换 Provider 或替代 Attempt。W2-D 本地显式装配 v1 当前为
`W2_D_LOCAL_ASSEMBLY_ACCEPTED_DEVELOPMENT_SLICE`；W2-E2 受信 `text.stats` 统一 Apply 为
`W2_E2_TRUSTED_TEXT_STATS_APPLY_ACCEPTED_DEVELOPMENT_SLICE`；W2-E3 Workspace Channel Apply 为
`W2_E3_WORKSPACE_CHANNEL_APPLY_ACCEPTED_DEVELOPMENT_SLICE`；W2-E4 DeepSeek Model replacement 为
`W2_E4_DEEPSEEK_MODEL_REPLACEMENT_ACCEPTED_DEVELOPMENT_SLICE`；E5-A Requires/permission grant 为
`W2_E5_A_REQUIRES_PERMISSION_GRANT_ACCEPTED_DEVELOPMENT_SLICE`；固定 Document Insight 双 Port 为
`W2_E5_B_DOCUMENT_INSIGHT_DUAL_PORT_ACCEPTED_DEVELOPMENT_SLICE`；窄 REMOTE Action Host 为
`W2_R1_REMOTE_ACTION_HOST_ACCEPTED_DEVELOPMENT_SLICE`。这些 accepted 都只覆盖各自窄范围；
Operator Module Apply 与 Module Conformance 保持 experimental，任意第三方进程内 Action、其他
REMOTE/WASM、公网或任意第三方 Channel Provider、跨 Provider/多 Provider Model、通用 multi-Port/
多 Binding、自动路由、广义通用装配与 W6/W7 保持 planned。该段记录 W2-R1 收口时的历史边界；
`W6_NEXT` 只是 W5 路线的历史 marker，当时全局下一入口为尚未验收的 `W2_R2_WASM_HOST`，不表示首次生产部署、公开 Beta 或
`RELEASE_READY` 已获准，
`CUTOVER_NOT_APPLICABLE_NEVER_DEPLOYED` 与 `FIRST_PRODUCTION_DEPLOYMENT = NOT_APPROVED` 均保持。

## 9. 原始设计影响复核

| 检查项 | 当前架构的影响 |
|---|---|
| Agent 与 Workspace 自由组合 | 保留；Seed 只是最小示例，新 Run 仍以显式引用重新组合 |
| 模块自由外挂 | 保留；Role/静态 Skill、本地只读 Knowledge、Agent Memory、受信 MCP、compiled `text.stats`、Workspace-owned loopback Channel Endpoint 与窄 DeepSeek Model replacement 已通过统一 Apply/Dry-run 边界装配。Channel target 固定为 `WORKSPACE_CHANNEL_ENDPOINT`，Control/Catalog 与 revision-0 Cursor seed 原子发布；Model 只允许同一已批准 artifact/adapter/provider Instance 内显式 flash↔pro，并闭合 Price、Authority、SecretRef identity 与可选 Profile。E5-A 只给 Knowledge 增加 same-Profile exact Model Require 与受 Core 裁剪的 `knowledge.read`；E5-B 只给固定 Document Insight identity 增加 Context→Action 两步 Apply、Action→Context 两步 Disable 与两个优先 exact selector，不建立第二张图或第二个授权库，也不开放通用 multi-Port/多 Binding。R1 只增加默认关闭的 exact `REMOTE/freeagent-action-http/v1` 外挂 Action Host，endpoint/SecretRef 仍由 Core grant；R2 再增加默认关闭、零 Host capability 的 exact `WASM/freeagent-action-wasm/v1` 纯计算外挂 Host。二者执行都只能穿过原 Gateway，不能泛化为其他 REMOTE/WASM、任意第三方进程内代码或生产级不可信隔离。`expected_runtime_request` 只是 Operator expectation，Core 才按 exact Port/request/consumer schema 选择固定 handler。artifact grant 仍由 Core-owned handler 裁剪；私有 executor 只限制执行权，不把知识、记忆、工具、模型或渠道固化进 Core |
| 专业、复合与多 Agent | Parent/Child + Specialist、默认关闭的公平 Scheduler、可选 Reviewer、W5-D1 最多一次预冻结有界修复与 W5-X1 双边授权窄 Transfer 已接入；W5-F1 在同一 Store-derived frontier 上闭合持续负载公平、稳定归位、完整取消、恢复、UNKNOWN 原 Attempt 对账和权限负例。W4-L2 独立 Learning Reviewer、W4-L3 无权限 Version/Operator 交接及 W4-L4 显式周期纵链的历史验收保持不变。自动选择 Reviewer、无界返工、动态图、后台 Worker 与未授权跨 Workspace 内容传递仍 fail-closed |
| 权限、审计和副作用安全 | 加强；seed 不自授权，未对账状态阻止切换 |
| Runtime/Store 事实源 | 各一份；无双写、旧 Store 依赖或运行时 fallback |
| Universal Loop 模块无关性 | 保留；Loop 只识别冻结 Port 合同、通用 Action 与 Channel send 状态，不识别知识、工具或渠道模块的名称、版本或实现品牌 |
| Pure Chat 轻量路径 | 保留；无画像不读画像 CONFIG，无对应动态 Binding 时不读取 RAG/Memory/Action/MCP/Channel artifact、Adapter、Host、Memory Store、Action SQL、Channel config/Cursor/SecretRef 或 SDK；未选择 Composite/Reviewer 时不读取 family/reviewer 投影；Store-only 启动事务也不加载任何可选模块内容 |
| 85%/100% 上下文策略 | 实现原始约定；85% 确定性压缩可复用经验证的直接前驱连续最旧前缀摘要并重算当前 token，100% 只按序移除达到预算所需的最小完整旧前缀，不清空持久历史 |
| 当前多轮边界 | W1 已完成真实 DeepSeek 50 轮、head/revision CAS、一轮一 Run、阈值、loopback HTTP create/chat、服务重启、正常 OS 进程 25+25 轮和备份恢复续聊；Conversation 与 Composite 仍互斥，Conversation+Action/Channel 不声明成熟，且不冒充开放式生产多轮或公开 Beta |
| 证明代码与真实消费者 | S1、S2.1、ModelProfile、RAG、Memory、Action、窄 MCP、Channel、Composite 与 W3 K0–K3/M0–M2 分批验收；W2-E2 从普通 Pure Chat Store 经统一 Apply 闭合 compiled `text.stats` 的真实 model→action→model、exact retry、Disable 与 Backup/Restore。W2-E3 又以同一 Agent/Profile 和共享 Channel Instance 闭合双 Workspace/Endpoint 隔离、duplicate 零重发、Endpoint-scoped Channel `UNKNOWN`、Disable 引用保留及 Backup/Restore 后续推；两批均明确为网络无关或本地 loopback 证据。W2-E4 在同一 Apply/Catalog CAS 内闭合 flash↔pro、回滚、跨 Workspace/Profile、Usage/cost、UNKNOWN 原 Attempt 与 Backup/Restore，并明确未调用真实 API。W2-E5-B 从 Pure Chat Store 经固定 Document Insight 的 Context→Action 两步 Apply，在同一 Run 闭合真实本地 RAG、Gateway `text.stats`、双 Model Attempt、Action Attempt、result reservation、exact retry、Backup/Restore 与反向 Disable，并明确未调用真实 API。W2-R1 又从 exact Apply/Catalog 穿过 production lazy loader、native Adapter 与唯一 Gateway，闭合 POST 前 fail-closed、篡改拒绝、Disable 零访问、一次 Provider `FAILED`、UNKNOWN/终态失败、重启及完整 Backup/Restore；这些是分层 hermetic 证据，不冒充真实公网 Provider E2E。W2-R2 再从统一 Apply 穿过 production lazy loader、原 PENDING Attempt、唯一 Gateway 与 native WASM Host，闭合 deterministic success/FAILED、canonical local Usage、exact retry、构件删除后的终态/UNKNOWN 零重放、终态事务失败恢复和完整 Backup/Restore；它不冒充 OS/container 或生产级恶意多租户隔离。可选能力均经过真实 composition、Store/Loop、重入或 backup 链，W1 真实 50 轮与其他 canary 分别记录、不互相冒充。W4-L2 有独立真实 DeepSeek PASS 与全状态 Backup 篡改矩阵；W4-L3 有 Store/CLI/module-verify/dry-run/恢复后重导出证据；W4-L4 有严格合同 canary、唯一修正重跑、显式 Tick、Store-only reconcile、只读 report 与完整 Backup 证据；W5-X1D 与 F1 分别保留各自 4/4 真实 2xx、REQUEST/RESULT、Usage/费用和 exact retry 零新效果证据，F1 另闭合 Scheduler/取消/恢复/权限稳定性矩阵 |

本清单的开发切片验收不改变最初的开放架构目标，也不把尚未实现的远程 RAG、后台
学习式 Memory、其他 REMOTE/WASM Action、真实第三方公网互操作、完整/远程 MCP、公网 Channel Provider、不可信本地
进程、Reviewer 自动选择/无界返工循环、动态图、任意跨 Workspace 内容共享或后台 Learning 自动治理
提前声明为可用。W5 只允许显式双边 grant 下的窄 REQUEST/RESULT envelope；其余跨 Workspace 内容
继续 fail-closed。它
证明 S1 仍是唯一后续开发基础，且本地 RAG 路由/exact reuse、Agent 轻量 Memory/统一 Apply、
Conversation Summary 持久复用、本地 Action/Gateway、Operator 完全信任的窄 MCP stdio Tool 与默认关闭的
Workspace-scoped loopback Channel、同 Workspace depth-1 Composite 纵链、默认关闭且不
理解任务内容的公平 Scheduler、可选 Reviewer、最多一次预冻结有界修复、双边授权 Workspace Transfer
  及 F1 稳定性开发切片已在其上闭环；W2-D 本地显式装配 v1、W2-E2 受信 `text.stats` 统一 Apply、
W2-E3 Workspace Channel Apply、W2-E4 窄 DeepSeek Model replacement、W2-E5-A 窄
Requires/permission grant、W2-E5-B 固定 Document Insight 双 Port、W2-R1 窄 REMOTE Action Host、
W2-R2 窄 WASM Action Host、W2-R3 第三方纯计算/停机撤权、W2-U0 供应链纯合同、W2-U1 本地
governed observation、W2-U2 持久 Source/Snapshot observation、W2-U3 离线 Upgrade Review 与 W2-U4
approved Declarative Profile Context replacement 已按各自范围完成窄开发验收，U4 历史状态为
`W2_U4_OPERATOR_APPLY_ACCEPTED_DEVELOPMENT_SLICE / W6_0_CONTROL_API_CONTRACT_NEXT`。
U3 自身不下载 package，也不执行 staging/Install/Activate/grant/Bind/Apply；U4 只在 exact approval/current/
source 闭包下重用既有 Apply/CAS。Operator Module Apply 与 Module Conformance 仍 experimental，任意第三方进程内 Action、其他 REMOTE/WASM、
公网或任意第三方 Channel Provider、跨 Provider/多 Provider Model、通用 multi-Port/多 Binding、
自动路由、广义通用装配、W6 在线控制面与 W7 仍 planned；W6.0 当前只进入合同设计；
未来首次生产部署、生产级 sandbox、公网 Provider 与最终发布仍需独立授权和门禁。
